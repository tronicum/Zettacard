# Board

See `docs/KANBAN.md` for column rules, WIP limits, and definitions of ready/done. See `docs/PERSONAS.md` for who does what.

## Done

- **DN-69 — Module 2A (CRA Secure Supply Chain & Vulnerability Handling): first non-DORA dossier this session, EN-canonical, catches two live CRA corrigenda and a roadmap category error (2026-08-16)** — Fifth module in this session's B2B batch, and the first on a different regulation: the EU Cyber Resilience Act (Regulation (EU) 2024/2847), not DORA. `cra_supply_chain` module: 20 questions across 4 topics (scope_dates/manufacturer_duties/sbom_vulnerability/reporting), reading CRA Art. 3/13/14/64/69/71 and Annexes I-II in full via the Cellar route, EN+DE. Deliberately **EN-canonical, not DE-canonical** (a first for this project's compliance modules) - the roadmap doc's own audience for this module is EN/PL/RO DevSecOps/CTOs in Poland/Romania tech-hub markets, not a DE-first audience like every DORA module. Confidence distribution: 14 Tier A, 6 Tier B, zero Tier C; 110 quoted legal fragments programmatically re-matched against retrieved OJ text (6 German quotes caught and corrected during that pass). **Two live corrigenda caught, relevant to anyone drafting CRA content from the original OJ PDF**: Art. 64(10) as originally published cross-referenced the wrong paragraph range for the small-manufacturer fine derogation (fixed by corrigendum to cover the actual Art. 13/14 fine paragraph); and the German-language Art. 13(8) originally contained an extra clause ("der erwarteten Produktlebensdauer und") with no English equivalent, silently creating a longer vulnerability-handling window in German-market material - deleted by a later corrigendum (OJ L, 2025/90555). **Key finding**: the roadmap's Art. 14 framing needs correction, though less severely than the DORA incident-reporting module's did - both the exploited-vulnerability and severe-incident notification tracks are actually symmetric at 24h/72h from awareness (not from the early-warning stage), diverging only at the final stage (14 days after a fix is available, vs. one month after the 72h notification for incidents); the CRA's own "intermediate report" is discretionary (CSIRT-requested), not mandatory like DORA's; and reports go to the CSIRT coordinator plus ENISA simultaneously, not a market-surveillance authority. **Both roadmap compliance dates (11 Sept 2026, 11 Dec 2027) check out against Art. 71(2) verbatim** - but a third date the roadmap misses (Chapter IV applies from 11 June 2026) and a real scope trap were found: Art. 69(3) applies the Art. 14 reporting duties to the ENTIRE installed base placed on the market before 11 Dec 2027, not just new post-2027 products. **The CRA mandates no specific SBOM format** - Annex I Part II(1) requires only "a commonly used and machine-readable format," naming none; CycloneDX and SPDX are industry convention, not CRA law; VEX is a concept implemented by multiple formats (CSAF/CycloneDX/SPDX/OpenVEX), not a format itself - flagged the roadmap's own sample-MCQ idea ("building a VEX file") as a category error that should be rewritten before use elsewhere. German-orthography QA run and independently re-verified (not just self-reported): 610 real umlaut/ß characters in the German fields, zero ASCII-transliteration residue.

- **DN-68 — DORA Module 5 (Incident Reporting & the 4-hour rule): dossier + draft pilot, closes a Tier-C gap two prior dossiers flagged and left open, corrects the roadmap's own framing (2026-08-16)** — Fourth DORA B2B module this session (`dora_incident`, docs/dora-incident-pre-review-dossier-2026-08-16.md + data/dora_incident_pilot_DRAFT.json + data/gen_dora_incident_draft.py generator script, all deliberately unwired/uncommitted-to-build). 20 questions across 4 topics (vorfallmanagement/klassifizierung/meldefristen/meldeinhalt), reading DORA Art. 17-20 plus three Level 2 instruments in full EN+DE via the Cellar route: Delegated Reg. (EU) 2024/1772 (incident classification RTS), Delegated Reg. (EU) 2025/301 (reporting-timeline RTS - this is where the actual deadline language lives, not in DORA itself), and Implementing Reg. (EU) 2025/302 (report templates/procedures ITS). Confidence distribution: 14 Tier A, 6 Tier B, **zero Tier C** - this deliberately closes the exact gap both the 2026-08-13 `dora` dossier and this session's own `dora_executive` dossier had flagged and left open (the 4-hour/72-hour/one-month figures were never previously verified against primary Level 2 text). 20 verbatim citation quotes programmatically re-matched against the retrieved OJ text, 20/20 exact. **Key finding, and the most commercially important correction of this whole DORA batch**: the roadmap doc's own framing ("timing starts at formal Major Incident classification, not first alert") is only half right. Art. 5(1)(a) of Delegated Reg. (EU) 2025/301 sets the initial-report deadline as **the earlier of** 4 hours from classification **and** 24 hours from awareness ("and" joins both limbs) - classification-only timing is correct just for the one case where classification happens more than 24 hours after awareness (Art. 5(2)). Worked example in the dossier: aware Monday 08:00, classified Tuesday 06:00 → the report is due Tuesday 08:00 (two hours after classification), not four - a team trained on the roadmap's sentence alone would file late while believing it was early. The intermediate report (72h) runs from submission of the initial notification, not from classification; the final report (one month) runs from the (latest) intermediate report - neither stage's clock is "from the incident" at all. **Shipped content found to need a correction, not yet fixed**: `dora_pilot.json`'s `dora-meldepflichten-03` states the intermediate report is due 72 hours "of classification" (should be: from the initial report's submission), and `dora-meldepflichten-02`'s explanation mischaracterizes the 24-hour awareness limb as internal best-practice guidance rather than what it actually is, a binding outer limit on the initial report deadline - both questions still pick the least-wrong answer option, so the defect is in the explanation text, not the answer key. Also noted: DORA's own text contains zero occurrences of "four hours"/"24 hours"/"72 hours" in either language - any training material citing "Art. 19 DORA" for these figures is citing a provision that doesn't contain them, and neither of the two prior DORA-related dossiers this session had named Implementing Reg. (EU) 2025/302 at all, even though six of this module's twenty questions are unanswerable without it. German-orthography QA (the real bug caught and fixed in the immediately preceding Module 4 round) was run proactively this time: 397 verified real umlaut/ß characters, zero ASCII-transliteration residue, confirmed independently rather than only by the authoring subagent's own self-report.

- **DN-67 — Root-caused the Netlify Blobs storage bug from DN-66's badge work; issue-badge/get-badge converted to Functions v2 (2026-08-16)** — The `hasToken: false`/`MissingBlobsEnvironmentError` bug that blocked badge storage since this session's earlier badge-hashing round (see the badge-hashing entry below) is now root-caused and fixed in code, though NOT yet confirmed live - see "Not done" below. Diagnosed by deploying three harmless, non-secret probe env vars alongside a temporary diagnostic in `issue-badge.js`: a probe named with the exact "NETLIFY_" prefix, and a second probe with the more specific "NETLIFY_BLOBS_" prefix (matching Netlify's own auto-injected `NETLIFY_BLOBS_CONTEXT` var name), both reached `process.env` fine - ruling out any env-var-name-reservation theory. A third probe, identical in every way except `envVarIsSecret: true` (matching the real `NETLIFY_BLOBS_TOKEN`'s own secret flag), did NOT reach `process.env`. **Conclusion: this site's zip/API deploy method (CLI/MCP upload, not git-linked continuous deployment) never injects secret-flagged env vars into a Functions v1 runtime at all** - a Netlify platform behavior, not a config mistake in this repo (two earlier rounds this session had already ruled out scope/context settings). The fix chosen was NOT to un-mark the token as non-secret (that trades the bug for a real security regression - a Netlify Personal Access Token sitting in cleartext in the dashboard - and this session's safety classifier had already correctly blocked that exact action twice in an earlier round). Instead: `issue-badge.js`/`get-badge.js` (Functions v1, `exports.handler`, event/callback style) became `issue-badge.mjs`/`get-badge.mjs` (Functions v2, `export default`, Request/Response style) and `lib/identity-hash.js` became `identity-hash.mjs` (ESM export, identical logic) - Functions v2 gets Netlify's automatic, zero-config, per-deploy-scoped Blobs credentials, confirmed live via a throwaway `blobs-v2-probe.mjs` test function (deployed, called `getStore()` with zero options, wrote and read back a blob successfully) before converting either real endpoint, specifically to avoid discovering a v2-specific gap only after committing to the rewrite. `blobsStoreOptions()` and the `NETLIFY_BLOBS_TOKEN` dependency are gone entirely - no secret to manage, rotate, or leak. `jose`/`@netlify/blobs` are now static ESM imports instead of the lazy-import-with-cache workaround v1's CommonJS bundling needed (irrelevant once the file itself is ESM). All four temporary probe env vars were deleted after diagnosis; `NETLIFY_BLOBS_TOKEN` itself was deliberately left in place (harmless dead config, not yet confirmed unneeded until the new code is actually live) rather than removed pre-emptively. **Not done / the real limitation this round**: could not confirm the fix live from this sandbox. The available deploy pathway (`npx @netlify/mcp@latest` CLI via a site-scoped proxy-path token) got stuck serving an identical, byte-for-byte-same-digest stale function bundle across **5 consecutive deploy attempts** with fresh tokens each time, spaced 90+ seconds apart, even after touching file mtimes to rule out a timing/snapshot-lag theory - each attempt reported `state: "ready"` with a new deployId, but the actual runtime behavior (tested via direct curl against both the site alias and the exact per-deploy subdomain URL, not just deploy metadata) kept executing the old v1 code. This is a tooling limitation of this session's deploy pathway, not a defect in the new code (which passed `node --check` and mirrors the old logic field-for-field otherwise) - flagged rather than declared done, per this project's own standing discipline against self-reported looks-right claims. Committed and bundled to the user's Mac (`zettacard-blobs-v2-fix.bundle`) for a real deploy from there or via git-linked CD once pushed, which should not hit the same caching behavior.

- **DN-66 — DORA/CRA B2B training roadmap: three Opus-reviewed legal dossiers + draft pilots for Module 2B (Art. 30 procurement), Module 1A (Executive DORA Briefing), and Module 4 (Information Register) (2026-08-16)** — Kickoff of a new B2B expansion initiative the PO had originally scoped for January but explicitly pulled forward this session ("start with it... use as much tokens as you want... use opus for a legal review"), based on a distilled version of the PO's own prior Gemini research, saved to the Claude project as `claude/dora-cra-b2b-training-roadmap-2026-08-16.md` (9 modules total, TAM-ranked). Two parallel Opus subagents produced pre-review dossiers + `_DRAFT`-suffixed 20-question DE/EN pilot JSONs, deliberately **not wired into the app** (not in `build_modules.py`/`modules_manifest.json`, nothing built, nothing committed) pending human legal sign-off, matching this project's established pilot-then-scale discipline. Both dossiers explicitly re-did primary-source verification that the earlier 2026-08-13 `dora` dossier had flagged as only secondary-sourced — `eur-lex.europa.eu` is blocked by an AWS WAF JS challenge (HTTP 202, empty body) to both curl and WebFetch; the EU Publications Office's Cellar repository (`publications.europa.eu/resource/celex/<CELEX>`) was found and used as the working primary-source alternative (EN/DE via `Accept-Language`), a durable finding for future EU-regulation research in this project. **Module 2B (`dora_procurement`, docs/dora-procurement-pre-review-dossier-2026-08-16.md + data/dora_procurement_pilot_DRAFT.json)**: 20 questions across 5 topics on Art. 30 ICT-contract clause requirements, reading the base regulation plus two delegated RTS (2024/1773 on contractual content, 2025/532 on subcontracting) in full. Key finding: the "unrestricted audit and access right" language in the PO's original research is real (Art. 30(3)(e)(i)) but materially over-read — it's critical-tier-only (nothing in the Art. 30(2) baseline), qualified by its own anti-hollowing-out clause, subject to an express negotiated-alternative carve-out (Art. 30(3)(e)(ii)), and constrained by Art. 28(6)'s risk-based pre-determined frequency requirement. Also flagged (not fixed): the already-shipped `data/dora_pilot.json`/`dora_course.json` blur the Art. 30(2)/(3) tier split the same way, and use "IKT-Drittanbieter" where the official OJ German term is "IKT-Drittdienstleister". **Module 1A (`dora_executive`, docs/dora-executive-pre-review-dossier-2026-08-16.md + data/dora_executive_pilot_DRAFT.json)**: 20 questions across 4 topics for management-body/board audiences, built around Art. 5(2)'s board-duty enumeration. Key finding: the PO's original research's "personal liability / Berufsverbote / penalties up to 10% of annual revenue" framing does not hold up against DORA's own text — DORA sets no EU-wide fine amount and no management-ban power itself (Art. 50(3)-(4)); it only obliges Member States to make individuals addressable "subject to the conditions provided for in national law" (Art. 50(5)); the "10%" figure appears nowhere in DORA (traced to a Romanian national implementing ordinance) and DORA's only percentage figure is the unrelated Art. 35(6)-(8) 1%-of-daily-turnover penalty against critical ICT third-party providers, not financial entities or individuals. German national-law liability route (§25a/§36/§47a KWG, §93 AktG business-judgment-rule, §43 GmbHG) documented separately as the actual mechanism, with an open item flagging that this needs a lawyer's confirmation before any wording ships. **Both dossiers rate every citation "High" confidence** (verbatim primary-source reads; no Tier-C/secondary-only claims), with a small number of Tier-B items (legal characterization rather than recitation) explicitly called out as the highest-value use of a reviewer's time. **Module 4 (`dora_register`, docs/dora-register-pre-review-dossier-2026-08-16.md + data/dora_register_pilot_DRAFT.json)**: 20 questions across 5 topics (registerpflicht/registerstruktur/lieferkette/funktionsklassifizierung/datenqualitaet), reading Art. 28(3)+(9) plus the adopted Implementing Regulation (EU) 2024/2956 (ITS templates, in force, corrected by a September 2025 corrigendum that renumbers template B_06.01's criticality column from `.0060` to `.0050` — any earlier data model is wrong) in full. Key finding: the roadmap's own "annual mandatory register submission" framing is not what DORA says — Art. 28(3) contains three separate supervisory duties, none of which is an express annual full-register submission; the only yearly duty is an aggregate report on new arrangements/provider categories/contract types, and the full register goes to the authority only "upon its request." The real annual collection cycle is supervisory practice built on top of the text, not the text itself — copy should say "the annual submission your supervisor runs," not "the annual submission DORA requires." Also found: Nth-party subcontractor mapping is scoped to CIF chains only (unlimited depth), not "map everything" or "map tier 1 only," both common wrong framings. Shipped `dora_pilot.json`'s own register question (`dora-drittparteien-01`) was checked and holds up correctly. **Caught and fixed as a QA step**: the subagent that authored this module's DE content wrote it in ASCII-transliterated German throughout (e.g. "fuehrt"/"muessen"/"ausschliesslich" instead of "führt"/"müssen"/"ausschließlich") instead of proper orthography — inconsistent with every other module in the project. Caught by an independent verification pass (grep for real umlaut/ß character count = 0 across all "de" fields), then fixed by a dedicated follow-up subagent using a manually-vetted per-word correction (not a blind regex, to get German ss-vs-ß rules right), re-verified independently afterward (557 real umlaut/ß characters present, zero ASCII residue). Worth remembering as a real failure mode for future non-English content-authoring subagent tasks: JSON validity and schema checks alone don't catch this — an explicit character-class check for the target language's special characters is needed. **Not done this round**: any decision on Module 1B's overlap with the existing generic `dora` module (flagged to the PO for a call, not yet decided), and correcting the shipped `dora`/`dora_course` tier-blur + terminology drift found in §4.3 of the Module 2B dossier (logged as a known gap, deliberately out of scope for this batch).

- **zettacard-kb tooling repo (SQLite citation index + staleness lockfile) built; Sportboot Binnen/See replaced with the full 515-question official ELWIS catalog, deployed (2026-08-14)** — Follow-up to researching `HugoFara/boating-licence` (a third-party open-source German/French/Swiss boating-licence question bank with an interesting SQLite knowledge-base + staleness-checker architecture) for possible reuse. Two pieces shipped this round; a third (France/Switzerland modules) was explicitly scoped by the PO but NOT started — see "Explicitly deferred" below. **(1) zettacard-kb**: a new, separate repo (not a Zettacard subfolder — PO asked for "an extra repo/org at github" for KB tooling specifically) with an MIT-licensed SQLite indexer (`src/build_kb.py`) that reads every `data/*_pilot.json` in the main Zettacard repo and populates `modules`/`questions`/`citations` tables, plus a staleness checker (`src/staleness.py`) that fingerprints each distinct `legal_basis` citation string per module into a committed `data/citations.lock.json` lockfile and flags drift on re-run. Architecture modeled on (not copied from) `boating-licence`'s own `KnowledgeUnit`/staleness pattern. v1 run against the local checkout: 9 modules / 500 questions / 334 distinct citations indexed. **Known, disclosed limitations** (in the repo's own README, not hidden): (a) only reads local `data/*_pilot.json`, which is desynced from several live-only modules (sportboot_binnen/see, lksg, kartellrecht, kyc_aml, hinweisgeberschutz) — a `--from-live` mode pulling from `app/data/<module>/core.json` instead/in addition is flagged as a natural follow-up; (b) staleness-checking is citation-STRING-only (sha256 of the citation text itself) — unlike `boating-licence`'s real upstream-source fetching (PDF/HTML diffing against gesetze-im-internet.de/EUR-Lex/BSI), this v1 has no source-URL mapping yet, so it only catches "did the citation text change between runs," not "did the underlying law change" — real staleness detection against primary sources is future work, explicitly not oversold as done. Delivered as a git bundle (this sandbox can't push to GitHub directly, same constraint as the main repo) since the device bridge to the PO's Mac was intermittently unreachable this session despite a folder being shared. **(2) Sportboot Binnen/See ELWIS scale-up**: replaced the prior ~50 (Binnen) + ~30 (See) independently-authored question pools with the FULL official WSV/BMDV Fragenkatalog (Stand 2023-08) — 300 Binnen + 215 See = 515 questions, ingested via `boating-licence`'s sha256-fingerprinted pipeline. Question/option TEXT is the official catalog's own verbatim wording (§5(2) UrhG amtliches Werk — free reuse incl. commercial, with citation to www.elwis.de, unmodified), a different sourcing posture than every other module in this app (which use independently-authored paraphrases). **Two real gaps found and explicitly disclosed rather than silently shipped or silently worked around**: (a) the official catalog itself has ZERO explanations for any question — every one of the 515 explanations in this module is newly AI-authored (2026-08-14), grounded in the relevant BinSchStrO/SeeSchStrO/COLREG rule area, but is Zettacard's own new content, not part of the official catalog, and has not been independently legally reviewed — flagged explicitly in both pilot files' `legal_review_status`; (b) this round ships DE+EN only, not the full 12-locale set the prior (smaller) pool had — the other 10 locales are a tracked follow-up, not done. `app.js`'s existing `fetchLocaleTextWithFallback()` (originally built for LKW's own DE/EN-only gap) already handles this gracefully — non-DE/EN users get working English text with a `state.contentLangFallback` flag, not a crash. **Also found and fixed as a byproduct**: neither module had ANY `TOPIC_LABELS` entry before this round, despite being live in production since 2026-08-12 — the topic filter chips were silently rendering raw `topic_code` strings (`getTopicLabel()`'s `fallbackTopic` path) the whole time. Added real DE/EN labels for the new shared 9-topic taxonomy (`verkehrsregeln`/`lichter_signale`/`seemannschaft`/`schifffahrtszeichen`/`recht_dokumente`/`wetterkunde`/`gezeiten`/`navigation`/`umweltschutz`, inherited from the source repo's own theme classification and harmonized across both catalogs) that replaces the old separate 5-topic (Binnen: `vorfahrt`/`bezeichnung`/`schallzeichen`/`betonnung`/`seemannschaft`) and 4-topic (See: `betonnung`/`seefunk_vorfahrt`/`gezeiten`/`seewetter`) sets. `EXAM_TOPIC_DRAW` recomputed proportional to each topic's real question count in the new pool, then nudged so every topic with ≥1 question always has a nonzero draw chance (pure proportional rounding had zeroed out Binnen's `gezeiten`/`navigation`/`umweltschutz` and See's `recht_dokumente`, each 1-2-question edge topics from the official catalog's own real-world weighting). **Verified**: locally via Playwright (both modules load their full new question count with zero console errors; topic filter chips render real labels; explanation + legal-basis citation render correctly on reveal; Exam Simulation draws real questions from the new pool) and live post-deploy via `curl` against `zettacard.netlify.app` (300/215 question counts confirmed, new `TOPIC_LABELS` entries confirmed present in deployed `app.js`) — a live Chrome cross-check was attempted but the browser extension wasn't connected this round, curl+Playwright verification stood in for it. **Explicitly deferred, not started this round**: France (Permis Plaisance côtière/eaux intérieures, 146 questions) and Switzerland (190 questions) boating-licence modules, both scoped by the PO to be built from `boating-licence`'s own CC BY-SA 4.0 content with attribution — schema conversion, license/attribution handling, and module registration (`modules_manifest.json`/`TOPIC_LABELS`/`app/index.html` cards/exam-tuning maps) not yet touched. Also still open: a second, independent translation-fidelity verification pass (the source repo runs one via a dedicated subagent stage — `translate_answers/` then `translate_verdicts/` — that Zettacard's own translation batches don't currently have, beyond structural JSON validation) is worth considering given the one translation-corruption near-miss caught earlier this same week (it_sicherheit's Hindi batch).

- **DN-49 Phase 2: practice-quiz HTML shipped; it_sicherheit expanded 40→64; landing-page compliance-badge gap fixed; DN-12 legal-review package consolidated (2026-08-13)** — Four items in one session, in response to a live production bug report and a follow-up "any ideas?"/"do the other improvements" round. (1) **DN-52 Phase 2 practice-quiz HTML**: the practice-quiz JS shipped 2026-08-11 with zero matching markup (20 element IDs referenced by `app.js` didn't exist in `app.html` at all — the actual root cause of a same-session app-crash bug, previously worked around with existence guards). Added the real `#practice-picker`/`#practice-view`/`#practice-results` modals + `#exam-pick-practice` entry button, matching existing `.exam-modal`/`.exam-view` markup conventions (no new CSS). Also fixed `#practice-exit-btn`'s label, which was never set anywhere despite `PRACTICE_QUIZ_STRINGS.exit` already existing for it. Verified via a full local Playwright run (12-question mixed-scope flow, SRS localStorage populated, completions/certificate localStorage never touched, zero console errors) and re-verified live post-deploy via a real Chrome session on `zettacard.netlify.app` (same deployed code as `zettacard.de`, confirmed byte-identical via `curl -k`; `zettacard.de` itself was intermittently unreachable from this session's sandboxed network path due to an Anthropic egress-gateway TLS artifact, not a real production issue). (2) **it_sicherheit expanded 40→64 questions**: closed the documented content gap vs. SoSafe (`compliance-competitor-pricing-and-course-gaps.md` section 3) with 3 new topic categories — `social_engineering` (pretexting/tailgating/USB-drop/vishing/smishing/BEC/badge-sharing), `ransomware` (recognition/response/backup strategy/spread vectors), `ki_bedrohungen` (AI voice-clone fraud/deepfake video calls/AI-written phishing/safe AI-tool usage) — 24 new questions, 8/topic, DE+EN authored directly then translated into all other 10 locales via 3 parallel agent passes (one JSON-parse casualty from a mid-stream continuation boundary injecting a stray fence marker into one Hindi string, caught and hand-repaired before merge, not shipped broken). `TOPIC_LABELS.it_sicherheit` gained matching 12-locale entries. Verified via a full `build_modules.py`/`split_module()` rebuild (zero locale gaps across all 12 languages, 8 questions/topic) and a local Playwright pass (new filter chips render, all 64 questions load, spot-checked question/detail rendering, zero errors). (3) **Landing-page compliance-badge gap**: found while answering a user question about why some workplace-training module cards show a "Betriebliche Pflichtschulung" subtitle badge and others don't — `app/index.html`'s `subtitles` lookup table was simply missing `datenschutz`/`ki_act` entries even though both sit in the same `groupCompliance` group (same heading, same shared legal disclaimer) as `arbeitssicherheit`/`it_sicherheit`/`hinweisgeberschutz`, which did have entries. Nothing suggested this was intentional (the `if (subtitles[mod])` guard just silently skips rendering the span when an entry is missing) — added the two missing entries. (4) **DN-12 legal-review package consolidated**: built a single handoff index (`claude/legal-review-package-index-2026-08-13.md` in the Claude project, not the repo — same location convention as the 4 existing per-module dossiers) covering all 5 pending-review modules (DORA/NIS2/KYC-AML/Kartellrecht/LkSG) instead of leaving counsel to discover five separate documents cold. Notably: (a) found and flagged that Kartellrecht has *two* AI review docs from the same day, one superseding the other (the earlier `-legal-review-` pass's 3 fixes are already applied and confirmed in the later `-pre-review-dossier-`, which is the current authoritative one — a lawyer opening both cold could easily mistake this for two open, unreconciled reviews); (b) built LkSG's first-ever citation ledger (it had no dossier at all despite being live with 30 questions) by extracting the module's own `legal_basis` field per question directly from shipped content — explicitly labeled as a first-pass extraction, not an independent statute re-verification like the other four, with an open-items list mirroring what those four already did. **A genuinely separate PDF-certificate investigation was explicitly NOT done as new work**: BACKLOG.md's own outer "DN-49" summary card (further down, "Remaining, not yet done: a PDF-download variant...") was stale and contradicted its own detailed "DN-49 Phase 3" Done entry, which shows this shipped 2026-08-11 using the browser's native print-to-PDF specifically *instead of* jsPDF/html2canvas, to preserve this app's zero-dependency offline-first architecture — corrected that stale line rather than vendoring a PDF library against a documented, reasoned prior decision.

- **2026-08 content expansion round 3 — motorrad expanded 90→138, new module fuehrerschein_bus (48Q, D1/D1E/D/DE), registered and deployed (2026-08-12)** — Same overnight integration/deployment pattern as rounds 1/2 (entries directly below): content authored, independently fact-checked, and fixed in a prior session; this round did registration, validation, and delivery only, per instruction. **Content basis**: `motorrad` DN-65 Zusatzstoff expansion added 48 questions (IDs `-16` through `-27` on each of the 4 existing topics: `fahrphysik`/`schutzausruestung`/`verkehrsverhalten`/`fahrerlaubnis`, `besondere_bedingungen` left untouched at 30), sourced from § 6 FeV (A1/A2/A class definitions), Anlage 7/7b FeV (Zusatzstoff/Fahrschulung structure), § 21a Abs. 2 StVO (Schutzhelmpflicht), and EU-VO 168/2013 (ABS/CBS braking-equipment rules) — new hazard-perception, lean/countersteering physics, ABS/CBS braking-distribution awareness, helmet-standard/ATGATT protective-gear, filtering-legality/blind-spot/group-riding scenarios, and technical pre-ride-check content, DE/EN only for this round like the rest of tonight's new content (see `data/motorrad_pilot.json` `meta.description`). `fuehrerschein_bus` is an entirely new module (48 questions, 4 topics × 12: `pbefg_verhalten`/`lenkzeiten_bus`/`fahrzeugtechnik_bus`/`notfall_bus`), `class_scope` uniformly `["D1","D1E","D","DE"]` on every question (a sibling-classes model like `lkw`'s C1/C/CE, not an `extends`-based addon like BE), sourced from § 14/§ 15 BOKraft (Fahrgastverhalten, Beförderung von Sachen), § 22 PBefG (Beförderungspflicht), § 6 FeV (D-family class definitions incl. the D1 16-passenger/8m limit and D1E/DE 750kg trailer threshold), EU-VO 561/2006 Art. 3(a)/4(d)/6-8 (bus-specific local-service/multi-manning/occasional-service tachograph exemptions, not just the generic driving/rest-time rules), § 18 FPersV (10-17-seat non-commercial exemption), VO (EU) 181/2011 (bus passenger rights: reduced-mobility assistance, companion travel, mobility-aid damage compensation), and § 35g/§ 35h StVZO (fire-extinguisher and first-aid-material requirements) — full sourcing writeup in `claude/fuehrerschein-bus-module-2026-08-12.md`. Both modules carry the project's standard `legal_review_status: "NOT legally reviewed..."` caveat in pilot-json `meta`, unchanged this round; `fuehrerschein_bus`'s additionally flags the Anlage 7 FeV D-family point/question counts (D: 40 Fragen/138 Punkte, D1: 35 Fragen/121 Punkte) as not yet cross-checked against the module's current placeholder pass-rule. **Runtime data note**: `app/data/fuehrerschein_bus/` didn't exist yet at the start of this round even though `data/fuehrerschein_bus_pilot.json` already had all 12 locales merged in from prior work — ran only `data/gen_fuehrerschein_bus_i18n.py` (not `data/gen_fuehrerschein_bus.py` first, which would have overwritten the already-merged 12-locale pilot back down to DE/EN-only) to regenerate `app/data/fuehrerschein_bus/core.json` + all 12 `locales/*.json` from the existing merged pilot; `motorrad`'s runtime files were already fully expanded and 12-locale-complete going into this round, no regeneration needed. `data/build_modules.py` was again deliberately NOT run, per rounds 1/2's own documented near-miss (its `shutil.rmtree(APP_DATA)` doesn't know about any of tonight's module directories) — `app/data/modules.json` was hand-synced from `data/modules_manifest.json` by direct copy instead. **Registration work done this round**: `data/modules_manifest.json` (+ `app/data/modules.json`) — added `fuehrerschein_bus` as `scopeKind: "class"` with 4 options (`D1`/`D1E`/`D`/`DE`, each with a full 12-locale label, no `extends` needed since every question's `class_scope` already lists all 4 codes directly, same pattern `lkw`'s C1/C/CE uses); `motorrad` needed no manifest change (unregistered dimension is unaffected by pool size). `app/app.js` exam-tuning maps: `fuehrerschein_bus` added to `EXAM_QUESTION_COUNT_BY_TYPE` (28, of a 48-question pool), `EXAM_TIME_LIMIT_MS_BY_TYPE` (30 minutes), `MAX_ERROR_POINTS_BY_TYPE` (8, the established ~1/3-of-draw ratio), and `EXAM_TOPIC_DRAW` (7/7/7/7 across its 4 even topics); `motorrad` was checked and left unchanged — it has no entry in any of these 4 maps (falls through to the generic driving-exam defaults: 30-question draw, 45-minute limit, 10 error points, uniform-random topic top-up), and since the default draw of 30 is well under even the old 90-question pool, growing to 138 doesn't create an under-draw problem, so no override was added. `app/index.html` — full 12-locale `MODULE_LABELS` entry for `fuehrerschein_bus` (`de`: "Führerschein Bus (D1/D1E/D/DE)", each locale independently translated), subtitle reusing `s.theoryExam` (same pattern as `fuehrerschein`/`motorrad`/`lkw`), and added to the existing `groupDriving` group alongside `fuehrerschein`/`motorrad`/`lkw`/`angelschein`/`angelschein_bayern`/`angelschein_nrw` (checked first — round 2's angelschein variants were already there, not duplicated). **Validation**: `node -c app/app.js` passes; `node -e` parsing both inline `<script>` blocks extracted from `app/index.html` via `new Function()` passes for both; all 30 touched/new JSON files (`data/modules_manifest.json`, `app/data/modules.json`, `data/fuehrerschein_bus_pilot.json`, `app/data/fuehrerschein_bus/core.json` + all 12 `locales/*.json`, `data/motorrad_pilot.json`, `app/data/motorrad/core.json` + all 12 `locales/*.json`) parse cleanly via `python3 -c "import json; json.load(...)"`; `fuehrerschein_bus`'s `core.json` question-id set matches all 12 of its `locales/*.json` files exactly (48 ids, zero missing/extra in any locale); `motorrad`'s `core.json` question-id set matches all 12 of its `locales/*.json` files exactly (138 ids, full old+new set, zero missing/extra in any locale). **Deployed**: staging (`480e3ec6-76f6-414e-a7bc-eb3e661f5816`) first, curl-verified, then production (`b244f9b2-e45a-48c0-9f59-0405f587c213`), curl-verified again — per the PO's standing explicit authorization for both staging and production deploys this session, given directly in response to a direct question. **Explicitly deferred, not built this round** (per `claude/content-expansion-scoping-2026-08-12.md`): Angelschein coastal-core Länder (Schleswig-Holstein/Mecklenburg-Vorpommern/Niedersachsen/Hamburg/Bremen) and inland-core Länder (Hessen/Rheinland-Pfalz/Saarland/Sachsen/Sachsen-Anhalt/Thüringen/Brandenburg/Berlin); Sportbootführerschein SKS/SSS (no standardized public elwis-hosted catalog); Führerschein LKW C1/C1E/C/CE further scale-out beyond the existing 90→120-question pool; Führerschein L/T (agricultural/tractor, flagged low-value/least reuse); the pending Anlage 7 FeV D-family point/question-count cross-check for `fuehrerschein_bus`'s placeholder pass-rule; and the two needs-PO-decision items, unchanged from rounds 1/2's flag and still not greenlit: § 34a GewO Bewachungsgewerbe (security-guard licensing) and § 34c GewO Immobilienmakler (real-estate broker licensing) — neither has a unified official public question catalog, only private vendor compilations that would risk reproducing a commercial vendor's bank if used as a base, so both remain blocked on an explicit PO sourcing-strategy decision before any content work starts.

- **2026-08 content expansion round 2 — 3 new personal-license modules (angelschein_bayern, angelschein_nrw, amateurfunk_a), registered and deployed (2026-08-12)** — Same overnight integration/deployment pattern as round 1 (sportboot_binnen/sportboot_see/waffensachkunde/amateurfunk_e, entry directly below): built by a prior session (content authoring + independent fact-check + fixes done separately), this round did registration, validation, and delivery only, per instruction. **Content basis, per module**: `angelschein_bayern` (48 questions, 5 topics: `schonzeiten_bayern`12/`fanggeraete_bayern`9/`fischereischein_bayern`10/`fischkunde_bayern`9/`gewaesserschutz_bayern`8) sourced from BayFiG/AVBayFiG statute text on gesetze-bayern.de, a standalone `exam_type` rather than a region option on the existing generic `angelschein` module since Bavaria's regime is a fully self-contained statute set; `angelschein_nrw` (56 questions, 5 topics: `schonzeiten`12/`geraete`10/`fischereischein`14/`fischkunde`12/`gewaesserschutz`8) sourced from LFischG NRW statute text on recht.nrw.de, also standalone (distinct from the existing generic `angelschein` module's lighter NRW region-scope addon); `amateurfunk_a` (45 questions, 6 topics: `antennentechnik`8/`leitungstheorie`8/`ausbreitung`8/`leistung`8/`modulation_digital`7/`emv_hochleistung`6) — the Klasse A follow-up flagged as "planned separately" in round 1's `amateurfunk_e` entry, sourced from freirecht.de's AFuV/BEMFV text plus the BNetzA Klasse A Fragenkatalog PDF. **Fact-check findings and fixes** (done in the prior session, independently re-verified again against primary sources before this round's registration, not just trusted): `angelschein_bayern` — 3 `legal_basis` citation errors found and fixed (`angelschein-bayern-fanggeraete-06`: §16→§15 Abs. 1 Nr. 6 AVBayFiG; `angelschein-bayern-gewaesserschutz-06`: §11 Abs. 9→Abs. 8 AVBayFiG, this one embedded in question/explanation text across all 12 locales plus the underlying `data/i18n_angelschein_bayern/*.json` translation source files, not just the pilot JSON, so the fix survives any regeneration; a third `gewaesserschutz-07` Art.-citation fix). `angelschein_nrw` — 4 findings and fixes: `angelschein-nrw-fischkunde-07` was a genuine fish-ID error (fused pelvic fins are a Gobiidae/goby trait, not the Groppe's, per de.wikipedia.org) — rewrote the question around the invasive Marmorierte Grundel (round goby) instead, full text/option/explanation rewrite in all 12 locales; `angelschein-nrw-geraete-09` corrected per the official NRW Fischerpruefungs-Fragenkatalog PDF (hand-rod count is "gesetzlich nicht geregelt," not governed by the §42 citation the draft had used); `angelschein-nrw-schonzeiten-03` explanation text fixed in all 12 locales to add NRW's Aal-Schonzeit geographic qualifier (Rheinhauptstrom ohne Nebengewässer only, not statewide), sourced from fischereiverein-rietberg.net's LFischVO text. `amateurfunk_a` — 5 findings and fixes, all AFuV/BEMFV `legal_basis` citation errors: the `AFUV_A` constant (§4 Abs. 2→§4 Abs. 1 Nr. 1 AFuV) propagated to ~40 affected questions across `antennentechnik`/`leitungstheorie`/`ausbreitung`/`leistung`/`modulation_digital`; 3 `emv_hochleistung` questions' §13 AFuV citation (which actually covers remote/automatic stations, not interference) corrected to §16 Abs. 4/§17 AFuV, with 1 of those 3 also needing a full DE+EN question/explanation rewrite since it had asserted a nonexistent §13 obligation; 1 `leistung` question's §3 BEMFV (numeric EMF limits only) corrected to §8 BEMFV i.V.m. §5/§9 BEMFV (Standortbescheinigung/Anzeigepflicht) for the certification-duty claim it was actually making. All 3 modules carry the project's standard `legal_review_status: "NOT legally reviewed..."` caveat in pilot-json `meta` (unchanged this round); `angelschein_nrw`'s additionally flags that its Schonzeit dates/Mindestmasse and LFischG paragraph numbers should be independently re-verified against the current official Landesfischereiverordnung NRW before commercial use, since a 2025/2026 LFischG/LFischVO Neufassung was found mid-sourcing but couldn't be fully cross-checked paragraph-by-paragraph in the time available. **AMATEURFUNK_A DESIGN FLAG — OPEN PRODUCT DECISION, flagged explicitly, not resolved this round**: `amateurfunk_a` is deliberately TECHNICAL-ONLY (zero Betrieb/Vorschriften questions) because Klasse A shares those two exam sections ENTIRELY with Klasse E per §4 Abs. 4 AFuV (upgrading E→A only adds a technical exam) — duplicating amateurfunk_e's existing ~24 Betrieb/Vorschriften questions into a second module would create a drifting duplicate copy of the same content. Before this module can offer a realistic full 3-section Klasse A exam simulation, product needs to decide: (a) have the exam composer draw Betrieb/Vorschriften questions live from the `amateurfunk_e` pool at draw time for a full Klasse A simulation (preferred — avoids drift, but needs `EXAM_TOPIC_DRAW`/`drawExamQuestions()` extended to pull cross-module), or (b) physically duplicate those questions into `amateurfunk_a`'s own pool for standalone operation (simpler, but creates exactly the drift risk (a) avoids). Until resolved, `amateurfunk_a`'s exam only covers its own 45-question technical delta — see `data/amateurfunk_a_pilot.json` `meta.design_flag_betrieb_vorschriften` for the full writeup this entry summarizes. **Registration work done this round, same pattern as round 1**: `data/modules_manifest.json` (and its deployed copy `app/data/modules.json`, kept in sync by hand since `data/build_modules.py` is destructive — see round 1's note below) — added all 3 as `scopeKind: "class"` with a single `"ALL"` option, full 12-locale label. `app/app.js` exam-tuning maps, scaled to each pool's real size: `EXAM_QUESTION_COUNT_BY_TYPE` — `angelschein_bayern: 30` (48-question pool), `angelschein_nrw: 30` (56-question pool, kept at the same 30-question scale as `angelschein_bayern` for consistent exam-taking time rather than scaling up with the larger pool), `amateurfunk_a: 22` (45-question pool, technical-only draw); `EXAM_TIME_LIMIT_MS_BY_TYPE` — 30/30/20 minutes respectively (`amateurfunk_a` shorter than `amateurfunk_e`'s 30 since it's a smaller, technical-only draw); `MAX_ERROR_POINTS_BY_TYPE` — 8/8/6 (the established ~1/3-of-draw ratio); `EXAM_TOPIC_DRAW` — proportional to each module's real `topic_code` distribution, counted directly from each `data/<module>_pilot.json` rather than assumed: `angelschein_bayern` 8/6/6/6/4=30, `angelschein_nrw` 6/5/8/6/5=30, `amateurfunk_a` 4/4/4/4/3/3=22. `app/index.html` — full 12-locale `MODULE_LABELS` entries (`de`: "Angelschein Bayern", "Angelschein Nordrhein-Westfalen", "Amateurfunk Klasse A", each independently translated across the other 11 locales), subtitles reusing `s.theoryExam` (same pattern as the other personal-license modules), `angelschein_bayern`/`angelschein_nrw` added to the existing `groupDriving` group alongside `fuehrerschein`/`motorrad`/`lkw`/`angelschein` (they're Angelschein variants, not a new category), and `amateurfunk_a` added to round 1's `groupSpecialty` ("Specialty licences") group alongside `sportboot_binnen`/`sportboot_see`/`waffensachkunde`/`amateurfunk_e`. `data/build_modules.py` was again deliberately NOT run, per round 1's own documented near-miss (its `shutil.rmtree(APP_DATA)` doesn't know about any of this round's or round 1's new module directories and would delete them) — `app/data/modules.json` was instead hand-synced from `data/modules_manifest.json` by direct copy-and-validate. **Validation**: `node -c app/app.js` passes; `node -c` on the extracted inline `<script>` from `app/index.html` passes (the JSON-LD `<script>` block correctly fails as non-JS, as expected); all touched/new JSON files (`data/modules_manifest.json`, `app/data/modules.json`, all 3 modules' pilot JSON + `core.json` + all 12 `locales/*.json` files) parse cleanly via `python3 -c "import json; json.load(...)"`; every module's `core.json` question-id set matches all 12 of its `locales/*.json` files exactly (48/56/45 ids respectively, verified programmatically, zero missing/extra in any locale). **Deployed**: staging (`480e3ec6-76f6-414e-a7bc-eb3e661f5816`) first, curl-verified, then production (`b244f9b2-e45a-48c0-9f59-0405f587c213`), curl-verified again. **Explicitly deferred, not built this round** (per `claude/content-expansion-scoping-2026-08-12.md`, sections 2/4): Angelschein coastal-core Länder (Schleswig-Holstein, Mecklenburg-Vorpommern, Niedersachsen, Hamburg, Bremen — shared saltwater/Wattenmeer core with thin per-state overlays); Angelschein inland-core Länder (Hessen, Rheinland-Pfalz, Saarland, Sachsen, Sachsen-Anhalt, Thüringen, Brandenburg, Berlin — shared freshwater core with thin overlays, lowest Angelschein priority given smaller standalone audiences); Sportbootführerschein SKS/SSS (no standardized public elwis-hosted catalog, would need original synthesis from DMYV/DSV syllabus — higher content-creation cost and lower sourcing confidence than the already-built Binnen/See); Führerschein Bus D-family (D1/D1E/D/DE — sequenced after LKW's tachograph content for efficiency, not yet started); Führerschein Motorrad A-family completion (pilot exists but full A1→A2→A Zusatzstoff build not done); and the two needs-PO-decision items, unchanged from round 1's flag and still not greenlit: §34a GewO Bewachungsgewerbe (security-guard licensing) and §34c GewO Immobilienmakler (real-estate broker licensing) — neither has a unified official public question catalog, only private vendor compilations that would risk reproducing a commercial vendor's bank if used as a base, so both remain blocked on an explicit PO sourcing-strategy decision before any content work starts.

- **2026-08 content expansion — 4 new personal-license modules (sportboot_binnen, sportboot_see, waffensachkunde, amateurfunk_e) + lkw expansion 90→120, registered and deployed (2026-08-12)** — Overnight integration/deployment pass on top of a prior session's content-authoring work (fact-checked and QA'd separately; this round did registration, validation, and delivery only, per instruction). **Content basis, per module**: `sportboot_binnen` (50 questions, 5 topics × 10: `vorfahrt`/`bezeichnung`/`schallzeichen`/`betonnung`/`seemannschaft`) sourced from the official WSV/BMDV Fragenkatalog Binnen (Stand 01.08.2023, fetched directly from elwis.de) plus BinSchStrO statute text; `sportboot_see` (30 questions, 4 sea-specific topics × 7-8: `betonnung`/`seefunk_vorfahrt`/`gezeiten`/`seewetter`) sourced from the official WSV Fragenkatalog See (same August 2023 edition) plus KVR/COLREG and SeeSchStrO; `waffensachkunde` (50 questions, 4 topics of 12-13 each: `grundlagen`/`munition_ballistik`/`aufbewahrung_transport`/`handhabung_sicherheit`) sourced from the official Bundesverwaltungsamt (BVA) Sachkunde-Fragenkatalog gem. §7 WaffG (Stand 16.12.2024) plus WaffG/AWaffV/StGB §32 statute text; `amateurfunk_e` (40 questions, 3 topics: `technik` 16/`betrieb` 12/`vorschriften` 12) sourced from the BNetzA's 2006-edition Fragenkataloge (Technische Kenntnisse Klasse E; Betriebliche Kenntnisse und Kenntnisse von Vorschriften Klassen A und E) as structural/topical reference plus the current AFuV statute text — Klasse E only, a Klasse A follow-up is a separate future module. `lkw` was expanded from 90 to 120 questions (StVZO/EU561/FPersG grounding, same pattern as the existing pool) — no new registration needed since `lkw` was already a registered module; it just falls through to the driving-exam defaults in the exam-tuning maps, same as `motorrad`/`angelschein`. All 5 authoring-round QA passes and fixes (done in the prior session, verified again this round via full JSON/ID-set checks): `sportboot_binnen` 22/50 initial errors found incl. one safety-critical backbord/steuerbord (port/starboard) right-of-way rule inversion, requiring a second targeted fix round before re-verification passed; `sportboot_see` 1/30; `waffensachkunde` 9/50; `amateurfunk_e` 2/40; `lkw` 3/30-new-questions — all fixed and re-verified prior to this round. Every new module carries the project's standard `legal_review_status: "NOT legally reviewed..."` caveat in its pilot-json `meta` (set at authoring time, unchanged this round) — `waffensachkunde`'s additionally states explicitly that it is not legal advice and does not replace an accredited Sachkundepruefung course. **Registration work done this round**: `data/modules_manifest.json` — added all 4 new modules as `scopeKind: "class"` with a single `"ALL"` option (12-locale label), same placeholder-scope pattern as `lkw`/`angelschein`/the compliance modules, since none of these have a real class/region dimension to model. `app/app.js`'s exam-tuning maps, scaled to each module's real-world exam and pool size rather than the 6-question B2B-compliance pattern: `EXAM_QUESTION_COUNT_BY_TYPE` — `sportboot_binnen: 30` (real SBF Binnen exam is ~30 combined questions), `sportboot_see: 20` (proportional draw across the pool's 4 sea-specific topics), `waffensachkunde: 30` (matches the real Sachkundepruefung written exam's ~30 questions), `amateurfunk_e: 20` (smaller than the real exam's 3×25 sectioned format since this pilot pool is an initial coverage sample, not a full replica — see the pilot's own `pass_rule_note`); `EXAM_TIME_LIMIT_MS_BY_TYPE` — 30/25/30/30 minutes respectively; `MAX_ERROR_POINTS_BY_TYPE` — 8/6/8/6 (roughly the driving-exam default's ~1/3-of-total-points ratio); `EXAM_TOPIC_DRAW` — proportional to each module's real topic_code distribution in its pilot pool (verified by counting `topic_code` occurrences in each `data/<module>_pilot.json` directly, not assumed from the module description): `sportboot_binnen` 6/6/6/6/6, `sportboot_see` 5/5/5/5, `waffensachkunde` 8/8/7/7, `amateurfunk_e` 8/6/6. `app/index.html` — added 12-locale `MODULE_LABELS` entries for all 4 modules (`de`: "Sportbootführerschein Binnen", "Sportbootführerschein See", "Waffensachkunde (§7 WaffG)", "Amateurfunkzeugnis Klasse E", each independently translated across the other 11 locales, not machine-copied from English/German), subtitles reusing the existing `s.theoryExam` string (same pattern as `motorrad`/`lkw`, since these are personal-license theory exams, not workplace-compliance training), and a new module-grid group `groupSpecialty` ("Specialty licences" / "Spezial-Lizenzen", 12-locale heading added to `ENTERPRISE_STRINGS` alongside the existing `groupDriving`/`groupCompliance`/`groupEnterprise` headings) containing all 4 new modules, since none of the 3 existing groups (driving licences, workplace compliance, B2B enterprise) fit this content. **Runtime data pipeline note (real near-miss caught mid-round)**: `data/build_modules.py` does a full `shutil.rmtree(APP_DATA)` before rebuilding — running it (to sync the new manifest entries into `app/data/modules.json`, its intended purpose) silently deleted the just-generated `app/data/{sportboot_binnen,sportboot_see,waffensachkunde,amateurfunk_e}` directories along with `app/data/fuehrerschein`'s primers/sign_reference files, because the script's own hardcoded module list doesn't know about the 4 new modules yet (DN-64's own entry above already flagged this exact rmtree blast-radius risk as the reason it deliberately skipped running the script for `lksg`). Caught immediately via `git status`; the 4 new modules' runtime files were regenerated from their still-intact pilot JSONs via their own scoped generator scripts (`data/gen_sportboot_binnen.py`, `data/gen_sportboot_see.py`, `data/gen_waffensachkunde_i18n.py`, `data/gen_amateurfunk_e_i18n.py` — the latter two read from the already-12-locale-merged pilot files, not their earlier DE/EN-only `gen_waffensachkunde.py`/pre-i18n counterparts, so re-running them was safe and didn't regress locale coverage), and the fuehrerschein primer files were restored via `git checkout --`. `data/build_modules.py` itself was not modified this round; a future round should teach it about the 4 new modules (and `lksg`, per DN-64's own note) so it can be run safely again, or its rmtree replaced with a more surgical per-module overwrite. **Validation**: `node -c app/app.js` passes; all 194 JSON files under `app/data/` plus `data/modules_manifest.json` parse cleanly; every one of the 4 new modules' `core.json` question-id set matches all 12 of its `locales/*.json` files exactly (50/30/50/40 ids respectively, verified programmatically, zero missing/extra in any locale); `class_scope` is `["ALL"]` on every question in all 4 modules, matching `questionMatchesScope()`'s expectations. **Live-served Playwright verification** (a local Python static server on `app/`, Chromium from `/opt/pw-browsers`, since this is a build-step-free static PWA with no dev server config): all 4 new modules load via their deep-link URL (`app.html?exam=<type>&scope=ALL`) with real question text rendering and zero console/page errors; `drawExamQuestions()` invoked directly in-page for `sportboot_binnen` returned exactly 30 questions, confirming the new `EXAM_QUESTION_COUNT_BY_TYPE`/`EXAM_TOPIC_DRAW` wiring is live and correct; the landing page (`index.html`) renders a 4th "Specialty licences" module group with all 4 new module cards and correct English labels, zero console/page errors. **Deployed**: staging first (`480e3ec6-76f6-414e-a7bc-eb3e661f5816`), curl-verified the new modules' JSON and the app shell are reachable, then production (`b244f9b2-e45a-48c0-9f59-0405f587c213`), curl-verified again — see the deploy-status note in this same round's session report for the exact verified URLs. **Explicitly deferred, not built this round** (tracked in `claude/content-expansion-scoping-2026-08-12.md`): Angelschein per-Bundesland modules (Bayern/NRW priority), Amateurfunk Klasse A (a natural follow-up to this round's Klasse E module, reusing its `betrieb`/`vorschriften` pool per AFuV §4(4)'s own upgrade-path logic), Führerschein Motorrad A-family completion, Bus D-family, SKS/SSS (Sportküstenschifferschein/Sportseeschifferschein); and two items explicitly flagged as needing a product-owner decision before any content work starts, since neither has a full official public question catalog the way every other module in this app is grounded in: §34a Bewachungsgewerbe (security-guard licensing) and §34c Immobilienmakler (real-estate broker licensing).

- **Optional multi-email identity binding for wallet-import badges (2026-08-11)** — Follow-up to the "can Credly also import the badges?" conversation: Credly's own documented behavior checks that the email embedded in an uploaded badge matches an email already on the uploader's Credly account, but Zettacard's credentials are deliberately zero-PII by design (no earner email embedded at all today) - so a straight file upload could plausibly fail Credly's own matching check even after the same day's `jku`/`vc.id`/context conformance fixes. Rather than adding a single mandatory email (which would compromise the zero-PII default for every user), confirmed OB3's actual JSON-LD context defines `credentialSubject.identifier` with `@container: @set` (verified via WebFetch against `purl.imsglobal.org/spec/ob/v3p0/context-3.0.3.json` directly, not assumed from an implementation guide's single-example JSON, which is genuinely ambiguous on cardinality on its own) - meaning a credential can legitimately carry *multiple* hashed identities at once, so a learner can bind whichever email(s) they actually use on Credly/other wallets (personal, work, or both) without the app ever requiring one. Built: (1) `netlify/functions/sign-credential.js` now accepts an optional `emails` array (0-3 addresses) in the signing payload, validates/normalizes each (lowercase+trim, standard email regex, deduped, capped at `MAX_IDENTITY_EMAILS = 3`, rejecting the whole request with a clear 400 if the cap or format is violated), and `buildCredentialClaims()` embeds them as `credentialSubject.identifier`, an array of OB3 `IdentityObject`s - each one salted (`crypto.randomBytes(16)`) and SHA-256-hashed (`sha256$` + hex digest of `email + salt`, the standard OB2/OB3 hashing convention) rather than stored in plaintext, so the JWT says "this earner controls an email that hashes to X," never the address itself. When no `emails` are submitted (the default for every automatic background sign via `trySignCompletion()` in `app/app.js`, which never sends them), `identifier` is omitted entirely - not an empty array - so the zero-PII-by-default posture is completely unchanged unless a learner explicitly opts in at export time. (2) `app/app.js`: a new `signPersonalizedCredential(record, emails)` function calls the same signing endpoint with the extra `emails` field and returns a **separate** JWT, deliberately not persisted to `record`/localStorage or merged with the record's own default signature - this is a one-off export action, not a change to what `trySignCompletion()` already produced automatically. `renderWalletRow()` (the "Add to a badge wallet" card, DN-63) gained a new form: an email input (reusing the exact same native `<input type="email" multiple>` + comma-split + `CERT_EMAIL_RE` pattern `renderEmailCertRow()`'s multi-recipient send already established, for a consistent UX and zero new validation code) plus a "Download personalized credential" button that produces a second `.jwt` file download, separate from the existing anonymous JWT/certificate downloads. New `CERT_STRINGS` keys (`walletIdentityTitle`/`Intro`/`Placeholder`/`DownloadBtn`/`Signing`/`DoneMsg`/`ErrorMsg`/`TooManyMsg(n)`) added across all 12 locales via the established anchor-insert script pattern, each independently translated (not machine-copied from English), explicitly stating in the intro copy that the emails are hashed, never stored, and used only for that one download. New CSS (`.cert-wallet-identity*`) mirrors `.cert-email-row`/`.cert-email-form`'s existing "dashed divider, input + button, wraps on narrow screens" layout exactly. **Verified against a real signed credential from `zettacard-staging`, not a mock**: deployed to staging, signed a completion record with two emails (`Personal@Gmail.com`, `work@example.com`, deliberately mixed-case to confirm normalization) via the live endpoint, decoded the returned JWT and confirmed `credentialSubject.identifier` is a 2-entry array of correctly-shaped, correctly-salted `IdentityObject`s and that neither email appears anywhere in the JWT in plaintext; independently re-verified with `jose`'s `createRemoteJWKSet`/`jwtVerify` against staging's live JWKS that the signature still validates correctly with the identity claims present; confirmed the 4-email-cap-exceeded and invalid-email-format cases both return a clean `400` with a specific error message rather than a signed credential; confirmed the no-`emails`-field default path (what every real exam pass already uses automatically) produces a JWT with no `identifier` key at all, byte-for-byte the same shape as before this feature existed. Client-side UI verified via Playwright against a local server: the identity form renders inside the wallet row, an invalid email is caught by native HTML5 validation before any request fires, a 4-email submission shows the "at most 3" error inline, and a valid submission shows a graceful inline error (rather than a crash) when the signing call doesn't succeed - zero console/page errors throughout. **Explicitly a personalization step, not a change to any other credential**: the record's own default `signedJwt` (used for `/verify/<slug>` links, the standalone anonymous JWT download, and the JSON reference document) is completely untouched by this feature - the personalized, email-bound JWT is a separate, on-demand-only artifact a learner can generate as many times as they like with different emails, never stored server-side or client-side beyond the one download.
- **Mobile header horizontal-overflow fix (2026-08-11)** — Real bug from a live user report ("the header scrolls far outside the screen" on phone). Root cause: `.title-row`'s right-hand control cluster in `app/app.html` (`module-info`/`primers`/`sign-reference`/`offline-prep`/`certificates`/`review`/`profile-switch`/`module-switch`/`exam-start`/`theme-toggle` buttons plus the 12-language `<select>` - up to 10 controls) was a plain inline `style="display:flex; gap:8px; align-items:center;"` with no wrapping - on a phone-width viewport that's ~500-600px of un-wrappable content forced into a ~320-390px viewport, which pushed `<header>` (and with it the whole page, since nothing constrained `body`/`html` width) past 100vw, so the entire viewport gained sideways scroll - not just the header failing to stay put, the page itself became horizontally scrollable, matching the "scrolls far outside the screen" description exactly. Fix, in `app/styles.css`: (1) the real fix - gave that inline-styled div a real class (`.header-controls`, in `app/app.html`) with `flex-wrap: wrap; justify-content: flex-end; row-gap: 8px; flex: 1 1 auto; min-width: 0`, so the control cluster wraps onto as many right-aligned lines as it needs instead of forcing the page wider; also added `flex-wrap: wrap; row-gap: 8px` to `.title-row` itself as a second layer of the same fix. (2) a belt-and-braces backstop - `overflow-x: hidden` on `html` and `body` (plus `max-width: 100vw` on `body`) so any *other* future element doing the same thing can't drag the whole page sideways again, even if its own wrapping fix is missed. Verified via Playwright at four phone widths (320px iPhone SE, 375px, 390px, plus a 768px tablet and 1280px desktop sanity check) across three different modules (Fuehrerschein - the module with the most header buttons shown, since `primers-btn`/`sign-reference-btn` are Fuehrerschein-family-only; a workplace-compliance module `datenschutz` which shows `module-info-btn` instead; and the newest module `lksg`) - `document.documentElement.scrollWidth <= clientWidth` (i.e. zero horizontal overflow) held at every width tested, zero console/page errors, and the desktop/tablet screenshots confirm the header still renders as a single tidy row there (the wrap only kicks in when it's actually needed, not unconditionally).
- **DN-51 — OB3 conformance fixes from the 2026-08-11 re-verification: `jku`, `vc.id`, versioned `@context` (2026-08-11)** — Closed the concrete 3-item follow-up list the same day's "DN-51 (re-verification)" entry left open (see that entry below for the original findings). Implemented all three fixes in both `netlify/functions/sign-credential.js` (`buildCredentialClaims()` + the `SignJWT` call's protected header - the real signed artifact) and `app/app.js` (`credentialJsonDoc()` - the client-side reference JSON download), mirroring each other's shape as the project's convention requires: (1) added a `jku` protected-header claim to the signed JWT, pointing at the same `/.well-known/jwks.json` URL the function already returned as `jwksUrl` in its API response but never carried IN the JWT itself; (2) added a top-level `vc.id` (`${ISSUER_URL}/credentials/${record.id}`) in addition to (not replacing) the JWT's own `jti`; (3) swapped the unversioned `.../context.json` for the versioned `.../context-3.0.3.json` in both places. **Verified against a real signed credential from `zettacard-staging`, not a mock**: deployed the fix to staging first, signed a fresh completion record via the live `sign-credential` function, decoded the returned JWT and confirmed all three claims are actually present in production output; independently re-verified with `jose`'s `createRemoteJWKSet`/`jwtVerify` against staging's live JWKS that the signature still validates correctly (unchanged - these are additive claims, not a key/algorithm change). **Re-ran 1EdTech's official validator (`vc.1ed.tech`, `OB30Inspector`) against the fixed credential** using the real API (uploading with a `.jwt` filename so the tool correctly detects `type: JWT` - the original re-verification pass's `.txt` upload had silently fallen back to unsupported plain-text handling on a retry attempt, since corrected): result improved from 1 error + 1 warning + 1 exception down to **zero** of all three - the JSON-Schema `id`-missing error, the unversioned-context warning, and the JSON-LD exception are gone. **One FATAL remains, and won't be fixed by claims alone**: `"Unable to retrieve jwk value from url specified in kid.. Reason: Error solvig did: <kid>"` - reading the message closely confirms both 1EdTech's validator and CertLister's (per the original re-verification pass) resolve `kid` *exclusively* as a DID (`did:web`/`did:key`), not via the standard `jku` header claim this round added - a limitation of those two specific validator implementations, not evidence the `jku` fix was wrong (a generic JOSE-aware verifier honoring `jku`, which is functionally what this project's own tooling already does, still verifies the credential correctly, confirmed above). Closing this fully for these two validators specifically would need a `did:web` issuer identity - Option 4 in `docs/open-badges-signing-scoping.md`'s original architecture menu, the most involved of the four and explicitly not recommended as a first step there - so it's logged as a genuine, deliberate follow-up (see the open Backlog DN-51 entry) rather than attempted here. Full writeup in `docs/open-badges-signing-setup.md`'s new "Fixes applied 2026-08-11" section.
- **DN-52 Phase 2 — kickstart-learning-journey practice-quiz tier (2026-08-11)** — Second concrete build step on `docs/kickstart-learning-journey-scoping.md`, following Phase 1's Fuehrerschein topic primers (2026-08-09, see that entry below). Built **only** section 5 of the scoping doc (the practice-quiz mechanic) - explicitly not sections 2/3/4 (topic/scenario primers for the other modules and topics, which need fresh sourced legal/statute content per topic and are a separate, much larger content-authoring effort) and not section 6 (the three-path kickstart wizard), per this round's scoping instruction. This mechanic was chosen precisely because it needed zero new content authoring or legal sourcing - it's pure reuse of already-verified question data and already-built exam/SRS machinery. A new short (`PRACTICE_QUIZ_QUESTION_COUNT = 12`, within the doc's 10-15 range), low-stakes quiz mode sits between flashcards and Exam Simulation: learner picks a scope first (mixed/all-topics, or a single topic - reusing the same topic set `TOPIC_LABELS`/the topic-filter chips already use), then answers questions one at a time with **immediate** right/wrong feedback per question (unlike Exam Simulation's reveal-at-the-end), ending in a plain score summary. **Hard constraint, verified correct**: this mode has no timer, no pass/fail rule, and never calls `recordCompletion()`/`trySignCompletion()` or touches the completions/certificate localStorage list - only a genuine Exam Simulation pass does that (finishExam() in app.js, unchanged) - per the PO's decision already recorded in section 5 of the scoping doc, re-read before starting to make sure this constraint was implemented exactly as decided, not reinvented. It DOES feed the Leitner spaced-repetition schedule via the exact same `feedExamResultsIntoSrs()` hook Exam Simulation/Training already use (that function only reads a generic `{questions, answers}` shape, so `state.practiceQuiz` works with it completely unmodified - no SRS code was duplicated or touched). Question selection reuses existing machinery rather than a new algorithm: a single-topic scope is `shuffle()` (the same helper `drawExamQuestions()` itself uses) over that topic's own questions; "mixed" scope leans on `drawExamQuestions()`'s own already-realistic topic-weighted full draw and takes a shuffled subset of it (`drawPracticeQuestions()` in app.js, right above `openExamPicker()`) - no second topic-weighting table was invented. The per-question interaction (pick an option, reveal immediately, see the correct answer highlighted plus the existing explanation/legal-basis text) deliberately reuses the flashcard "try it yourself" self-answer flow's exact visual language (`.option.correct`/`.your-wrong-pick`/`.picked`, `.correct-mark`/`.wrong-pick-mark`, `.explanation`/`.legal-cite`) instead of exam mode's reveal-only-at-the-end pattern or any new CSS - a returning learner already recognizes the look from flashcards. **Entry point**: a third button ("Practice quiz") added to the existing exam-mode picker card (`#exam-picker`, next to Training/Simulation) rather than a new header button or the full three-path wizard (section 6, still out of scope) - satisfies the "working, discoverable entry point near where Exam Simulation/Training/Flashcards are already launched" ask without the larger wizard redesign. Clicking it opens a small scope-picker modal (`#practice-picker`, mixed + one button per topic, scrollable list since Fuehrerschein has 11 topics) before starting the quiz view (`#practice-view`) and results view (`#practice-results` - deliberately has no certificate section at all, unlike `#exam-results`, so there's structurally nothing there that could grow into one by accident). **UI copy**: a new `PRACTICE_QUIZ_STRINGS` object (deliberately separate from `EXAM_STRINGS`, not a few overridden keys on it, so "exam"/"Prüfung" wording can never leak in) carries explicit no-stakes framing at every stage - the entry button itself ("Short, casual, no time pressure — doesn't count toward a certificate."), the scope picker ("No time limit, no pass/fail rule, no certificate — just 12 questions to try"), and the results screen ("this quiz doesn't count as an exam attempt and doesn't produce a certificate... For a certificate, use Exam Simulation"). **Locale gap, explicitly disclosed** (same disclosure pattern this project already uses for DN-43's `modules_manifest.json` intro.steps DE/EN-only gap, called out in the scoping doc's own section 7): `PRACTICE_QUIZ_STRINGS` is DE+EN only for this Phase 2 build, not all 12 UI languages like most of this app's other locale objects - `practiceQuizStrings(lang)` falls back to English for the other 10 languages, same `X[lang] || X.en` fallback chain used throughout app.js (e.g. `signRefStrings()`/`primerStrings()`). Worth closing in a follow-up round once there's real usage signal, same as the DN-43 gap. **Verified**: `node --check app/app.js`; a Playwright smoke test against a local Python static server (`app/` served on :8934, Chromium launched directly from `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`) driving the real UI end to end on the Fuehrerschein module - opened the exam picker, confirmed the practice-quiz entry button and its no-stakes copy render, chose "mixed" scope, answered all 12 questions (4/12 correct in this run), confirmed the results screen shows the plain score with no certificate/pass-fail UI, confirmed the completions/certificates localStorage key was completely unchanged (empty before and after - no certificate was created), confirmed the SRS localStorage key went from unset to a real 12-entry Leitner schedule (one entry per answered question, box 0 or 1 depending on whether that question was answered correctly), and confirmed zero console/page errors throughout. A second Playwright pass confirmed the topic-scoped path too: all 11 Fuehrerschein topic buttons render in the scope picker (plus "Mixed"), and picking "Right of way & intersections" correctly drew only vorfahrt-topic questions into the quiz. **Explicitly NOT built this round, still open per the scoping doc**: section 2 (the sign shape/category primer's motorcycle/LKW equivalents), section 3 (topic primers for every module beyond Fuehrerschein - Motorrad/LKW/Angelschein/all 5 compliance modules), section 4 (situational/scenario primers - Vorfahrt decision-flow first, per the doc's recommended pilot), and section 6 (the kickstart path wizard tying primers/flashcards/practice-quiz/exam together into an explicit three-way choice, extending DN-43's module-intro mechanism) - all of these need either fresh sourced content per topic (primers) or a larger UX redesign (the wizard), neither of which fit this pass's "pure mechanic reuse, no new content-authoring burden" scope.

- **DN-51 (re-verification) — ran the two external OB3 validators for real, found 2 genuine remaining conformance gaps + confirmed the sandbox Playwright/Chromium block is still present (2026-08-11)** — Closed the last open item from the 2026-08-08 "remaining step" entry below, which had fixed `achievement.id`/proof-shape but explicitly left "re-run the two external OB3 validators (CertLister, 1EdTech's) and re-run `scripts/test_full_exam_badge.mjs`" undone, blocked at the time by a sandbox-wide Playwright/Chromium `net::ERR_CONNECTION_RESET` networking outage. Re-checked that outage first rather than assuming it had cleared - it hadn't (`scripts/test_full_exam_badge.mjs` against `zettacard-staging` still fails on the very first `page.goto`, immediately, before any exam logic runs) - but found that both validators accept `curl`-driven HTTP requests directly (a plain multipart POST for 1EdTech's `vc.1ed.tech/upload`, real JS logic reverse-engineered from CertLister's own shipped bundle for `certlister.com/ob3-validator/` since its checks run entirely client-side and its one server call is Turnstile-captcha-gated), so the validator re-check itself got done this round using a real JWT signed live by `zettacard-staging`'s deployed `sign-credential` function against its real live JWKS - not a hand-built or hypothetical credential. **Result: both validators found the same root defect, independently** - the signed JWT's header carries only a bare-UUID `kid` with no `jku` (JWK Set URL) claim and no `did:web`/`did:key` issuer identity, so neither validator has any way to discover `/.well-known/jwks.json` from the credential alone (this app's own tooling only works because it hardcodes that URL convention out of band). 1EdTech's official validator (`vc.1ed.tech`, `OB30Inspector`, the same `digital-credentials-public-validator` deployment named in the scoping doc) additionally flagged a genuine JSON Schema violation - `$: required property 'id' not found` - because `buildCredentialClaims()` in `netlify/functions/sign-credential.js` only ever added `credentialSubject.achievement.id` (the 2026-08-08 fix below), relying on the JWT's `jti` claim to cover the top-level `vc.id` per VC-JWT's standard claim mapping - but 1EdTech's validator schema-checks the decoded `vc` object directly and doesn't do that `jti`→`id` promotion first, so the schema check fails regardless of whether the JWT-VC mapping "should" count in theory. Also flagged (lower severity): a WARNING for using the unversioned `.../context.json` instead of the versioned `.../context-3.0.3.json`, and a repeatable JSON-LD-processing ERROR+EXCEPTION pair not yet root-caused (possibly downstream of the missing `id`, possibly independent - left honestly unresolved rather than guessed at). CertLister's client-side checks (structure/context/expiry) all PASS on the same credential - notably looser than 1EdTech's schema, since CertLister doesn't require a top-level `id` at all - with only the signature/issuer check landing on "unverified" (amber, not a hard failure) for the same DID-resolution reason as 1EdTech's fatal. As a substitute for the still-blocked full-browser exam flow, independently re-verified the real sign→publish→verify round trip via `curl` + `jose`'s `createRemoteJWKSet` against staging's live JWKS (passed, matching the Playwright script's own independent-verification step) and `curl`-posted the same signed JWT to `save-verified-credential-v2.mjs` on staging (succeeded, produced a working `/verify/<slug>` link, confirming its independent re-verification-before-persist logic still works) - no bug found in that file, so it was **not modified**. Full findings, the concrete 3-item follow-up fix list for `sign-credential.js`/`app.js` (add a `jku` header claim; add `vc.id`; use the versioned context URL), and exact validator API/request details written up in `docs/open-badges-signing-setup.md`'s new "Re-verification 2026-08-11" section. **Not done**: the actual fixes themselves (deliberately left for a follow-up round, since this pass was scoped to verification, not another round of signing-function changes) and the in-browser exam-UI portion of `scripts/test_full_exam_badge.mjs` (still needs working Playwright/Chromium networking, unrelated to the signing feature itself - confirmed via `curl` to the identical URL succeeding fine).

- **DN-65 — Independent certificate-language selector (2026-08-11)** — Closes the last open item from DN-56's clarifying question, where the PO answered "beides" (both) to "an independent certificate-language selector AND full 12-locale coverage" - 12-locale coverage was verified done back then; the selector itself stayed open until now. Added a per-record language dropdown (`certLangSelectHtml()`, 12 endonym-labeled options copied from `app.html`'s own `#lang-select`, same flag-prefix convention) to both the "My certificates" panel and the exam-results certificate card, independent of `state.lang` (the app's own UI language) - a learner using the app in Turkish can generate an English-language certificate for an employer who doesn't read Turkish without switching their whole session. Selection is session-only (a `Map` keyed by `record.id`, not persisted to localStorage) since this is a per-download choice, not a standing preference the way `state.lang` itself is. `certificateHtmlDoc(record, lang)` and `printCertificateAsPdf(record, lang)` both gained an optional second parameter (defaulting to `state.lang` so every other/older call site is unaffected); the certificate document itself, previously hardcoding English strings ("Certificate of Completion", the "N question exam simulation · N error point(s) · N safety-critical miss(es)" summary line) even when every other piece of cert UI was already localized, now uses two new `CERT_STRINGS` keys (`certDocTitle`, `certDocSummary(n, err, wrong)`) added across all 12 locales, plus a `certLangSelectLabel` for the dropdown's own accessible label. The JSON credential download deliberately still uses `state.lang` for the small amount of human-facing text it has - out of scope for this round, not an oversight, since the JSON is primarily a data document (its schema fields aren't translated at all) rather than the human-readable certificate the HTML/PDF downloads are. Verified via Playwright: default dropdown value matches `state.lang`; selecting French and generating the certificate produces `<html lang="fr">`, the French `certDocTitle`/`certDocSummary` text, while `state.lang` itself remains unchanged (confirming the selector is genuinely independent, not a disguised app-language switch). New CSS (`.cert-lang-row`/`.cert-lang-select-wrap`/`.cert-lang-select-label`/`.cert-lang-select`) deliberately smaller/more understated than the header's own `.lang-select-wrap`, since this is a secondary per-download choice, not primary navigation.

- **DN-64 — New compliance module LkSG (Lieferkettensorgfaltspflichtengesetz) + it_sicherheit expansion (3 new categories, 24 questions) (2026-08-11)** — Two content-gap items from the competitor-research pass (`docs/compliance-competitor-pricing-and-course-gaps.md`), covered together since both were pure content-authoring work following the DN-53/DN-54 pilot pattern. **(1) New module `lksg`**: 8th compliance module, third enterprise/premium-focused one (alongside kyc_aml/kartellrecht) - the German supply-chain due-diligence law. DE/EN-only 30-question pilot (6 per topic × 5 topics: `anwendungsbereich`/scope & the 1,000-employee threshold since 1 Jan 2024 (previously 3,000 from 1 Jan 2023) including Konzern/group attribution under §1 Abs. 3, `sorgfaltspflichten`/the core §3-5 duties and risk analysis incl. the Bemühenspflicht-not-Erfolgspflicht distinction, `praevention_abhilfe`/preventive-vs-remedial measures with the "immediate cessation in own business area" vs. "remediation concept first, termination as ultima ratio for direct suppliers" split under §7, `beschwerdeverfahren`/the §8 grievance mechanism, `sanktionen`/BAFA enforcement, the §24 fine tiers (up to €800k / €500k / €100k) and the 2%-of-turnover rule for companies over €400m, §22 procurement exclusion, and §3 Abs. 3's "no new civil liability" clause plus §11's special standing to sue), grounded in WebSearched, current gesetze-im-internet.de/BAFA/press-source statute text and enforcement guidance (not fabricated section numbers) - sources included Bird & Bird, KPMG Law, and Bundestag Drucksache coverage of the September 2025 government bill (cabinet-approved, still in Bundestag committee as of the January 2026 debate per `bundestag.de`) that would abolish the §10 reporting duty and narrow the fine catalogue; since that bill had NOT yet passed as of this module's writing, the module documents the CURRENT law and flags the pending change explicitly in both `legal_review_status` and a dedicated question (`lksg-sanktionen-06`) rather than presenting either state as settled. Authored via a one-off script (`data/gen_lksg.py`, kept for authoring history same as `data/generate_pilot.py`) that writes both the master `data/lksg_pilot.json` (matching `kyc_aml_pilot.json`'s exact shape) and the deployed `app/data/lksg/core.json` + `locales/{de,en}.json` directly - `data/build_modules.py` was updated to include lksg in its own module list for future regenerations, but was deliberately NOT run (its `shutil.rmtree(APP_DATA)` would touch every other module's deployed files mid-review, an unacceptable blast radius for a two-task content change). Registered everywhere kyc_aml/kartellrecht already appear: `app/app.js` (`COMPLIANCE_MODULES` Set, `EXAM_QUESTION_COUNT_BY_TYPE`/`MAX_ERROR_POINTS_BY_TYPE`/`EXAM_TIME_LIMIT_MS_BY_TYPE` at 6 questions/4 error points/10 minutes matching the other two enterprise modules exactly, `EXAM_TOPIC_DRAW` at 2/1/1/1/1 across the 5 topics, `TOPIC_LABELS`), `app/data/modules.json` + `data/modules_manifest.json` (full 12-locale UI label, even though question content itself is DE/EN-only - same split every other DE/EN-pilot module uses), and `netlify/functions/save-verified-credential-v2.mjs`'s `COMPLIANCE_EXAM_TYPES` Set - this is the exact spot a prior real bug lived (DN-50's hinweisgeberschutz was forgotten here and got a live 400 rejection on an already-passed exam before being caught by board audit); grepped `sign-credential.js` too and confirmed it uses a generic `SAFE_CODE_RE` regex with no allowlist, so nothing to add there. `app/index.html`: full-12-locale `MODULE_LABELS` entry, added as a third card to the existing enterprise/B2B callout section (`ENTERPRISE_STRINGS` gained `lksgTitle`/`lksgDesc` per locale, "two new compliance modules" copy changed to "three" in all 12 languages via an exact-match Python script with a per-locale warning check - zero warnings fired, confirming every locale's insertion point and count-phrase were matched exactly once), added to the `groupEnterprise` module-grid group alongside kyc_aml/kartellrecht (its real-world 1,000+-employee threshold puts it squarely in the same enterprise-buyer bracket), and the page's own "11 modules"/"11 Module"/etc. locale strings bumped to 12 across all 12 languages. **(2) it_sicherheit expansion**: added 3 new categories (8 questions each, 24 total, going from 40→64 questions) to the existing, already-fully-12-locale-translated module: `social_engineering` (pretexting, phone impersonation/vishing, tailgating, baiting via a dropped USB stick, authority/urgency psychology - deliberately broader than the existing `phishing` category, which was left untouched) - `ransomware_malware` (a dedicated deep-dive distinct from `datensicherung`'s glancing coverage: what ransomware is, why not to pay, common infection vectors, double-extortion/data-exfiltration, network segmentation and least-privilege as the anti-spread measure, patch management, and the GDPR-breach-notification angle when personal data is exfiltrated) - `deepfakes_ki_bedrohungen` (AI-generated phishing recognizing that "no more typos" invalidates the old grammar-error heuristic, deepfake-CEO video-call fraud including the real, WebSearched 2024 Arup Hong Kong ~US$25m case with multiple simulated deepfake participants in one video conference, voice cloning/vishing 2.0, visual tells like unnatural blink/lip-sync, and - the one question most at risk of drifting into the wrong module - an explicit item distinguishing this security-awareness/verification skill from the separate `ki_act` module's regulatory-compliance obligations, so the two modules stay non-overlapping by design, not by accident). First read `app/data/it_sicherheit/locales/en.json` in full to confirm the locale-file shape (`{id: {question, options, explanation}}`, full text per locale, not overrides referencing core.json) before authoring anything. Grounded in real BSI IT-Grundschutz-Kompendium Bausteine confirmed via WebSearch (`OPS.1.1.4 Schutz vor Schadprogrammen` for the ransomware/malware citations, the `Social Engineering – der Mensch als Schwachstelle` and `Deepfakes – Gefahren und Gegenmaßnahmen` BSI consumer-guidance pages for their respective categories), matching the existing module's citation style and difficulty/tone exactly (verified by reading full existing `phishing`/`datensicherung` questions side by side before drafting). All 24 questions translated into all 12 locales (288 question/option/explanation triples total) via a second one-off script (`data/gen_it_sicherheit_expansion.py`, DE/EN authored directly, the other 10 written directly rather than machine-translated, keeping technical loanwords like Phishing/Ransomware/Deepfake/CEO-Fraud/Tailgating/Baiting/Pretexting/Vishing glossed on first use per language - the same convention the existing 40 questions already use for "spear phishing" etc.) that appends (never rewrites) to `data/it_sicherheit_pilot.json` (the master source, kept in the same shape as the other `*_pilot.json` files), `app/data/it_sicherheit/core.json`, and all 12 `app/data/it_sicherheit/locales/*.json` files - each append path raises on any id collision with existing content, so a bug re-running the script would fail loudly rather than silently duplicate/overwrite. it_sicherheit needed NO changes to `app.js`'s `EXAM_QUESTION_COUNT_BY_TYPE`/`MAX_ERROR_POINTS_BY_TYPE`/`EXAM_TIME_LIMIT_MS_BY_TYPE`/`EXAM_TOPIC_DRAW` maps - it was never in any of them (falls through to the driving-exam defaults already), so scaling its pool from 40 to 64 questions is fully transparent to exam-mode config; confirmed by grep before concluding no update was needed, not assumed. **Verification, both tasks**: `python3 -c "import json; json.load(...)"` on every touched/created JSON file (lksg's 4 files, it_sicherheit's core.json + all 12 locale files + the master pilot file, `app/data/modules.json`, `data/modules_manifest.json`) - all valid; `node --check` on `app/app.js`, `netlify/functions/save-verified-credential-v2.mjs`, `netlify/functions/sign-credential.js`, and the extracted inline `<script>` block from `app/index.html` - all pass; question-count cross-checks (lksg: 30 in core.json meta and actual array length and both locale files; it_sicherheit: 64 in core.json meta/actual and all 12 locale files, 8 topics × 8 questions each including the 5 pre-existing ones); uniqueness checks confirming no duplicate question text within any single locale for either module; every new question's `correct` field resolves to exactly one option letter; grepped `kyc_aml`/`kartellrecht`/`hinweisgeberschutz` across `app/app.js` and both netlify functions to confirm lksg was added to every list they appear in (one hit deliberately left alone: a comment-only historical reference to kartellrecht's own DN-53 addition, not a live allowlist). Session-numbering note: this entry claims DN-64, not DN-63 - DN-63 (badge wallet portability, below) was already claimed by a parallel session by the time this work started; all in-code comments originally drafted as DN-63 were renamed to DN-64 via a repo-wide `sed` pass before this entry was written, confirmed via grep that zero DN-63 references remain in any file this session touched. **Not done / explicitly out of scope this round**: neither module has been reviewed by a licensed lawyer or IT-security professional - both carry the same `legal_review_status: "NOT legally reviewed..."` disclaimer pattern every other module uses, and lksg's additionally flags the pending 2025/2026 amendment bill and the 2027 EU CSDDD transposition deadline as forward-looking re-review triggers.

- **DN-63 — Badge wallet portability: "Add to a badge wallet" UI, real Learner Credential Wallet import scoped and deliberately deferred (2026-08-11)** — Follow-up to `docs/badge-wallet-portability-scoping.md` (DN-51's scoping doc), whose step 3 ("defer real wallet-side integration until there's a real user asking for it") had been left open explicitly pending PO demand - the PO is now asking for it. Re-checked the doc's original assumption first, since the session's own new context (real outbound email via Resend, DN-60/DN-62) made one of the doc's two stated blockers stale: WebSearched and read Learner Credential Wallet's own README, its pilot FAQ, and related GitHub issues (not re-assumed from the earlier scoping pass) and confirmed LCW's QR/link import route is the **VC-API Interaction URL protocol** - a full issuer-hosted exchange service (initiate/continue against server-held workflow state), not a static link or a plain GET-fetchable credential URL. That's genuinely new backend infrastructure on the scale of a stateful exchange endpoint, not a small stateless function like `sign-credential.js`, for a benefit (skip Credly/Open Badge Passport's existing manual-upload step, for LCW users only) smaller than what DN-51/DN-51b already shipped - so, per the scoping doc's own stated criteria, **deliberately not built this round**; documented why in the scoping doc itself (section 5 step 3, now DONE-partial) rather than silently skipped. What **was** built: a new `renderWalletRow()` in `app/app.js`, following the exact pattern of `renderVerifyLinkRow()`/`renderEmailCertRow()` (a `<div class="cert-wallet-row">` slot per completion card, rendered into both the "My certificates" panel and the exam-results screen, re-rendered alongside the badge/JWT/verify rows every time a record gets a fresh chance to sign), gated identically to `renderVerifyLinkRow()` (`COMPLIANCE_MODULES.has(record.examType) && record.verified && record.signedJwt` - wallet import only makes sense for a credential a third party could actually verify once imported). It doesn't duplicate any existing control; it makes the two paths that already worked but weren't legible as "wallet import" explicit: (a) points at the existing signed-JWT download button (DN-51) with concrete per-platform steps ("Credly: profile → the Other tab → upload the downloaded JWT file"; the same for Open Badge Passport's "Import a badge"), and (b) a one-line pointer to the existing opt-in email-yourself flow (DN-60/DN-62, "you can also email yourself this certificate to save it for later") - explicitly reusing that feature rather than building a second, separate email-capture flow, per the scoping doc's standing constraint (section 2: never make email a requirement for a badge feature). Six new `CERT_STRINGS` keys (`walletRowTitle`/`walletRowIntro`/`walletCredlyStep`/`walletObpStep`/`walletLcwStep`/`walletEmailNote`) added across all 12 locales via a one-off Python exact-match-insert script (each locale's unique `emailTooManyMsg` line used as the anchor, verified as occurring exactly once per locale before inserting), translated per-language rather than copied from English. New CSS (`.cert-wallet-row`/`.cert-wallet-title`/`.cert-wallet-intro`/`.cert-wallet-list`/`.cert-wallet-email-note`) mirrors `.cert-verify-row`'s "dashed top border, only when populated" treatment. Verified: `node --check` on `app/app.js`; brace-balance check on `app/styles.css`; three Playwright scenarios against a local static server (a signed `kyc_aml` compliance-module record renders the wallet row with Credly/Open Badge Passport/LCW text all present; an unsigned record renders an empty wallet row; a signed but non-compliance-module `fuehrerschein` record also renders empty, confirming the gate matches `renderVerifyLinkRow()`'s exactly) plus a fourth Playwright check that the exam-results screen's copy of the row renders identically via `renderExamResults()`. **Not built, and flagged in the scoping doc as needing its own separate scoping pass if ever wanted**: a real VC-API exchange endpoint for genuine one-click Learner Credential Wallet import - real new stateful backend infrastructure, out of scope for this round by design, not an oversight.

- **DN-62 — Certificate email: support multiple recipients (2026-08-11)** — PO decision: "the email sending should be an button after the exam. probably better so the user can select multiple addresses to send the email to" (the button already lived on the exam-results screen since DN-60 - this is about the recipient field). Changed `netlify/functions/send-certificate-email.mjs`'s `to` field to accept either a single email string (backwards compatible) or an array, validating each address individually, deduping, and capping at `MAX_RECIPIENTS = 10` (a "send to yourself + your manager/HR" feature, not a mailing-list tool - the cap is a deliberate anti-abuse guard, well under Resend's own 100-recipient API limit). All recipients go in Resend's `to` field (not `bcc`) since the expected use case is a small group who already know each other, unlike the contact form. Client side (`app/app.js`): the single `<input type="email">` in `renderEmailCertRow()` gained the native HTML5 `multiple` attribute, which gives free browser-native comma-separated-email-list parsing/validation with no extra widget - `sendCertificateEmail()` now takes an array, built by splitting on commas, trimming, deduping, and validating each address client-side (mirroring the server's own validation) before sending; a new `emailTooManyMsg(n)` locale string (all 12 languages) shows if someone pastes in more than 10. Updated `emailPlaceholder` across all 12 locales to hint "comma-separated." Verified via `node --input-type=module` unit tests of the function directly (dedup, array-vs-string backwards compatibility, the 400 rejection above 10 recipients) and Playwright against the app (a message like `"a@example.com, b@example.com, a@example.com, c@example.com"` correctly deduped to 3 unique recipients in the outgoing payload, success message rendered) - a deliberately malformed list with an empty entry between two commas correctly fails the native browser validation before ever reaching the send handler, which is expected/acceptable UX rather than a bug to work around.

- **DN-61 — Landing-page contact form via Resend, live RESEND_API_KEY smoke test, found & documented Resend testing-mode limitation (2026-08-11)** — Follow-up to DN-60 ("contact form probably also a good idea"). Built `netlify/functions/send-contact-message.mjs` (same v2/export-default style, shares `RESEND_API_KEY`/`RESEND_FROM_ADDRESS` with `send-certificate-email.mjs`): validates name/email/message, includes a honeypot field (`hp_website`) that silently no-ops a submission without ever calling Resend if a bot fills it in, sets the visitor's address as `reply_to` so replying goes straight back to them, and delivers to `contact@zettacard.de` by default (overridable via new `CONTACT_TO_ADDRESS` env var). Added a full contact form to `app/index.html` (name/email/message + submit, new `CONTACT_STRINGS` across all 12 locales, new CSS) right above the footer, wired to POST to the new function with disable-while-sending/success/error states mirroring `renderEmailCertRow()`'s pattern from DN-60 - implemented separately in index.html's own inline script since the landing page doesn't share app.js's module/state. Verified via Playwright (form renders and re-renders correctly on language switch, honeypot field present but off-screen, correct JSON payload posted, success/error states and input-clearing work) and `node --input-type=module` unit checks of the function's honeypot and email-validation branches directly.

  **Live smoke-testing this round** (user added `RESEND_API_KEY` mid-session, first to production only, then to staging too after being asked): confirmed `send-certificate-email` genuinely delivers on both staging and production once the key + a fresh deploy are in place (env var changes need a redeploy to be picked up - confirmed this empirically after two rounds of "still not configured" until redeploying). Then found a real limitation via a live 502 on the contact form: **new Resend accounts are in "testing mode" until a sending domain is verified, and can only deliver to the account's own signup address** - confirmed by temporarily echoing Resend's raw rejection reason back in the API response (`403 validation_error: "You can only send testing emails to your own email address..."`), then reverting that debug code once root-caused.

  **Resolved by a PO decision, not a code fix**: user said to route every contact-form submission to `sels@tnwx.net` only (not the published `contact@zettacard.de` Impressum address) - changed `CONTACT_TO_ADDRESS`'s default accordingly. Since `sels@tnwx.net` happens to be the Resend account's own signup address, this incidentally sidesteps the testing-mode restriction entirely - **the contact form is now confirmed genuinely working end-to-end in production**, verified with a real submission from a non-owner email address (`visitor-test@example.com`) landing successfully (`{"sent":true}`) on both staging and production after redeploying with the new default. `send-certificate-email` does NOT get the same free pass, since its recipient is always whatever address the *visitor* enters in the certificate flow (essentially never the account owner's own address) - it remains blocked on real domain verification (setup doc step 2) before it will work for actual customers, documented prominently in `docs/certificate-email-setup.md` so this isn't mistaken for "done" just because a same-owner-address smoke test succeeded.

- **DN-60 — Automatic certificate email via Resend (2026-08-11)** — Second half of the user's request: "can you show the complete end2end flow with an finished exam? also we could email it to customers (users, doing it)" (E2E demo delivered separately). User picked Resend over SendGrid and "automatically after passing" over an explicit send-button-only flow, via AskUserQuestion. Built `netlify/functions/send-certificate-email.mjs` (v2/export-default Netlify Function, matching `save-verified-credential-v2.mjs`'s style): validates the request (same shape/plausibility checks as `sign-credential.js`'s validator, duplicated not imported), builds a DE/EN transactional HTML email (subject + congratulations + pass details + verify link if the credential has one, or a "download it from the app" note if not), and POSTs to Resend's REST API via plain `fetch` - no new npm dependency, so `netlify.toml`'s existing `[functions] directory` config covers it with zero changes. Added 6 new `CERT_STRINGS` keys (`emailLabel`/`emailPlaceholder`/`emailSendBtn`/`emailSending`/`emailSentMsg`/`emailErrorMsg`) across all 12 in-app locales in `app/app.js`. Added `renderEmailCertRow()` (mirrors the existing `renderVerifyLinkRow()` pattern: input + button, disables while sending, shows a success/error message) and wired it into both the exam-results screen's certificate area (the actual "automatically after passing" trigger context) and the "My certificates" panel, for parity with the existing HTML/PDF/JSON download buttons and verify-link row there. Deliberately NOT gated to `COMPLIANCE_MODULES` or to a verified/signed record like the verify-link row is - this works for every module and both signed and unsigned records, since emailing yourself a copy is a strictly lower-stakes action than creating a permanent public verification link. New CSS (`.cert-email-row`/`.cert-email-form`/`.cert-email-input`/`.cert-email-sent`/`.cert-email-error`) mirrors the existing `.cert-verify-*` rules' layout. Verified via Playwright against a local mock HTTP server (Python's stock `http.server` returns 501 on POST, so a small custom handler was used instead): the email row renders in both surfaces, clicking send POSTs the correct JSON payload (`to`/`lang`/`moduleLabel`/`scopeLabel`/`examType`/`scopeCode`/`passedAt`/`totalQuestions`/`errorPoints`/`wrongHighStakes`/`verifyUrl`) to `/.netlify/functions/send-certificate-email`, and both the success (`emailSentMsg`) and failure (`emailErrorMsg`) UI paths render correctly. The actual Resend send itself could not be tested end-to-end since no `RESEND_API_KEY` is configured yet - see new `docs/certificate-email-setup.md` (mirrors `docs/open-badges-signing-setup.md`'s structure) for exactly what the operator still needs to do (create a Resend account, verify a sending domain via DNS, set `RESEND_API_KEY`/`RESEND_FROM_ADDRESS` env vars) before this delivers real mail - until then the button fails gracefully with the existing error-message UX, same as any other offline/network failure this app already handles. Privacy note included in both the function's own header comment and the setup doc: the typed email address is used only for the single send request and is never persisted anywhere in this app.

- **DN-59 — Removed "not a substitute for individual legal advice" from the Enterprise section (2026-08-11)** — PO decision, immediately following DN-58: that specific line doesn't belong on the landing page at all - PO's suggestion was the AGB (Terms & Conditions) and/or the Impressum page (which already has a "Haftung für Inhalte" section that would be a natural home for it) as possible future locations, not a firm instruction to add it there yet. Removed the `<p id="enterprise-disclaimer">` element and its `render()` call from `app/index.html` entirely, rather than leaving an empty/near-empty paragraph. The `ENTERPRISE_STRINGS[lang].disclaimer` string data itself was deliberately left in place (unused) across all 12 locales rather than deleted, in case it's useful copy to draw from if/when AGB or Impressum text gets drafted later. Verified: inline-script syntax check passes; `id="enterprise-disclaimer"` no longer appears anywhere in the markup or JS; Playwright screenshot confirms the Enterprise section renders cleanly with no dangling empty element. **Not done yet, flagged as a possible follow-up**: actually adding equivalent wording to `app/impressum.html`'s "Haftung für Inhalte" section or to Terms & Conditions - PO floated both as options but didn't ask for either to be built out this round.

- **DN-58 — Dropped "not yet reviewed by outside counsel" from both landing-page disclaimers (2026-08-11)** — PO decision, immediately following DN-57: explicitly asked to remove the "rechtlich noch nicht extern geprüft"/"not yet reviewed by outside counsel" clause from both the Enterprise-section disclaimer and the new group-level "Workplace compliance" disclaimer, keeping the rest of each sentence (the "not a substitute for individual legal advice" line, and the "employer decides whether this satisfies their statutory training obligations" framing). PO's stated reasoning: employers already have to work out their own implementation for topics like Geldwäsche/AML regardless of Zettacard's own review status - that's not something worth hedging with a review-status disclaimer, and the "employer decides" framing (kept) already carries the relevant weight. Explicitly reversible ("können wir bei Bedarf immer noch nachholen") if it turns out to matter later. Reworded all 12 locales for both strings via exact-match replacement (no re-translation from scratch - just the one clause removed, verified nothing else in either sentence changed). `legal_review_status` itself is untouched in the module data (`core.json` meta) - this only affects what's surfaced in the landing-page UI, not the underlying field a future round could still choose to show elsewhere. Verified: grep confirms zero remaining UI-facing instances of the phrase across all 12 locales (the one remaining code-comment hit was updated for accuracy, not left stale); Playwright screenshot of both sections confirms the new wording renders correctly.

- **DN-57 — Legal-review disclaimer extended to workplace-compliance modules, "Original" wording dropped, enterprise CTA deep-link bug fixed (2026-08-11)** — Three related fixes from the user's follow-up questions after DN-56:
  1. **Translation-coverage answer**: confirmed via investigation - datenschutz/arbeitssicherheit/ki_act/it_sicherheit/hinweisgeberschutz are all fully translated (12 locales, 40/40 questions each); kyc_aml/kartellrecht remain DE/EN-only pilots (30 questions each, per DN-54's deliberate scope) - not a gap, the documented pilot state.
  2. **Legal-review disclaimer surfaced for the 5 workplace-compliance modules**: until now, the "not yet reviewed by outside counsel" caveat only ever showed on the landing page's Enterprise section, for kyc_aml/kartellrecht - even though every other compliance module's `core.json` meta already carries a `legal_review_status` field that was never actually surfaced anywhere in the UI. Per the user's explicit framing ("rechtlich nicht geprüft ist relevant für die Umsetzung der Gesetze - das muss der Unternehmer entscheiden, nicht wir"), added a new `groupComplianceDisclaimer` string (all 12 locales) rendered under the "Workplace compliance" group heading on the landing page (`app/index.html`), stating plainly that whether this content satisfies an employer's statutory training obligations is the employer's own decision to make - user explicitly scoped this to the landing page only for this round (an in-app module-intro-screen version was offered and declined for now).
  3. **Dropped "Original" from the disclaimer wording**: the existing Enterprise-section disclaimer read "Original-Lerninhalte, rechtlich noch nicht extern geprüft..." - per the user's point that this is confusing (reads like a claim about an official/external "original" source, when it's just Zettacard's own content) and that the actually load-bearing part is the legal-review caveat, not an authorship claim. Reworded to "Lerninhalte von Zettacard entwickelt, ..." across all 12 locales (both the existing enterprise disclaimer and the new group-level one use this framing). Left the *unrelated* footer copyright line ("Original-Lerninhalte, CC BY-NC-SA 4.0") untouched - that one asserts original authorship for the CC license itself, a different and still-valid claim, not the confusing juxtaposition the user was pointing at.
  4. **Real bug found and fixed mid-session** (user report: "Kostenloser Pilot springt auch derzeit zum Führerschein"): the Enterprise section had one shared "Start the free pilot" button for both kyc_aml and kartellrecht, linking to plain `./app.html` with no module hint at all - so a returning visitor whose profile had previously used a different module (e.g. Fuehrerschein) landed back on THAT module every time, regardless of which module's card they clicked. Fixed properly, not just patched: split into two per-card CTA buttons (`./app.html?exam=kyc_aml&scope=ALL` / `?exam=kartellrecht&scope=ALL`), and added real deep-link handling in `app.js`'s `loadActiveProfileState()` - a `?exam=`/`&scope=` query param now overrides whatever's saved for that profile, validated against the real module manifest (invalid/incomplete params degrade safely to the existing saved-state/picker behavior, confirmed via Playwright), and is stripped from the URL via `history.replaceState` immediately after being read so it only affects the page's first load, not every later profile switch in the same session. General-purpose fix, not special-cased to just these two modules - any future landing-page CTA can link straight into a specific module the same way.
  - Verified: `node --check`-equivalent syntax pass on both `app.js` and `index.html`'s inline scripts; JSON-LD block re-validated; full 12-locale coverage check for the two new/changed disclaimer strings (zero gaps); Playwright confirmed the CTA hrefs are module-specific, the group disclaimer renders with the right text, the deep-link override actually changes `state.examType`/`state.scopeCode` away from a seeded stale saved module, and three regression/edge cases (no query params, invalid module code, exam-without-scope) all degrade safely without crashing.

- **DN-56 — Staging environment + embedded certificate verification (2026-08-11)** — Per the user's explicit ask ("Signierung und den Staging/Prod und die Sprache noch einbauen"), three related pieces:
  1. **Staging/Prod split**: created a separate Netlify site, `zettacard-staging` (id `480e3ec6-76f6-414e-a7bc-eb3e661f5816`, `https://zettacard-staging.netlify.app`), deploying the same repo, to verify changes before they hit `zettacard.de` production - same direct-deploy MCP flow, just pointed at a different site id (git push remains blocked this session, see standing note elsewhere in this file). Per `docs/open-badges-signing-setup.md`'s own pre-existing key-rotation guidance ("consider a separate non-production keypair... so a leaked preview-deploy log can't compromise the production key"), staging got its own ES256 signing keypair (kid `a3b138a4-...`) rather than a copy of production's (kid `be87070e-...`) - both public keys now live in the one shared `app/.well-known/jwks.json` (both sites deploy the same file), and `verify-credential-v2.mjs`'s existing kid-lookup already resolves either environment's credentials correctly with zero function-code changes. Verified live end-to-end: staging's `sign-credential` function signs with its own distinct kid/issuer, production is unaffected and still signs with its original kid - confirmed via real API calls against both deployed sites, not just code review. Full writeup in `docs/open-badges-signing-setup.md`'s new "Staging environment" section.
  2. **PDF/certificate signing**: until now, only the separate JSON credential + JWT download were cryptographically signed - the certificate HTML/PDF document itself (the thing people actually print/hand to an employer) was a plain, unsigned-looking page even for a genuinely signed record. `certificateHtmlDoc()` (shared by both the HTML download and the PDF print flow from DN-49 Phase 3) now embeds real verification directly, in one of three honest states depending on what's actually true: (a) a permanent public `/verify/<id>` link already exists - embed it as text AND as a scannable QR code, so a printed/offline copy is still independently checkable by anyone with a phone; (b) the record is cryptographically signed but no public link was created yet - say so plainly, with the real key ID, rather than falling back to the generic "unverified" disclaimer, which would understate what's actually true; (c) neither - the original self-issued disclaimer, unchanged. The QR code is generated by a vendored (not CDN-loaded) MIT-licensed encoder (`app/vendor/qrcode.js`, kazuhikoarase/qrcode-generator, ~56KB) rendered as inline SVG - deliberately not jsPDF/html2canvas/a CDN image API, keeping certificate generation fully offline-capable like the rest of this zero-backend PWA; added to `service-worker.js`'s precache list (`CACHE_NAME` bumped v7->v8 per that file's own documented discipline for app-shell dependency changes). The HTML/PDF download buttons now also call `ensureSignedCredential(record)` first (matching the JSON download's existing behavior) so a record that finishes signing in the background between page-load and click-time still gets the fresher, more-verified certificate rather than a stale unsigned one. Verified: the QR actually decodes to the exact verify URL via a real scanner (`pyzbar`/`zbar`, not just visual inspection); all three states produce the correct HTML branch (Playwright); print-rendered PDF output checked for both states and fits cleanly on one A4 page.
  3. **Language**: `certVerifyIntro`/`certSignedNoLinkNote` (the two new certificate-embedded verify strings) added across all 12 `CERT_STRINGS` locales (the German/English drafted directly, the other 10 translated carefully - short, generic, low-legal-risk UI copy, not statute text, so translated directly rather than via the fable-agent legal-review pattern used for actual compliance-module content). Reused the existing but previously-unused `verifyLinkLabel` key rather than adding a duplicate. Ran a full programmatic gap-check across all 27 `CERT_STRINGS` keys × 12 locales (including the new DN-49-Phase-3 `downloadPdf` key) - zero gaps found. The independent "pick a certificate language separate from the app's current UI language" idea the user also asked about is explicitly **not** part of this round - `certificateHtmlDoc()` still follows `state.lang` same as before; scoped separately below since it's a UX-flow change (a selector control), not a translation-coverage question.
  - **Deferred to a follow-up round** (flagged, not forgotten): an explicit certificate-language selector independent of the app's current UI language (e.g. a German-UI user generating an English certificate for a foreign employer) - today the certificate always follows whatever `state.lang` currently is. The user's own "Open Badges wallet integration" follow-up (DN-51's remaining scope: a real "add to Credly/Learner Credential Wallet" button using the already-live signed JWT) is queued to start after that.

- **DN-49 Phase 3 — Client-side PDF certificate download (2026-08-11)** — Per `docs/paid-verifiable-certificates-scoping.md` section 3.3's recommendation ("start client-side for the MVP... only invest in server-rendered PDF if real B2B customers push back"), added a "Download certificate (PDF)" button alongside the existing HTML/JSON downloads, in both places a completion's certificate is offered ("My certificates" panel and the just-passed exam-results screen). Implementation deliberately adds zero new dependencies: this is a zero-backend, offline-first static PWA (see `service-worker.js`'s precache list) with no bundler, so pulling in jsPDF/html2canvas from a CDN would either break offline use or require vendoring a library for what's still an unproven-demand feature. Instead, `printCertificateAsPdf()` renders the exact same `certificateHtmlDoc()` markup the HTML download already uses into a hidden iframe (avoids disturbing the visible app and the popup-blocker risk a `window.open()` flow would carry), appends a small print-only `@page`/margin override so it lays out on one clean A4 page instead of inheriting the on-screen version's large centering margin, and calls the iframe's own `print()` - every modern browser's print dialog offers "Save as PDF" as a destination, so this gets a pixel-faithful, zero-dependency PDF of the same design already in use. `downloadPdf` button label added to `CERT_STRINGS` across all 12 locales (same "(HTML)"/"(PDF)"/"(JSON)" bracket-suffix convention already used for the other two). Verified: `node --check` syntax pass; a Playwright harness that monkeypatches `HTMLIFrameElement.prototype`'s `contentWindow` getter to spy on `print()` confirmed exactly one iframe is created and `print()` fires exactly once per click, both from a direct `printCertificateAsPdf()` call and from an actual button click inside the rendered "My certificates" panel (seeded via `localStorage` under the real per-profile key, `dn-p-<id>-completions` - the first attempt used the old flat pre-migration key `dn-completions` and silently rendered nothing, since that key was migrated away from a while back); a full-page screenshot at a 420px mobile width confirmed all three buttons (HTML/PDF/JSON) stack cleanly via the existing `.cert-card-actions` flex-wrap CSS with no layout changes needed; and a second Playwright pass fed the actual print-CSS-augmented HTML through headless Chromium's PDF export with print-media emulation, confirming the rendered certificate fits on a single A4 page with correct margins and matches the existing certificate design exactly. User asked mid-build whether `pdf.js` could be used later too - noted that `pdf.js` is a PDF *viewer/renderer* (for displaying existing PDFs), not a generator, so it doesn't substitute for this feature; flagged as a real but separate future enhancement below (an in-app preview-before-download step) rather than folded into this pass.
- **DN-55 — Landing page overhaul: missing modules, enterprise/B2B section, visual grouping, SEO (2026-08-11)** — Per the user's explicit ask to improve the landing page (`app/index.html`) as a separate track from quiz content, found and fixed a real content bug first: the module grid was a hardcoded 8-item list that never got updated across DN-50/DN-53/DN-54 - hinweisgeberschutz, kyc_aml, and kartellrecht were entirely absent, and the "8 modules" heading was stale (should be 11). User then chose all four follow-up options offered: (1) fix the missing modules, (2) add a dedicated enterprise/B2B section, (3) a visual/design refresh, (4) SEO/discoverability. **Content fix**: added the 3 missing modules to `MODULE_LABELS` (copied verbatim from `data/modules_manifest.json`, same rule the other 8 already followed) and updated `modulesHeading` from "8" to "11" in all 12 locales (careful with Arabic numeral-noun agreement specifically - "11" takes a singular accusative noun in Arabic unlike "8"'s plural, so ar became "11 وحدة" not "11 وحدات"; verified the Slavic-language plural forms already in place for "8" - genitive plural in uk/pl/ru - are the same form "11" needs too, so those only needed the digit swapped). **Visual refresh**: reorganized the flat module grid into 3 labeled groups (Driving & leisure / Workplace compliance / For enterprises) rather than one undifferentiated list, since 11 items in one grid was harder to scan than 8 was. **Enterprise/B2B section**: a new, visually distinct section (accent-tinted card, "New" badge) between the features row and the module grid, pitching kyc_aml and kartellrecht specifically at banks/financial-services/large-corporation visitors with copy that cites real specifics (§6 Abs. 2 Nr. 6 GwG's training mandate, §81d GWB fine-mitigation, real BaFin/road-construction-cartel cases) rather than generic marketing copy, plus an explicit "not yet reviewed by outside counsel" disclaimer matching this project's standing honesty norm for the two modules' own legal_review_status. Drafted DE/EN directly, translated to the other 10 locales via the established 3-parallel-agent pattern (uk/pl/ru, ar/zh/hi, tr/fr/es/it), instructed to keep "§6 Abs. 2 Nr. 6 GwG," "§81d GWB," "BaFin," and "KYC/AML" untranslated/verbatim since those name a specific German legal provision, authority, and international acronym respectively. **SEO/discoverability**: added Open Graph + Twitter Card meta tags, a canonical link (pinned to `https://zettacard.de/`, the primary custom domain, not the netlify.app alias), and JSON-LD structured data (`Organization`/`WebSite`/`SoftwareApplication`, `@graph`-linked) - none of which existed before. Generated a real 1200×630 `icons/og-banner.png` (brand colors/logo matching the existing app icon, built from a new `icons/og-banner.svg` master via `cairosvg`) since OG/Twitter unfurlers need an actual raster image, not inline SVG - first attempt used emoji glyphs which rendered as tofu boxes in the headless render environment (no color-emoji font installed), fixed by swapping to plain colored-dot bullets instead of emoji, a rendering-environment-safe choice regardless of what fonts a given unfurler bot has. Added `app/robots.txt` (allow-all + sitemap pointer) and `app/sitemap.xml` (the 4 real static pages - `app.html` is a client-rendered SPA with no server-distinct sub-URLs to enumerate beyond its one entry). Verified: all 3 `<script>` blocks (JSON-LD + 2 inline JS blocks) parse cleanly (`JSON.parse`/`new Function` in Node); every `getElementById` call in the JS resolves to a real `id` in the markup (19/19, scripted check); `sitemap.xml` validated as well-formed XML; a local static-file-server + Playwright headless-Chromium smoke test rendered the page with zero console/page errors and screenshots taken in dark mode, light mode, Arabic RTL (confirmed full mirroring plus the correct singular-Arabic "11 وحدة" numeral form), and a 390px mobile viewport - all reviewed visually, all looked correct (grouped grid, enterprise card, badge placement, legal footer links all present and readable in every mode checked). Minor iteration during review: the enterprise-module cards' subtitle originally repeated "For enterprises" a second time directly under the "FOR ENTERPRISES" group heading right above them - swapped to the "New" badge text instead to avoid the redundancy, caught by looking at the actual dark-mode screenshot rather than just the code. **Not done**: `app.html` (the app itself) was left untouched - this was explicitly the landing-page-only track per the user's own framing; the hero/feature copy's prose (the `lede` strings) still describes modules generically ("workplace-compliance topics") rather than exhaustively naming all 11, left as-is since the grid below already enumerates every module by name and rewriting 12 locales of hero prose wasn't part of what was asked.

- **DN-54 — Right-sized exam pool/draw for the two enterprise compliance modules (2026-08-10)** — Per the user's explicit sizing request ("25-30 questions, 5-7 at the exam"), scaled both kyc_aml and kartellrecht from their 20-question pilots to 30-question pools (6 per topic × 5 topics) and refactored the exam engine so exam size, pass threshold, and time limit are per-module instead of the single global constants every other module shares (30-question pool draw, max 10 error points, 45-minute Simulation timer - a driving-exam-length design that made no sense for a 6-question corporate knowledge-check). New content for both modules (10 new questions each, drafted via a `fable`-model agent per module, WebSearch-verified against current statute text) was chosen specifically to close real gaps the DN-53 pre-review dossiers had already flagged, rather than padding with more of the same: kyc_aml added Transparenzregister/§16a cash-payment ban, identification methods (§§11-13 GwG) and the entity's own risk-analysis duty (§§4-5 GwG), the §46 GwG transaction-suspension mechanic and the BaFin-vs-FIU/goAML reporting-channel split, the §25h KWG bank-specific overlay and correspondent-banking enhanced due diligence, and the §7 GwG Geldwäschebeauftragter duty plus a second, independent BaFin enforcement example (J.P. Morgan SE, EUR 45m, Nov 2025, distinct from the KT Bank case already used); kartellrecht added actual §19 GWB abuse-of-dominance conduct categories and vertical-restraint/resale-price-maintenance coverage, dawn-raid conduct and EU merger-control basics, the cross-jurisdiction individual-criminal-liability asymmetry (Germany's administrative-only company fines vs. US Sherman Act/UK cartel-offence individual imprisonment) and private follow-on damages (§§33a ff. GWB), Wettbewerbsregister deletion/self-cleaning mechanics and the EU-directive basis for German self-cleaning, and the leniency-immunity/individual-liability asymmetry plus the §81m GWB marker system. Engine changes in `app/app.js`: `EXAM_QUESTION_COUNT_BY_TYPE`/`MAX_ERROR_POINTS_BY_TYPE`/`EXAM_TIME_LIMIT_MS_BY_TYPE` maps (kyc_aml/kartellrecht: 6-question draw, max 4 error points ~"5 of 6 correct", 10-minute Simulation timer; every other module falls through to the original 30/10/45min driving-exam defaults, unchanged), new `EXAM_TOPIC_DRAW` entries for both modules (one topic draws 2, the other four draw 1 each, so every run touches all 5 topics), and all 12 locales' exam-picker copy (`pickerDesc`/`simDesc`) and results-summary copy (`summary`) converted from hardcoded "30 questions"/"45 minutes"/"max. 10" strings to functions parameterized by the actual per-module values - a real latent bug avoided, since without this every module's exam picker would have kept claiming "30 questions, 45 minutes" even while actually running a 6-question, 10-minute exam. Verified: a standalone Node harness re-implementing `drawExamQuestions()` against the real built `core.json` confirmed 6 unique questions and all 5 topics represented across repeated runs; full `data/build_modules.py` rebuild (both modules 30/30, zero locale gaps) + `assets/build_sign_reference.py` + `data/build_primers.py` re-run after (standard wipe-and-rebuild gotcha); `node --check app/app.js`. Deployed to Netlify. **Not yet done**: the "badge or PDF via email" delivery the user mentioned alongside the sizing request was explicitly scoped out of this round (user's own choice, since there's no email-sending capability anywhere in the app yet) - the existing in-app downloadable HTML certificate/JSON credential flow (already live for every module, including both new ones) is what a passed Simulation run produces today; real email delivery is a separate follow-up requiring a transactional-email-provider decision and API key from the operator.

- **DN-53 (follow-up) — "Pre-review dossiers" for both enterprise modules, cost-reduction pass before human-lawyer sign-off (2026-08-10)** — Per the user's explicit instruction to run a second fable-model pass whose job is to do as much of a real lawyer's legwork as an AI responsibly can, so the eventual attorney review costs less, ran a dedicated "pre-review dossier" agent per module (independent from, and deliberately not trusting, the earlier same-day accuracy reviews - re-verified everything fresh). Each dossier produced: a full citation ledger (every question's exact provision, verbatim/near-verbatim statutory quote, and working source URL - the specific busywork that eats the most billable lawyer hours), a currency re-check against today's statute text, a 3-tier risk classification (Tier A = rubber-stampable, Tier B = correct but worth a lawyer's sanity-check on framing, Tier C = genuine judgment calls flagged for actual attorney attention), a gap-hunt for what's conspicuously missing for the specific target audience (banks for KYC/AML, large corporations for Kartellrecht), and an independent vendor-phrasing similarity re-check. Saved in full to the Claude Project: `claude/kyc-aml-pre-review-dossier-2026-08-10.md` and `claude/kartellrecht-pre-review-dossier-2026-08-10.md`. **KYC/AML result**: zero new substantive errors across all 20 questions (this morning's leichtfertig/fahrlässig fix confirmed still correctly in place); 14 Tier A, 5 Tier B, 1 Tier C (`kyc-aml-verdachtsmeldung-04`'s lawyer-exception wording conflated client intent with the actual professional-knowledge legal standard, and overstated the GwGMeldV-Immobilien carve-out's scope) - fixed the wording to match the correct professional-knowledge standard and softened the Immobilien claim, but left this flagged as the highest-value item for actual counsel to confirm scope on. Gap list surfaced for a possible future round: §25h/§25i KWG overlap for actual credit institutions, the BaFin-vs-FIU supervisory split, Geldwäschebeauftragter (§7 GwG), the §46 GwG 3-day transaction-suspension mechanic, and Transparenzregister/goAML registration - none added yet, just logged. **Kartellrecht result**: zero new substantive errors on the core citations, but 2 additional wording-precision defects found that this morning's first review missed: `kartellrecht-selbstreinigung-04` implied both §123 and §124 GWB exclusions are equally "zwingend" (mandatory) when §124's exclusion grounds are actually facultative/discretionary and cartel offenses typically fall under that discretionary ground - fixed to state the distinction; `kartellrecht-selbstreinigung-02` implied training has its own separate "personelle Maßnahmen" statutory pigeonhole when the statute actually names technical/organizational/personnel measures together as one combined third requirement - fixed the framing. Also caught and fixed an overclaim in the module's own meta description (it promised Sec. 19 GWB abuse-of-dominance / Art. 102 TFEU coverage that no actual question in this pilot delivers) - corrected the description to be accurate and flagged real §19/Art. 102 coverage as a gap for the next round, alongside EU merger control, dawn-raid procedure, and leniency-race mechanics (first-mover-gets-immunity), none of which exist yet either. All fixes in both modules were factual/wording corrections that changed zero answer keys - both dossiers explicitly flag their respective Tier C items (the KYC/AML lawyer-exception nuance) as needing genuine attorney judgment, not something an AI pass should resolve unilaterally. Rebuilt via the full pipeline (`build_modules.py` → `build_sign_reference.py` → `build_primers.py`) after applying fixes, redeployed to Netlify. **Net effect for the stated goal (reduce billable lawyer time)**: both modules now have a ready-made citation ledger + risk-tiered summary a lawyer can work from directly instead of building one from scratch, with the genuinely judgment-dependent items already isolated and named rather than buried among 40 otherwise-solid questions. The human-lawyer/Kartellrechts- or GwG-Compliance-Beauftragter sign-off gate itself is unchanged and still required before commercial use - this pass reduces its cost, not its necessity.

- **DN-53 (second module) — Kartellrecht compliance pilot, 7th compliance module, second enterprise-premium module (2026-08-10)** — Per the user's own explicit build-order choice (KYC/AML first, antitrust second), built the deferred antitrust/competition-law module now that KYC/AML shipped and passed its own AI legal review. Grounded via a dedicated research pass (saved to the project as `claude/kartellrecht-question-bank-sources.md`) covering: the cartel prohibition and its three conduct categories (§1 GWB, mirroring Art. 101 TFEU), the "bezwecken oder bewirken" object-or-effect test and why hardcore restrictions get no market-analysis defense, the dominance test and 40%/collective-dominance presumption thresholds (§18 GWB), abuse-of-dominance categories (§19 GWB), the 10%-of-worldwide-group-turnover fine cap (§81c GWB — correcting an initial assumption it lived in §81e, which doesn't exist as a cap provision), the compliance-program fine-mitigation factor (§81d GWB) and the Bundeskartellamt's own explicitly-anti-checkbox compliance guidance (9 non-mandatory "building blocks," no prescribed training frequency), self-cleaning requirements for regaining public-tender eligibility (§125 GWB's three cumulative numbered requirements), and individual criminal liability for bid-rigging (§298 StGB, up to 5 years imprisonment, plus its voluntary-withdrawal defense). Three real, named 2025-2026 enforcement examples used as concrete question material: the €60.3m German road-construction bid-rigging cartel (Aug 2026 — coded-language coordination, e.g. using beer/sausage prices and hotel rates as stand-ins for bid figures, a genuinely excellent training scenario), the €458m EU automotive-recycling cartel (April 2025, 15 manufacturers + ACEA, a rare non-price/information-suppression cartel), and the €329m Delivery Hero/Glovo case (June 2025, the EU's first-ever labour-market/no-poach cartel fine — confirms no-poach agreements between competitors are actively prosecuted, not a grey area). Shipped: a 20-question DE/EN-only pilot (`data/kartellrecht_pilot.json`) across 5 topics (grundlagen, kernbeschraenkungen/hardcore restrictions & risk scenarios, bussgelder/fines & liability, selbstreinigung, straftaten/individual liability & enforcement practice), wired through the identical pipeline as kyc_aml: `build_modules.py`, `TOPIC_LABELS.kartellrecht`/`COMPLIANCE_MODULES` in `app.js`, `COMPLIANCE_EXAM_TYPES` in `save-verified-credential-v2.mjs` (both allowlists updated in the same commit again, keeping the DN-50 drift-avoidance discipline going), and a `modules_manifest.json` entry (label translated to all 12 locales). Free pilot, same as kyc_aml - pricing still deliberately deferred. **AI legal review (fable model) run immediately after drafting**, same discipline as kyc_aml: 17 of 20 questions confirmed accurate on first pass against current GWB/StGB text and the three named enforcement sources; found and fixed 2 real citation/wording defects (neither changed an answer key): `kartellrecht-straftaten-04`'s `legal_basis` was wrong (§81b GWB is association-fine liability, not the leniency program — corrected to §§81h ff. GWB, the actual Kronzeugenprogramm/Bonusregelung provisions), and `kartellrecht-selbstreinigung-03` incorrectly asserted a "schriftlich" (written-form) requirement for §125 Abs. 2 GWB's reasons obligation that the statute doesn't actually impose (removed). Also fixed a garbled explanation sentence in `kartellrecht-kernbeschraenkungen-03` (the automotive-recycling cartel: manufacturers agreed among themselves not to advertise recyclability to *customers*, not "to each other" as originally worded). Confirmed via the review: no question fabricates a published Bundeskartellamt precedent applying §81d's compliance-mitigation factor with disclosed figures (the research had already flagged that no such case exists yet) - the module correctly states the mitigation as statutory/discretionary, not exemplified by a named case. Rebuilt via the full pipeline (`build_modules.py` → `build_sign_reference.py` → `build_primers.py`) after both the initial draft and the post-review fixes; `node --check` clean on both edited JS/mjs files. **Standard caveat, same as kyc_aml and every other compliance module**: an AI legal-review pass materially improves the baseline but does not replace the human-lawyer/Kartellrechts-Compliance-Beauftragter review gate that still stands before any commercial or production use. With both enterprise-premium modules now built and AI-reviewed, remaining work per the user's own stated priorities is: pricing (explicitly deferred - free pilots for now), and scaling either module to 40 questions/12 locales once real demand or the human-lawyer review clears them for wider rollout.

- **DN-53 — KYC/AML compliance pilot, 6th compliance module, first enterprise-premium module (2026-08-10)** — Following the user's explicit push to go after big-corporation compliance buyers ("monopoly compliance and banking KYC, banks would pay a premium"), scoped both antitrust/Kartellrecht and KYC/AML candidates via two parallel research agents (report: `docs/antitrust-kyc-enterprise-scoping.md`, also mirrored to the Claude Project) before building either. KYC/AML won the build-first slot (user's own choice, of two options offered) because it rests on a genuinely stronger legal hook than antitrust: §6 Abs. 2 Nr. 6 GwG imposes an explicit "erstmalig und danach fortlaufend" (initial-and-ongoing) staff-training mandate on all ~16 categories of obligated entities (§2 Abs. 1 GwG) — banks, payment/e-money institutions, insurers, real-estate agents, goods dealers above threshold, and more — whereas antitrust-compliance training in Germany is discretionary/mitigation-only (§81d GWB fine-mitigation, §125 GWB Selbstreinigung for tender re-eligibility), not itself a statutory duty. A direct German competitor price point was found (exkulpa's GwG e-learning, €36→€30/person sliding by seat count), supporting a plausible €25-50/employee/year anchor once pricing is decided. Per the user's own choice, **ships free for now** — pricing intentionally deferred until real enterprise/bank demand is confirmed, matching every other compliance module's 0€ MVP posture. A third, narrowly-targeted research pass then pinned down precise citable facts before any quiz content was written (never paraphrased from a commercial training vendor's material, matching this project's standing sourcing discipline): §261 StGB's 18 March 2021 reform to an all-crimes predicate-offense approach; §10 GwG's five general due-diligence duties and EUR thresholds (€15,000 general, €1,000 per Art. 3 Nr. 9 EU-Reg. 2023/1113 for money/crypto-asset transfers, €2,000 gambling); §43 GwG's suspicious-activity reporting duty (no minimum-value exemption) and §47's tipping-off prohibition; §15 GwG's enhanced due diligence for PEPs (§1 Abs. 12-14, including family/close-associate scope) and high-risk countries, plus its §15 Abs. 4 senior-management-approval requirement; §56 GwG's fine tiers (up to €1m/2× economic benefit generally, up to €5m or 10% of annual turnover for credit/financial institutions specifically); and a real, specific enforcement example (BaFin's €600,000 fine against KT Bank AG, 23 January 2025, citing specific GwG paragraph violations). One lower-confidence figure flagged by the research (the §10 Abs. 6a goods/art-dealer thresholds) was deliberately excluded from question content rather than risked. Shipped: a 20-question DE/EN-only pilot (`data/kyc_aml_pilot.json`) across 5 topics (grundlagen/money-laundering fundamentals, sorgfaltspflichten/general due diligence, verdachtsmeldung/suspicious-activity reporting, verstaerkte_sorgfalt/enhanced due diligence for PEPs & high-risk, sanktionen/fines & the criminal offense), each question carrying a specific GwG/StGB paragraph citation, following the exact schema of the existing compliance-module pilots (verified by reading `data/hinweisgeberschutz_pilot.json` in full first). Wired the same way every prior compliance module launched: a `modules_manifest.json` entry (module label translated to all 12 locales even though question content itself is DE/EN-only for now, matching the module-picker/content-locale-count split every other module has), a new `split_module()` call in `data/build_modules.py`, and `TOPIC_LABELS.kyc_aml`/`COMPLIANCE_MODULES` entries in `app/app.js` plus the matching `COMPLIANCE_EXAM_TYPES` entry in `netlify/functions/save-verified-credential-v2.mjs` — added both allowlists in the same pass this time, specifically to avoid repeating the exact DN-50 allowlist-drift bug (client-side gate updated, server-side allowlist forgotten) found earlier in this project's history. Below the 30-question Exam Simulation floor by design (same as every module's original pilot). Verified: `python3 -m json.tool` + a structural check (20 questions, correct 4-per-topic split, DE+EN text/options/explanation present on every question, `correct` field shape matches the existing schema) on the pilot JSON before wiring it in; a full `data/build_modules.py` rebuild (zero locale gaps across all 10 modules, sanity checks passed); `assets/build_sign_reference.py` and `data/build_primers.py` re-run immediately after (the standing gotcha — a full rebuild wipes `app/data/**`, which are not among `build_modules.py`'s own outputs); `node --check` on both edited `.js`/`.mjs` files; a local static-file-server + `curl` check confirming `app/data/modules.json` lists `kyc_aml` and `app/data/kyc_aml/core.json`+`locales/{de,en}.json` serve the expected 20-question/20-entry shape. **Not yet done, by design per the user's own stated sequencing**: legal review (user explicitly asked to plan this via the `fable` model "later," once real content exists — this pilot is that real content, so the fable review is the logical next step); scaling to 40 questions/12 locales (deferred until after legal review, same order DN-50's Hinweisgeberschutz module followed); the antitrust/Kartellrecht module (explicitly deferred — user chose KYC/AML first); any pricing/paid-tier work (explicitly deferred — user chose "free pilot, decide pricing later"). **Standard caveat, same as every other compliance module**: this was drafted by an AI assistant grounded in verified statute text and one real enforcement example, not reviewed by a lawyer or GwG compliance officer — must not be relied on for actual regulatory training obligations until reviewed.

- **DN-53 (follow-up) — KYC/AML AI legal review pass, fable model (2026-08-10)** — Per the user's explicit request to plan an AI-model-driven legal review of the KYC/AML pilot "later" once real content existed, ran that review via the `fable` model against the live `data/kyc_aml_pilot.json` plus `docs/antitrust-kyc-enterprise-scoping.md`, question by question, cross-checked against current GwG/StGB statute text (dejure.org mirrors reflecting the FinmadiG amendments in force since 30 Dec 2024) and BaFin's own published enforcement notice for the KT Bank AG case. Result: 18 of 20 questions confirmed accurate on first pass, including the enforcement-case question (kyc-aml-sanktionen-03), which matched BaFin's notice text and cited paragraphs exactly. Found and fixed 2 real errors, both the same underlying mistake: conflating "fahrlässig" (ordinary negligence, not punishable) with "leichtfertig" (gross/reckless negligence, the actual §261 Abs. 6 StGB / §56 Abs. 1 GwG standard) — a distinction a real bank Geldwäschebeauftragter would flag immediately. Fixed kyc-aml-grundlagen-03 (question, a distractor option, and both DE/EN explanations reworded to the correct leichtfertig standard) and kyc-aml-sanktionen-04 (explanation wording only — the answer itself was already correct). Also applied one recommended enhancement: added a sentence to kyc-aml-verdachtsmeldung-04's explanation noting the §43 Abs. 6 GwG / GwGMeldV-Immobilien real-estate carve-out where the lawyer/notary reporting exemption falls away. Confirmed via the review that the EU's incoming AML package (Regulation (EU) 2024/1624 "AMLR", AMLD6, the new AMLA authority) does **not** invalidate any current answer — AMLR only applies from 10 July 2027 — but flagged that AMLR will lower the general occasional-transaction CDD threshold from EUR 15,000 to EUR 10,000, so `data/kyc_aml_pilot.json`'s `meta.legal_review_status` now explicitly notes a hard re-review deadline ahead of that date, independent of the normal 12-month renewal cycle. Rebuilt via the full pipeline (`build_modules.py` → `build_sign_reference.py` → `build_primers.py`), redeployed to Netlify, confirmed live via `curl` against `app/data/kyc_aml/core.json`. **This AI review does not replace the human-lawyer/GwG-Compliance-Beauftragter review gate** — that still stands before any commercial or production use; the review just means that gate now starts from a materially cleaner baseline than an unreviewed first draft. Next: same review treatment planned for the antitrust/Kartellrecht module once it's built (build order: KYC/AML first, antitrust second, per the user's own explicit sequencing).

- **Impressum + Datenschutzerklärung added (2026-08-09)** — Legal-compliance requirement: tronicum networx GmbH (Zettacard's operator) needs a proper German Impressum (§5 DDG, the 2024 successor to §5 TMG) and privacy policy (Art. 13 DSGVO), neither of which existed anywhere in the app before. Built `app/impressum.html` and `app/datenschutzerklaerung.html` as standalone static pages (German-authoritative text + a clearly-marked non-binding English translation below a divider, not a language toggle — legal text carries real mistranslation risk in a way this app's learning content doesn't, so these two pages are a deliberate exception to the project's usual 12-locale-from-day-one bar). Impressum content: tronicum networx GmbH, Markgrafendamm 4, 10245 Berlin; Geschäftsführer Stefan Sels; contact@zettacard.de / 0700 800 70000; Handelsregister Amtsgericht Berlin (Charlottenburg), HRB 159784 B; standard EU-ODR dispute-resolution clause and Haftung-für-Inhalte/Links/Urheberrecht boilerplate. No USt-IdNr. was provided, so that line was simply omitted rather than fabricated. The privacy policy was NOT written from a generic template — it was grounded in what this codebase actually does, checked directly rather than assumed: confirmed via `grep` that there are zero cookies, analytics, or tracking scripts anywhere in the repo; enumerated every actual `localStorage` key `app/app.js` writes (language/theme, module/scope selection, starred/seen questions, Leitner SRS progress, exam completions, local device-profile names, topic/role filter memory) as the "local storage on your device" section; and traced the one genuine exception where personal data DOES leave the device — DN-49's optional "create a permanent verification link" feature (`save-verified-credential-v2.mjs`), which stores an optional user-typed name plus exam metadata in Netlify Blobs (Netlify, Inc., USA) and makes it publicly reachable via the generated link, only when a user explicitly opts in. Confirmed separately that `sign-credential.js` (the JWT-signing step) never receives or stores a name, only exam metadata — this distinction between "signing" (stateless, no personal data) and "the public verify link" (stateful, optional name, opt-in) is called out explicitly rather than conflated, since blurring it would have made the policy either overstate or understate what actually happens. Flagged honestly in the policy that deleting an already-created verify-link record currently requires a manual request to contact@zettacard.de (there's no self-service delete button in the app, and DN-49's own BACKLOG entry already noted this session's tooling has no authenticated Netlify CLI session to run `netlify blobs:delete` itself) — the PO/operator will need an actual process for handling such requests when they come in, not just the promise of one. Wired in as footer links from both `app/index.html` (translated link labels in all 12 locales, pointing at the same DE/EN pages) and `app/app.html` (a small always-visible footer, reachable without opening any modal, since a German Impressum must be reachable in a couple of clicks from anywhere on the site, not buried behind app state). Verified: `node --check app/app.js`, an id-uniqueness scan across all 4 HTML files, and a local static-file-server + `curl` check that both new pages serve with HTTP 200 and contain the expected HRB number / no-cookies statement. **Standard caveat**: this was drafted by an AI assistant grounded in the actual codebase, not reviewed by a lawyer — the operator (tronicum networx GmbH) should have it checked by counsel before relying on it, especially the assumption that Zettacard's content isn't "journalistic-editorial" under §18(2) MStV (which would otherwise require a separately named `Verantwortlicher`) and that Art. 6(1)(a)/(b)/(f) GDPR are the correct legal bases cited for each processing purpose.

- **DN-52 Phase 1 — kickstart-learning-journey topic primers, all 11 Fuehrerschein topics + sign shapes/categories (2026-08-09)** — First concrete build step after the DN-52 scoping doc (`docs/kickstart-learning-journey-scoping.md`) and the PO's "early voluntary exam pass counts as completion" decision, per the PO's "let's do it step by step" instruction to work through the whole Fahrschule feature. Built exactly Phase 1 as scoped: a sign-shapes-and-categories primer (6 chunks) plus one topic primer per Fuehrerschein subject (vorfahrt, verkehrszeichen, gefahr, umwelt, verhalten, autobahn, parken, ladung, erstehilfe, fahrtuechtigkeit, anhaenger_be — 6-7 chunks each), 83 chunks total. Content was drafted by 11 independent parallel agents (one per topic) plus one directly-authored primer (shape/category), each grounded in 10 real, already-verified sample questions per topic pulled from `data/pilot_questions.json` (question/correct-option/explanation/legal_basis), with an explicit instruction not to invent new legal claims beyond those samples or extremely well-established StVO/StVZO/StVG/StGB structure — same sourcing discipline as every other content round this project has shipped (AGENTS.md non-negotiable constraint 1). Translated into all 12 locales via the established 3-way parallel-agent pattern (uk/pl/ru, ar/zh/hi, tr/fr/es/it), with German legal citations (§ references, Zeichen numbers) deliberately left untranslated/verbatim in every language since they name specific German legal provisions. Zero locale gaps, verified structurally (every chunk has a non-empty title+body for all 12 locales) both before and after merging. Wired through a proper build pipeline mirroring the existing question-data convention: `data/fuehrerschein_primers_source.json` (source of truth, DE+EN+10 more locales per chunk) → `data/build_primers.py` (new script, run from `data/`, fails loudly on any locale gap rather than shipping one silently) → `app/data/fuehrerschein/primers.json` (structural: id/topic_code/order) + `app/data/fuehrerschein/primers_locales/<lang>.json` (per-locale title/body) — edits go through the source file from now on, never the generated app/data files directly. Confirmed the standing `build_modules.py` wipe-and-rebuild gotcha applies here too (it deletes everything under `app/data/fuehrerschein/` including the new primer files, same as it's always done to `sign_reference.json`) — `build_primers.py` must be re-run after `build_modules.py`, same as `assets/build_sign_reference.py` already needed to be; documented in `build_primers.py`'s own header comment. Front-end: new "🧭 Learn" header button (Fuehrerschein-only, same visibility gating as the existing "📚 Signs" button), a topic-list modal (`#primers-view`), and a chunk-by-chunk reader modal (`#primer-reader`) reusing the exact dots/back/next wizard mechanic the DN-43 module-intro wizard already established, rather than inventing a new navigation pattern. The reader's last chunk becomes a "practice this topic now" handoff that closes both modals and jumps straight into the existing topic-filtered question list via `state.topicFilter` (the same mechanism the topic-filter chips already use, including the same `profileKey`-scoped persistence) — no new practice-tracking mechanism needed, per the scoping doc's Phase-1 boundary. Also found and fixed a real gap while wiring this in: `offlineAssetUrls()` (DN-46's "prepare for offline" feature) didn't know about the new primer files at all, which would have meant "prepare for offline" silently leaving primers dependent on a live network the first time one was opened — added `data/fuehrerschein/primers.json` + the active locale's `primers_locales/<lang>.json` to that list for the Fuehrerschein module. Verified: `node --check app/app.js`; a full `data/build_modules.py` + `assets/build_sign_reference.py` + `data/build_primers.py` rebuild in that order (confirms the three-artifact pipeline coexists correctly and nothing else drifted — `git status` showed zero unintended diffs from questions/sign-reference regenerating); a local static-file-server + `curl` check that `app/data/fuehrerschein/primers.json` and a sampled `primers_locales/ar.json` serve correctly with the expected 83-entry/12-locale shape; a structural locale-completeness script confirming zero gaps. **Not yet verified in a live browser** — same standing sandbox Playwright/Chromium networking outage as every other item this session (`net::ERR_CONNECTION_RESET` on all HTTPS); re-verify next session by opening the Fuehrerschein module, clicking "🧭 Learn," reading through a primer (including a non-DE/EN language), and confirming the final "practice this topic now" button correctly lands on that topic's filtered question list. Remaining per the scoping doc's build order: Phase 2 (situational crossing/scenario primers, Vorfahrt first since DN-3's 7 birds-eye diagrams already exist), Phase 3 (practice-quiz tier), Phase 4 (kickstart path wizard), Phase 5 stretch (roll primers out to other modules).

- **Role-filter row visible on every module, including Fuehrerschein (real bug, user-reported 2026-08-08)** — PO reported "the driving test still shows you stuff for IT security" - specifically the row of chips below the topic filter (All / All staff / HR / IT / Management). This was NOT the cross-module topic-filter leak fixed earlier this session (that was about a REMEMBERED filter value bleeding across modules after a reload) - this was a plain CSS bug making the role-filter row permanently visible on every module, always, regardless of `renderRoleFilter()`'s own `container.hidden = !isCompliance` logic in `app.js`, which was correct and never the problem. Root cause: `#role-filters` is `class="filters role-filters"` and starts `hidden` in `app.html`, but `.filters { display: flex; }` in `styles.css` is a normal author-stylesheet rule, and normal author rules always beat the browser's built-in `[hidden] { display: none }` rule regardless of specificity (origin/importance is resolved before specificity in the CSS cascade) - so setting the `hidden` property in JS never actually hid the row, on any module, the entire time DN-44's role filter has existed. This is the exact same bug class already fixed twice elsewhere in this file (`.image-note`/`.image-illustration`, `.exam-modal`/`.exam-view` - both have an explicit `[hidden]` override) - it just was never applied to `.role-filters` when that feature shipped. Fixed with the same explicit override pattern: `.role-filters[hidden] { display: none; }` (specificity (0,2,0), strictly beats `.filters`'s (0,1,0), so this fix holds regardless of cascade/source-order questions). Audited every other `hidden`-attribute element in `app.html` against its own CSS class for the same bug class - only `.role-filters` had it; `.review-actions`, `.explanation`, `.module-switch-btn`, `.offline-prep-status`, `.exam-timer`, `.exam-skip-banner`, `.back-btn` etc. don't set `display` on their own bare class rule, so `[hidden]`'s native behavior already worked correctly for all of them. Verified: CSS brace-balance check, a specificity calculation confirming the fix is unconditionally correct (not just "probably works"). **Not yet verified live in a real browser** - same sandbox Playwright/Chromium networking outage as everything else this session; re-verify next session by opening Fuehrerschein and confirming the role-filter row is now actually gone (not just present-but-empty), then switching to a compliance module and confirming it reappears correctly.

- **DN-49 (remaining step) — Blobs store scale-test past a handful of records (2026-08-08)** — Per the remaining DN-49 scope ("scale-testing the Blobs store past a handful of records"), and immediately useful given the same-day allowlist-drift fix above (needed a real end-to-end check that Hinweisgeberschutz permanent links actually work now, not just that the code compiles): scripted 25 real sign-and-save round trips against the live production functions (`sign-credential` -> `save-verified-credential-v2` -> `/verify/<slug>`), spread across all 5 compliance modules (5 each). Result: 25/25 created successfully, 25/25 unique slugs (no collisions), 25/25 read back as HTTP 200 with correct rendered content - confirmed directly (not self-reported) by fetching one Hinweisgeberschutz result and finding "✅ Signatur gültig", the correct module label, and the test participant name in the response body. This also served as the real end-to-end verification for the allowlist-drift fix above: Hinweisgeberschutz permanent-link creation, which would have 400'd before that fix, now genuinely works. **Known side effect, disclosed rather than hidden**: this created 25 real records in the LIVE production Netlify Blobs store (`verified-credentials`), each clearly labeled with a `scaletest-` id prefix and a "ScaleTest N" participant name - harmless synthetic data (no real person's information, no real exam results), but real entries nonetheless, and this session's tools have no Blobs delete capability and no authenticated `netlify` CLI session to run one (`netlify login` requires an interactive device-pairing step this sandbox can't do). To remove them: `netlify blobs:delete verified-credentials <slug>` for each of the 25 slugs below, from a machine with an authenticated Netlify CLI session - or just leave them in place, since they're inert and clearly marked as test data (`scaletest-` id prefix, "ScaleTest N" participant name) if anyone ever opens one directly. Slugs: `04c5248a-32d8-473e-967b-fee19221fdc5`, `81bc5c17-708b-4bef-b66e-f42992b0f6c2`, `52c76dfd-328f-4a96-953c-7bf802c4a909`, `08c3acae-fa01-4f38-8ee9-ceed1d846ac7`, `9ec2137b-32bf-49ef-a3f7-7f0937264302`, `45706fa1-e219-4778-a644-3e42fcfb9b02`, `6a71133a-7f3f-4fdb-802d-be9d80cb27ca`, `3abcb64a-d365-466f-b1d9-76c872522255`, `7d68ad01-99c8-4b95-beae-8ac7e15c102f`, `70510e9b-0392-4ee2-b530-5db435f4b9a5`, `579c51ee-5d7c-436c-ad83-7108f58c9542`, `a35d2cf6-bd8a-4e15-8666-afd1d7308fa4`, `79e03df0-36cc-470d-83a5-7c2424416f11`, `940c4cd0-f200-4eda-81fc-49d440918412`, `8160ba52-66bc-4a49-9292-685dd0af59d0`, `1bb9c8ae-fc81-4fac-b44c-f13bfc868967`, `d2f6d46f-13ef-4423-b976-2adccfd0fa5a`, `43677d31-595c-4eeb-a9ef-fdbea823fd38`, `b111c317-ead7-4276-ba7d-7fc2195e7ed5`, `22a5dc74-1b1b-4033-ab14-42be023c647a`, `532af282-510b-40d8-87a2-7880b08325a0`, `00bd8ed5-93d1-4602-8b4f-4d739d18b42f`, `74bec060-05ba-4181-acb9-c7b2721f78fb`, `208aa76b-c1c1-4882-9b8c-04b15f53665c`, `17ce0b7c-e6a6-479b-8181-3268b94f358c`.

- **DN-23 (safe-area-inset padding + tap-affordance hint, 2026-08-08)** — Last two open items from the first UX review's P2 list (focus-outline styling already done under DN-24). (1) Safe-area-inset: `app/app.html`'s viewport meta already had `viewport-fit=cover`, no change needed there; added `calc(<original> + env(safe-area-inset-<side>))` padding (additive, so it's a floor on top of existing spacing - a no-op `+0` on non-notched devices) to every fixed/sticky full-bleed surface: `header`, `main`, `.detail-header` (confirmed via `app.html` that `#exam-view` reuses this same class, so one fix covers both the flashcard detail view and the exam view's top bar), and `.detail-body` (the shared scrollable content area for both `.detail` and `.exam-view`). Checked `.exam-modal` too - it's a centered, non-edge-to-edge card (`padding: 20px` around a `max-width: 440px` card), so it doesn't need the same treatment. (2) Tap-affordance hint: found that the underlying complaint - options only looking tappable via `cursor: pointer`, which is invisible on a touch device with no hover state - was already half-fixed by the later "try it yourself" flashcard self-answer feature (adds `role="button"/"checkbox"`, `tabIndex`, keyboard handling, and a real click handler to pre-reveal option rows), so options genuinely are independently tappable now, just with no visible signal of that on touch. Closed the remaining gap: added a small `try-it-hint` badge (new `tryItHint` UI string, all 12 locales) next to the topic/points badges above the options, shown only pre-reveal, plus an explicit `:active` background-shift so tapping an option gives visible feedback instead of nothing. Exam-mode's own option rows were deliberately left alone - that view already reads as an interactive answer-selection UI on its own terms, no ambiguity to hint at. Verified: `node --check app/app.js`, a CSS brace/paren balance check (no linter available in this sandbox), and confirmed all 12 `tryItHint` locale entries landed in the correct per-locale `UI_STRINGS` blocks by grep. Same sandbox Playwright/Chromium limitation as every other item this session (`net::ERR_CONNECTION_RESET` on all HTTPS, `curl` unaffected) - no in-browser visual/touch check possible; re-verify next session once that's healthy (notched-device emulation for the safe-area padding, actual tap-and-see-feedback for the affordance hint).

- **Cross-module topic-filter leak + 2 missing TOPIC_LABELS entries (2026-08-08)** — PO asked to check all translations again and separately flagged seeing compliance-course categories show up while studying the driver's-licence module. Investigated and found a real bug, not a translation problem: `topicFilter`/`roleFilter` were saved under a single `profileKey("filter")`/`profileKey("role-filter")` key shared across every module for a profile, and restored in `loadActiveProfileState()` (used on app load/profile switch) BEFORE the saved module was even known. `selectModuleAndScope()` (used for an in-session module switch via the picker) correctly resets both to "all" in memory, but doesn't persist that reset unless the user actually clicks a filter chip afterward - so closing the app right after switching modules left the OLD module's last-clicked filter code sitting in the shared key, which then got restored as the NEW module's filter on the next app load. Confirmed with a standalone Node simulation reproducing the exact save/restore sequence: before the fix, a Datenschutz topic code ("grundprinzipien") leaked into a fresh Fuehrerschein load; after the fix, it correctly resolves to "all", with Datenschutz's own remembered filter still separately intact. Fixed by scoping both storage keys per module and reordering the restore logic so the saved module is known first. While auditing, also ran a corrected *structural* translation check (not just "does the locale key exist somewhere" - checks every table's actual 12-locale completeness) and found two real, pre-existing `TOPIC_LABELS` gaps unrelated to any earlier fix this session: **motorrad and lkw had no entry in `TOPIC_LABELS` at all** (not just missing locales) - since `renderFilters()` builds its topic-filter chip row from `Object.keys(TOPIC_LABELS[examType] || {})`, this silently suppressed the entire topic-filter UI for both modules and made every question's topic badge fall back to raw German text regardless of UI language; a stale comment blamed this on "a single topic," which is false (motorrad has 5 distinct topics, lkw has 4) - added all 9 topic labels across all 12 locales. Also found **fuehrerschein's own `anhaenger_be` topic (the Klasse BE trailer-operation content, 26 real questions) missing from `TOPIC_LABELS.fuehrerschein`** for the identical reason - added. Verified: `node --check`, a full `data/build_modules.py` + `assets/build_sign_reference.py` rebuild (zero gaps across all 9 modules' question content, the manifest labels, and the sign catalog), a corrected locale-completeness scanner (the first version of this scan had its own bug worth remembering - it false-flagged `ROLE_FILTER_STRINGS` because its `it` role-filter code collides with the `it` Italian locale code when recursed into blindly; fixed to check one level deep only, matching each table's actual shape), and the direct simulation proving the leak fix. Live-browser click-through still blocked by the same sandbox Chromium/proxy issue as the DN-28 round (re-confirmed: `curl` against the live site returns 200 and correct data, but Playwright gets `net::ERR_CONNECTION_RESET` on every attempt) - re-verify visually next session: switch between a compliance module and Fuehrerschein with a filter selected, reload the app, confirm no cross-contamination; check that Motorrad/LKW now show a topic-filter row at all.

- **DN-28 revisited — sign/diagram alt-text and Sign Reference catalog now in all 12 locales (2026-08-08)** — PO asked whether all language packs were current, correctly suspecting the Fuehrerschein sign descriptions weren't. They were right, but not from an oversight: DN-28 was an earlier deliberate PO call that sign/diagram alt-text was "fixed exam material, not learning-material text" and out of scope for translation. Asked whether to revisit it now (per standing scope-check discipline) - PO said yes, so: (1) `assets/build_sign_reference.py` (builds `app/data/fuehrerschein/sign_reference.json`, the "📚 Schilder"/Sign Reference catalog view) was hardcoded to only read the de/en locale files - generalized to loop over all 12, which was essentially free since the underlying question text/options/explanations it pulls from were already fully translated for unrelated reasons; regenerated the catalog with zero locale gaps. Also found and fixed a real bug while there: `renderSignReferenceView()` in `app.js` had `const contentLang = state.lang === "de" ? "de" : "en"` hardcoded, which would have silently discarded the newly-available 10 locales even after the data existed - now reads `state.lang` directly. `SIGN_REF_STRINGS` (view chrome) and `SIGN_CATEGORY_LABELS` (category headings) translated directly (10 short strings each). (2) `SIGN_ALT` (35 sign-image descriptions) and `DIAGRAM_ALT` (35 hazard-diagram ids × plain/answer, ~70 strings) - the genuinely new hand-authored alt text with no existing translated source to borrow from - translated into the other 10 locales via 3 parallel subagents split by language group, merged into `app.js` with a Python splice script (not hand-typed, to avoid transcription errors across ~700 short strings), verified with a small Node script that evals just those two const declarations and confirms all 12 locale keys exist on every entry (zero gaps). (3) Also closed a separate, unrelated DE/EN-only gap noticed while in this area: `MODULE_INTRO_STRINGS` (module-intro-wizard chrome, 6 keys) - never actually part of DN-28's scope, just a similarly-worded gap nobody had opened a card for; translated directly since it was cheap. Verified: `node --check` on `app.js`, `python3 -m json.tool` on the regenerated catalog JSON, a full `data/build_modules.py` rebuild (all 9 modules' own sanity checks still pass, zero locale gaps), and `curl` against the live deploy confirming `sign_reference.json` now serves all 12 locales per entry with correct-looking translated text. **Same live-browser verification gap as the DN-50 round just before this**: the sandbox's outbound Chromium/proxy path stayed down for this entire session (`net::ERR_CONNECTION_RESET` on every HTTPS site tried, re-confirmed again after this deploy) - no in-browser click-through of the Sign Reference view or a non-DE/EN UI language was possible. Re-verify visually next session once that's healthy: open Sign Reference under a language like `uk`/`ar`/`zh` and confirm real translated sign names/descriptions render (not an EN/DE fallback), and reveal a hazard-diagram question's alt text via a screen reader or the image `alt` attribute in a non-DE/EN language.

- **DN-50 (Hinweisgeberschutz pilot) — 5th compliance module, HinSchG (2026-08-08)** — Of the two new-module candidates flagged in the earlier compliance-pricing research (Hinweisgeberschutzgesetz + LkSG), built only Hinweisgeberschutz this round after a fresh legal-status check (web research, Aug 2026) found the two in very different states: HinSchG (whistleblower protection, in force since 2 July 2023) is legally stable and unchanged since mid-2025, while LkSG (supply-chain due diligence) is mid-reform right now - BAFA has administratively suspended its §10 reporting duty through end of 2025, a Bundestag bill to formally narrow the reporting duty/sanctions was still only in committee as of the January 2026 first reading (not yet law), and the EU's CSDDD Omnibus reform means LkSG itself is headed for eventual replacement (German transposition due by July 2027). Locking quiz content to a law mid-amendment risked being wrong within months, so **LkSG is deliberately deferred**, not built - revisit once the Bundestag amendment (or a full CSDDD-transposition replacement) actually passes, not before. Shipped: a 20-question DE/EN-only pilot (`data/hinweisgeberschutz_pilot.json`) across 5 topics (geltungsbereich/scope, meldestellen/internal+external reporting-channel mechanics, vertraulichkeit/confidentiality, repressalienschutz/§36 retaliation protection, sanktionen/Bußgeld), each citing its specific HinSchG paragraph, following the exact schema/tone of the existing datenschutz/arbeitssicherheit/ki_act/it_sicherheit pilots. Wired the same way those originally launched: a `modules_manifest.json` entry (label translated to all 12 locales even though question content itself is DE/EN-only for now - matches the module-picker/content-locale-count split every other module already has), a new `split_module()` call in `data/build_modules.py`, and `TOPIC_LABELS`/`COMPLIANCE_MODULES` entries in `app.js`. Below the 30-question Exam Simulation floor by design (same as every module's original pilot) - `#exam-start-btn` is automatically disabled until it's scaled up, confirmed both by reading `drawExamQuestions()`/`el("#exam-start-btn").disabled = state.questions.length < EXAM_QUESTION_COUNT` in `app.js` and via `curl` against the live `/data/modules.json` and `/data/hinweisgeberschutz/core.json`+`locales/de.json` after deploy. **Verification gap, worth flagging**: a live Playwright smoke test (`scripts/test_hinweisgeberschutz_smoke.mjs`) was written but couldn't run this session - the sandbox's outbound Chromium/proxy path was hard-down for the whole session (`net::ERR_CONNECTION_RESET` on literally every HTTPS site tried, including `example.com`, ruling out anything specific to this deploy), while plain `curl` against the same URLs worked the whole time. Verification for this round therefore rests on: `build_modules.py`'s own sanity checks (DE-locale completeness, scope-field presence) passing cleanly, manual JSON schema/content spot-checks, and `curl`-confirmed live deploy content (module list includes `hinweisgeberschutz`, correct label, correct 20-question core.json/de.json) - NOT an in-browser click-through. Re-run `node scripts/test_hinweisgeberschutz_smoke.mjs` next session once Chromium networking is healthy again, before treating this as fully e2e-verified. Remaining, not yet done: scale to 40 questions/all 12 locales (same pilot→scale pattern DN-48 already did for the other 4 compliance modules), legal review (folds into DN-12's existing gate), and the deferred LkSG module once its legal status settles.

- **DN-51 (smallest viable step) — standalone signed-JWT credential download (2026-08-07)** — Scoped in `docs/badge-wallet-portability-scoping.md` (researched against real wallet products - Credly, Canvas Credentials, 1EdTech Open Badges Passport, Learner Credential Wallet - rather than assumed), then built the recommended first step: a "Download signed credential (JWT, for wallets)" button next to the existing certificate/JSON downloads in both "My certificates" and the exam-results screen, gated to genuinely-signed (`verified && signedJwt`) records, translated across all 12 locales. This closes a real, previously-undiscovered gap: the existing JSON credential download nests the signed JWT inside a custom `proof.jwt` field, which is not what real badge wallets expect for file-upload import - they want the raw compact JWS itself, which is exactly `record.signedJwt` and is what this new button now offers standalone. Deliberately did NOT build email capture/delivery to "send" the badge (per the scoping doc's recommendation - the researched market norm of requiring an earner email is a GDPR exposure Zettacard's current zero-PII posture is better off not matching) and did NOT fix the separately-flagged `achievement.id`/proof-shape OB3-conformance gaps in `credentialJsonDoc()` itself (left as known future work, out of scope for this smallest-viable step). Verified via `scripts/test_full_exam_badge.mjs`, extended to click the new button after a real signed exam pass and byte-compare the downloaded file against the record's own `signedJwt` - passed against production (one run hit the already-known sandbox JWKS-timeout flakiness, the retry passed cleanly).

- **DN-51 (remaining step) — fixed `achievement.id`/proof-shape OB3-conformance gaps (2026-08-08)** — Closed the two concrete gaps left open by the 2026-08-07 round, in both places that build the credential shape: `buildCredentialClaims()` in `netlify/functions/sign-credential.js` (the real signed artifact, higher priority since it's what an external validator actually checks) and `credentialJsonDoc()` in `app/app.js` (the client-side unsigned/preview JSON download). (1) `achievement` now carries an `id` - a stable per-module+scope URI (`{origin}/achievements/{examType}-{scopeCode}`), correctly identifying the achievement *definition* shared by every earner rather than one earner's instance of it (instance-level identity was already handled correctly via the JWT's own `jti` claim, per VC-JWT's standard claim mapping - so no redundant top-level `vc.id` was added server-side). (2) Removed `credentialJsonDoc()`'s custom `proof: {type: "JsonWebSignature", jwt: ...}` field entirely - it was never a valid OB3 JOSE/Data-Integrity proof shape, and more fundamentally OB3's actual JWT-secured form doesn't use a `proof` field at all (the compact JWS *is* the credential - already shipped correctly via the 2026-08-07 JWT-download button). Replaced with plain `signedJwtNote`/`signedJwtAlg`/`signedJwtKid` fields that describe the signature honestly as reference metadata rather than claiming a proof mechanism the JSON document doesn't have. Verified: `node --check` on both changed files, a standalone Node repro of `buildCredentialClaims()` confirming `achievement.id` parses as a valid URL, and a live `curl` against the deployed `sign-credential` function decoding a real signed JWT's payload to confirm `achievement.id` is actually present in production output. **Not yet done at the time**: re-running the two external OB3 validators (CertLister, 1EdTech's) against a freshly-signed credential to confirm the gaps are actually closed from an outside perspective, and re-running `scripts/test_full_exam_badge.mjs` - both blocked by the same sandbox Playwright/Chromium networking outage as everything else this session (`net::ERR_CONNECTION_RESET` on all HTTPS). **Update 2026-08-11**: done - see the "DN-51 (re-verification)" entry above. Result: the `achievement.id` fix itself holds up, but both external validators found the JWT still isn't independently verifiable by a generic third party (no `jku`/DID identity carried in the JWT) and 1EdTech's validator additionally wants a top-level `vc.id`, not just `achievement.id` - so DN-51 is not fully closed after all; concrete follow-up fixes are listed in `docs/open-badges-signing-setup.md`'s new section.

- **DN-50 (remaining step) — Hinweisgeberschutz scaled to 40 questions/12 locales (2026-08-08)** — Closed the scale-up flagged as remaining scope in the 2026-08-08 pilot round. Added 20 new original questions (4 per existing topic: geltungsbereich, meldestellen, vertraulichkeit, repressalienschutz, sanktionen - 40 total now), each with a legal_basis citation WebSearch-verified against the real HinSchG statute text before use (not recalled from memory), covering scope thresholds for regulated financial-sector entities, internal/external channel deadlines and follow-up duties (§§14/16/17), confidentiality obligations even when a channel turns out not to be competent (§8), the reversed burden of proof for retaliation claims and how it can actually be rebutted (§36), whistleblower protection in legal proceedings (§§37-38), and the administrative-fine regime (§40). Then translated all 40 questions (question + 4 options + explanation each) into the other 10 locales via 3 parallel subagents split by language group (uk/pl/ru, ar/zh/hi, tr/fr/es/it), DE as canonical source, legal citations preserved verbatim rather than translated. `data/build_modules.py`'s `hgs_locales` special-case was removed - the module now shares `compliance_locales` (all 12) with the other 4 compliance modules, same as DN-48 did for them. This clears the 30-question Exam Simulation floor - exam mode is now enabled for Hinweisgeberschutz for the first time. Verified: a structural completeness check (all 40 questions × all 12 locales × question/4 options/explanation all present, zero gaps - not just "does build_modules.py's own sanity check pass"), `data/build_modules.py`'s own rebuild (zero locale gaps across all 9 modules), `assets/build_sign_reference.py` re-run immediately after (this project's recurring gotcha - the rebuild wipes `app/data/` and this file isn't one of `build_modules.py`'s own outputs), `node --check app/app.js`, and a spot-check of translated content (Ukrainian, confirmed natural phrasing and the §8/§36 citations preserved correctly). **Same live-browser verification gap as every other item this session**: no in-browser click-through was possible (sandbox Playwright/Chromium networking down all session) - re-run `scripts/test_hinweisgeberschutz_smoke.mjs` next session once that's healthy, and specifically confirm Exam Simulation is now selectable (not just that the button is enabled in the data). Remaining, not yet done: legal review (folds into DN-12), the deferred LkSG module once its legal footing stabilizes.

- **DN-49 Phase 1 — permanent verification links for compliance certificates, 0€ MVP (2026-08-07)** — Following the PO's "ja genau" go-ahead to build the free-tier permanent-link feature scoped in `docs/paid-verifiable-certificates-scoping.md`: a passed, cryptographically-signed compliance-module (Datenschutz/Arbeitssicherheit/KI-Act/IT-Sicherheit only) completion can now request a permanent, publicly shareable `/verify/<slug>` link - the actual deliverable a DGUV auditor or new employer can just open, not a JSON file only a technical person could check. Two new Netlify Functions: `save-verified-credential-v2.mjs` (validates the request is for an in-scope module, independently re-verifies the JWT signature server-side before ever persisting anything - never trusts the client's "this is signed" claim, exactly like `sign-credential.js` never trusts client-computed exam results - then stores the record in Netlify Blobs under a random UUID slug) and `verify-credential-v2.mjs` (a public, unauthenticated GET endpoint rendering a plain HTML page for a slug, re-checking the signature live against the current JWKS on every view, not just trusting what was true when the record was saved). UI: "My certificates" and the exam-results screen gained a verify-link row (only shown for verified compliance-module records) with an optional participant-name field, a create button, and the resulting link with copy-to-clipboard - wired through all 12 locales (9 new `CERT_STRINGS` keys × 12 languages). **Real debugging saga worth remembering for next time a Netlify Function needs Blobs**: the first working version used the classic `exports.handler` function style (matching `sign-credential.js`'s existing pattern) and failed in production three different ways in sequence, each only found by deploying and testing live, not by code review: (1) `getStore(name)` alone threw `MissingBlobsEnvironmentError` - classic/legacy (`exports.handler`) functions do NOT get Netlify's automatic Blobs siteID/token context injection the way v2 (`export default`) functions do; (2) passing `SITE_ID`/`NETLIFY_FUNCTIONS_TOKEN` (both auto-present in every function's env) explicitly to `getStore()` got further but Blobs itself returned a 401 - that token isn't scoped for the Blobs API; (3) switching both functions to the v2 `export default`/Web Request-Response style fixed Blobs outright, but then a *stale deploy-cache* issue appeared - re-deploys kept invoking the OLD `exports.handler` code under the same function name even after the source was completely rewritten and the old file deleted (confirmed by curling the deploy's own unique subdomain directly, not just the site alias) - fixed only by renaming the functions (`save-verified-credential-v2`/`verify-credential-v2`) to break whatever kept the old name pinned to a stale bundle. Separately, the `netlify.toml` `[[redirects]]` block for `/verify/*` was silently never applied in production (confirmed the exact same way - a fresh, never-before-cached slug still 200'd into the marketing landing page, even with `force = true` added) while the pre-existing catch-all rule in the same file kept working fine; moving both rules into a plain `app/_redirects` file (removing them from `netlify.toml` entirely, since `netlify.toml` redirects take precedence over `_redirects` when both exist and would otherwise mask it) fixed this too. **Lesson**: this deploy pipeline has a real, repeatable stale-artifact bug that survives normal content changes - when a function or redirect keeps behaving like an older version despite a confirmed-correct source and a successful-looking deploy, don't keep tweaking the same file/function name and redeploying; rename the function or move the config to a different mechanism (`_redirects` vs. `netlify.toml`) to break the cache pinning, and confirm behavior on the deploy's own unique subdomain (not just the site alias) to rule out edge caching as a separate confound. Verified end-to-end for real, not from self-report: signed a fresh completion via `sign-credential.js`, called `save-verified-credential-v2` for a real `verifyUrl`, fetched that URL and confirmed it rendered "✅ Signatur gültig" with the correct name/date/stats/issuer/key-ID and the honest trust-boundary disclaimer; confirmed an unknown slug correctly 404s and the SPA catch-all/`/app.html` still work after the redirects move; re-ran `scripts/test_full_exam_badge.mjs` against production twice (one run hit the already-known sandbox JWKS-timeout flakiness, the retry passed cleanly) to confirm the existing signing/badge feature had zero regressions from this round's changes.

- **DN-27 (pilot round: hazard-topic diagrams) + DN-14 (seen/star tracking) + "Frage schieben" exam skip (2026-08-07)** — Three cards in one round, scoped via AskUserQuestion first per standing PO instruction (pilot size for DN-27, feature depth for DN-14, exact skip semantics), then built by two parallel subagents on genuinely file-disjoint work (one only touched `assets/diagrams/`+`app/assets/diagrams/`+a new sibling script, the other only touched `app.js`/`app.html`/`styles.css` — kept off the same shared file per AGENTS.md's parallel-vs-sequential rule) and merged/verified independently afterward. (1) **DN-27 pilot**: 28 of the Fuehrerschein "gefahr" topic's 40 questions (all previously imageless) now have original schematic diagrams via new `assets/generate_hazard_diagrams.py` — braking-distance lane comparisons, aquaplaning cross-sections, black-ice hotspots, blind-spot wedges, and more; 12 were deliberately left unillustrated as purely definitional with nothing spatial to draw. `DIAGRAM_IDS`/`DIAGRAM_ALT` extended in `app.js` (de/en alt text only, matching the existing sign-alt-text precedent). (2) **DN-14**: lightweight per-question "seen" tracking plus a manual star/bookmark toggle in the detail view, a "⭐ Starred only" filter chip, and a star badge on list rows — all new `STAR_STRINGS` populated across all 12 locales. (3) **"Frage schieben"**: Simulation-mode exam questions can now be explicitly skipped; skipped ones are re-presented as a required second pass (skip unavailable on that pass) before the exam can finish and grade — Training mode unaffected. Independently verified (not just trusting the subagents' reports): merged the diagram agent's `DIAGRAM_IDS`/`DIAGRAM_ALT` snippet in myself since it also needed an `app.js` edit (the one file both agents' outputs touched), ran `node --check`, a locale-completeness script confirming all 12 locales for every new string, and live Playwright passes screenhotting an actual gefahr-01 diagram, the star-filter narrowing the list to exactly the starred question, and a full exam run that correctly entered and exited the skipped-questions review pass. All live and deployed.

- **Real signed-badge credentials confirmed end-to-end + visible badge display + automated regression test (2026-08-07)** — The JWT-signing feature (`netlify/functions/sign-credential.js` + `app/.well-known/jwks.json`, built in an earlier round) had never actually worked in production: every attempt this session to set the private signing key (`ZETTACARD_SIGNING_PRIVATE_JWK`) via the Netlify MCP connector's `manage-env-vars` reported success but the variable never landed in `getAllEnvVars` or reached the live function, across 5+ attempts with different scope/context combinations and even a full key rotation. Root cause, found only once the PO set it manually: those calls had all been passing `envVarIsSecret: true`, and secret-marked vars apparently don't reliably propagate through this connector in this environment - a non-secret write (done directly by the PO via the Netlify dashboard) worked immediately. Lesson for next time: if `manage-env-vars` reports success but a subsequent `getAllEnvVars` doesn't show the key, don't keep retrying with broader scopes - suspect the `envVarIsSecret` flag itself and ask the PO to set it via the dashboard (non-secret is fine for this use case; the value is a key that never leaves this one function's env anyway). Once live: (1) verified with a raw curl AND independently re-verified the signature with `jose`'s `jwtVerify`/`createRemoteJWKSet` against the real published JWKS - not just trusting the function's own `verified:true` claim; (2) built `scripts/test_full_exam_badge.mjs`, a real Playwright end-to-end test that loads the actual published answer key from `app/data/<exam>/{core.json,locales/de.json}`, drives a full 30-question Arbeitssicherheit Exam Simulation in a real browser answering every question correctly, and independently re-verifies the resulting completion record's JWT signature against the live JWKS - confirmed passing against production; (3) added a visible badge display (`renderBadgeRow()` in `app.js`, new `.cert-badge-*` styles) to both "My certificates" and the exam-results screen shown right after passing, so a real signed credential now visibly reads "✅ Signed badge" (gold emblem) versus a self-issued/unverified fallback's plain grey "Unverified" pill - not just a silent internal flag. Also caught and fixed a pre-existing locale gap while touching this code: `CERT_STRINGS` had only ever had `de`/`en` populated (a real violation of the project's own all-12-locales rule that had gone unnoticed since the certificates feature was first built) - backfilled all 10 missing locales for every existing key plus the 4 new badge-display keys.

- **AGENTS.md refresh + per-module "make available offline" feature (2026-08-06)** — Two asks in one round: (1) PO asked to optimize `AGENTS.md`, which hadn't been updated since early in the project despite substantial growth. A research subagent read the actual current repo tree and produced an updated version (reviewed and spot-checked against the real `netlify.toml`/`assets/*.py`/`scripts/` before accepting): repo layout now covers `app/`, `assets/generate_signs.py` (flagged as a single shared file needing sequential not parallel edits), `netlify/functions/`, `scripts/`, `netlify.toml`, `package.json`; "what this project is" now lists all 8 modules instead of the original 2; the multilingual constraint now explicitly names all 12 locale codes and requires a locale-coverage check as part of verification (grounded in this project's own repeated real locale-gap incidents); added a new "don't trust self-reported looks-right" discipline section and a "parallel vs. sequential work" rule of thumb, both citing this project's own BACKLOG history. (2) PO asked for an explicit per-module "prepare for offline" feature with visible status, plus asked to use subagents and confirm scope before large work - clarified scope via AskUserQuestion first (per-module button vs. central multi-select page; visible status vs. fire-and-forget) - chose per-module button + visible status. A subagent implemented it: new `#offline-prep-btn`/`#offline-prep-status` next to the existing Sign Reference header button, `offlineAssetUrls()` (core.json + resolved locale file + every unique sign SVG referenced by the loaded module's questions), a read-only `checkOfflineReadiness()` (via `caches.match()`, no cache name needed) run on every module/language load so a returning visitor sees "already ready" without re-fetching, and `prepareOffline()` (parallel `fetch()` with live progress, `Promise.allSettled` so one failure doesn't abort the rest) - deliberately requires NO change to `service-worker.js`, since its existing fetch handler already runtime-caches any successful non-shell fetch. New `UI_STRINGS` keys added to all 12 locales. **Caught two real problems the subagent's own report claimed didn't exist, via independent verification** (directly applying the AGENTS.md discipline just written): (a) the subagent's own diagram-image gap it flagged as "known, out of scope" was actually simple to close, so fixed directly instead of leaving it - diagram questions (the 7 `vorfahrt-*` ids with birds-eye scenario art) render two image variants (plain + revealed-answer) neither of which used `image_ref`, so `offlineAssetUrls()` was missing them entirely; added both variants for any `DIAGRAM_IDS` question. (b) the subagent's verification claimed "all 12 locale blocks spot-checked, all present, no locale missed" - independently re-checking with a small Node script found French and Russian were BOTH actually missing the new keys, because the new key/value pairs had been pasted as accidental duplicate keys inside the `en` and `uk` object literals instead (silently swallowed by JS's last-key-wins behavior, invisible in a casual read, real content sitting in the wrong locale block) - fixed by moving the (already-correctly-translated) French/Russian text into their real locale blocks. Independently verified end-to-end via Playwright, including a real network-cut test (not just Playwright's `context.set_offline()`, which the subagent found doesn't actually block localhost traffic in this sandbox - killed the actual HTTP server process instead): prepared Fuehrerschein/English offline, killed the server, reloaded with the connection genuinely dead - all 505 questions loaded, both diagram-question image variants loaded (`naturalWidth` 150, no broken image), status correctly showed "already ready" without re-fetching; switching to Motorcycle (never prepared) while still offline correctly failed to load rather than silently serving stale/wrong content, confirming the existing "only what was opened is cached" baseline wasn't broken by this feature.
- **Fixed 18 sign-recognition questions missing their sign image (2026-08-06)** — User sent a screenshot from a live exam session (a Spanish-language question literally describing "a triangular warning sign with a wavy line on the road surface (sign 112)") asking whether the sign image was missing. It was - a real content bug, not a rendering issue. Investigation found the pattern is systemic: 24 questions across `data/pilot_questions.json` cite a specific StVO sign in `legal_basis` while a SIBLING question about the exact same sign already has the correct `image_ref` set (e.g. `zeichen-39` describing Zeichen 112 in text had `image_ref: null`, while `zeichen-55` - a different question about the same sign - correctly pointed at `signs/112`). Checked each of the 24 individually rather than blindly patching all of them: 18 explicitly describe the sign's visual appearance in the question text itself ("Ein dreieckiges Warnschild mit...", "Ein rundes weißes Schild mit rotem Rand zeigt...", "Ein blaues rechteckiges Schild mit...") - these are unambiguous bugs, fixed by copying the sibling's `image_ref`. The other 6 were deliberately left alone after reading their text: `vorfahrt-26` is specifically about the ABSENCE of a sign at a junction (showing an image would contradict the question), `autobahn-31` describes an electronic variable-message gantry display (conceptually different from the static Zeichen 274 pictogram), and `vorfahrt-28`/`autobahn-20`/`mehrfach-04` cite a sign only as a legal basis for a scenario/rule question rather than describing its appearance - not clear-cut bugs, so not touched without a case-by-case call. Fixed ids: zeichen-26/27/28/30/31/32/34/35/36/37/38/39/40/41/43/44/45/48. Verified all 18 new `image_ref` values resolve to existing files in `assets/signs/` (139 total image_refs checked catalog-wide, zero missing), re-ran `build_modules.py` (531/96/90/90/40/40/40/40 across all 8 modules, zero locale gaps) and `assets/build_sign_reference.py`, and confirmed live via Playwright that the exact reported question (`zeichen-39`, sign 112) now renders its sign image in the flashcard detail view.
- **Landing page full 12-locale i18n + 5 modules' topic-filter label translations (2026-08-06)** — User asked "what about translating the landing page, and what translations are we missing overall" - a real audit (not just a guess) found: (1) question/option/explanation content is already 100% complete - all 8 modules × 12 locales × every question, zero empty fields, verified programmatically. (2) `UI_STRINGS` (app chrome - buttons, labels) already 100% complete across all 12 locales. (3) The marketing landing page (`app/index.html`) was DE/EN-only by an earlier deliberate design choice (documented in its own code comments), which read as a real gap once the rest of the app had full 12-locale parity - user chose to extend it. (4) `TOPIC_LABELS` (the per-module topic-filter dropdown) had full 12-locale coverage for Fuehrerschein only; the other 5 modules with a topic filter (Angelschein, Datenschutz, Arbeitssicherheit, KI-Verordnung, IT-Sicherheit) were DE/EN-only, silently falling back to English for the other 10 UI languages (a graceful, non-breaking fallback already in `getTopicLabel()`, but still a real translation gap) - user chose to fix this too. Both fixed: `app/index.html` rewritten from a hardcoded DE/EN button-toggle to the same `🌐` + native-endonym `<select>` pattern as `app.html`'s own language pickers, with a new self-contained `LANDING_STRINGS` object covering all 12 locales (hero copy, CTA, feature bullets, footer) - module names/subtitles are copied verbatim from the already-translated, already-verified `app/data/modules.json` rather than re-translated from scratch, so this page can't drift from the app's own wording for those terms. Added `dir="rtl"` handling for Arabic, matching the app's existing RTL convention. `app/app.js`'s `TOPIC_LABELS` extended with all 10 missing locales for the 5 non-Fuehrerschein modules (24 topic labels × 10 languages = 240 new translated strings, GDPR/occupational-safety/AI-Act/IT-security terminology). Verified: `node --check app/app.js` clean, a script confirmed zero remaining `UI_STRINGS`/`TOPIC_LABELS` locale gaps, Playwright screenshots of the landing page in German/Arabic/Chinese/French confirmed correct rendering (including RTL layout for Arabic), and a live click-through confirmed the Datenschutz module's topic-filter buttons now render in Turkish instead of falling back to English. Also flagged, but explicitly NOT changed this round (out of scope): the known 8-question DE-desync-with-10-other-locales gap and the "presence ≠ verified accuracy" concern, both already documented in `docs/fuehrerschein-translation-completeness-plan.md`.
- **Full 5-category sign-icon audit and fix, incl. user-flagged 301/306 (2026-08-06)** — User's own impression ("30% sind ok, manche wirklich solala") prompted a full audit of all 121 unique StVO sign icons against the real ADAC "Verkehrszeichen in Deutschland" reference brochure, category by category (5 dedicated read-only audit agents: Gefahrzeichen 21, Verbotszeichen 25, Gebotszeichen 11, Richtzeichen 42, Sonstige 22), each rating every sign OK/MINOR/MAJOR with a specific finding. Result: 35 OK (29%) / 38 MINOR (31%) / 48 MAJOR (40%) — the user's estimate was, if anything, slightly optimistic. User specifically flagged "301 und 306 checken, 301 sieht ganz anders aus" mid-audit; confirmed both signs were badly wrong (301 drawn as a plain filled yellow diamond with an invented crossroads pictogram; 306 byte-for-byte IDENTICAL to 301's SVG file, when they are legally distinct signs — 301 = single-junction priority, 306 = priority road continuing over multiple junctions). Given the choice between fixing only the 48 MAJOR signs or all 86 flagged (MAJOR+MINOR), user chose **"Alles auf einmal"** — all 86 across all 5 categories in one pass. Fixed sequentially (one fix-agent per category, since all drawing logic lives in the single shared `assets/generate_signs.py` file — parallel agents would have clobbered each other's edits), with every fix independently re-rendered via cairosvg and visually inspected by me before moving on, not trusted from agent self-report alone — this caught real remaining defects on 3 separate occasions (a motorcycle/moped/horse-rider/composite cluster in Verbotszeichen needed 2 extra fix rounds, the last one done directly rather than via a 3rd agent dispatch, because the same helper — `sym_horse_rider()` — kept reading as a llama/table rather than a horse; a Richtzeichen-round text-overflow bug on sign 430's "Autobahn" label and a broken fuel-pump/bed pictogram on 448.1 needed direct follow-up fixes after the fix-agent's own render checks missed them; a Sonstige-round text-overflow on 1020-30 "Anlieger frei" and an unclear wheelchair pictogram on 1020-11 needed the same direct follow-up). Also fixed, as part of the Sonstige round, a file-hygiene bug the audit surfaced: `1020-32.svg`/`1022-10.svg` existed only in the deployed `app/assets/signs/` copy, missing from the canonical `assets/signs/` generator-output directory (would have been silently lost on any future regeneration) — added proper registry entries so both are now generated correctly into both directories; confirmed via `diff` that the two directories' file listings are now identical (no orphans). 301/306/307 are now visually distinct and correct (301: plain white-diamond-frame + yellow-diamond-fill, no pictogram; 306: same plus a white/black-bordered diamond inset; 307: 306 + diagonal cancellation line). Total: ~30 sign-drawing functions/helpers touched across `assets/generate_signs.py`; all changes verified via cairosvg rendering grids (not just code review) before commit.
- **Zettacard domain rename, logo, landing page, flashcard self-answer, and translation-QA planning (2026-08-06)** — Following the PO's observation that the app now covers far more than driving-theory practice: (1) Netlify project renamed `drivenow-fahrschule` → `zettacard`, so the live site is now `zettacard.netlify.app` (a free `.netlify.app` rename via `update-project-name`, no domain purchase) - the old `drivenow-fahrschule.netlify.app` URL no longer resolves, JWT issuer fallback and doc references updated to match. (2) New original SVG logo (`app/icons/logo.svg` - a flashcard-with-dog-eared-corner-and-checkmark motif using the app's own color tokens), regenerated PNG icons, and a real favicon link added (previously none existed at all). (3) The quiz/exam app moved from `app/index.html` to `app/app.html` (functionally unchanged); `app/index.html` is now a proper DE/EN marketing landing page with an 8-module overview and a "Jetzt starten"/"Start now" CTA into the app; `manifest.json`'s `start_url` now points at `app.html` so an installed home-screen icon launches straight into the app, not the splash page. (4) **Flashcard "try it yourself"**: options in the study/detail view are now clickable before reveal (checkbox-toggle for multi_choice, single-pick otherwise, mirroring exam mode's interaction) - on reveal, a wrong self-pick gets a new red `.your-wrong-pick`/`✗ Deine Antwort` mark alongside the existing green correct mark, addressing user feedback that revealing an answer with no prior commitment felt disconnected from the review-mode "did you know it?" buttons. Review mode's know/don't-know buttons gained a caption ("Wie lief's mit dieser Karte?"/"How did that go?") plus a non-committal "suggested" highlight on whichever button matches the user's own self-answer attempt, if they made one. (5) Investigated a user-reported bug ("Zeichen 206/STOP sign not showing") - confirmed via direct data/asset checks and a live Playwright run that the image_ref, the SVG asset, and the resolution logic are all correct both locally and on the deployed site; concluded this was very likely a stale-cache artifact from testing mid-deploy-cycle earlier today, not a current code defect - the CACHE_NAME bump in this same round should have already resolved it structurally. (6) Per explicit "just plan this" instruction, wrote `docs/fuehrerschein-translation-completeness-plan.md` assessing translation completeness: confirmed all 531 Führerschein questions already have all 12 locales technically present (zero missing entries), but flagged that only DE/EN and the 50 vorfahrt/zeichen ids from an earlier round got real quality review - the other 10 locales across the remaining ~481 questions have never had a systematic accuracy pass, plus a known small desync (8 ids whose DE was reworded without syncing the other 10 locales). Proposes a phased QA plan (quick 8-id fix, then a prioritized high-stakes-question back-translation sweep, then a full-catalog pass) but implements none of it yet, as instructed.

- **DN-16 spaced repetition, signed JWT certificates, role-tagged/renewal-aware compliance content, and compliance-business scoping (2026-08-06)** — A 4-track push (4 sequential agents, each integrated and independently verified) exploring "compliance training as a real business," per PO direction: (1) **DN-16 spaced repetition**: a client-side Leitner-box study mode. Per-profile `profileKey("srs")` → `{[questionId]: {box: 0-4, dueAt}}`; box 0 = due now, 1 = 1 day, 2 = 3 days, 3 = 7 days, 4 = 21 days; wrong/"didn't know it" resets to box 0, correct promotes by one. Exam-mode completions auto-feed the schedule via `feedExamResultsIntoSrs()` in `finishExam()`; a new "📅 Review (N)" header button opens a due-only session reusing the existing flashcard `#detail-view` (via `state.reviewMode`) with new "I knew it"/"I didn't know it" self-assessment buttons replacing prev/next. Verified: wrong→box 0, correct→promoted+not-due, due-count badge accurate, review queue matches due list exactly, zero console errors. (2) **Signed JWT certificates**: implemented the "smallest viable step" already scoped in `docs/open-badges-signing-scoping.md` - a new Netlify Function (`netlify/functions/sign-credential.js`) signs completion records as ES256 JWTs against a static JWKS (`app/.well-known/jwks.json`), honestly documented trust-boundary limits (client-side grading can't be re-proven server-side, only the submitted-record's signature is real), validates payload shape/plausibility, and returns clean JSON errors. `app/app.js`'s `credentialJsonDoc()` now returns `verified:true`+`proof.jwt` when a signature was obtained, falling back gracefully to the original `unverified:true` shape if the function is unreachable (offline users, not-yet-deployed function) - never blocking certificate generation. Round-trip signature verification tested via `jose` (valid signs+verifies, tampered JWT correctly rejected, malformed payload rejected with 400, missing signing-key env var handled without a stack trace). **Requires a real deploy step before going live**: set the `ZETTACARD_SIGNING_PRIVATE_JWK` Netlify env var from a freshly generated keypair (`scripts/generate_signing_keypair.mjs`) and replace `app/.well-known/jwks.json` with that run's public JWKS - full steps in new `docs/open-badges-signing-setup.md`; the dev/test keypair used during this round must be treated as burned, not reused. (3) **Role-tagged, renewal-aware compliance content**: added a per-question `roles` tag (`all`/`all_staff`/`hr`/`it`/`management`, a real per-question judgment call across all 160 compliance questions, not a bulk default) plus per-module `renewal_months`/`renewal_basis`/`renewal_note` metadata to all 4 compliance modules' master files, WebSearch-verified against DGUV Vorschrift 1 §4 (Arbeitssicherheit: statutory annual), GDPR Art. 39/NIS2UmsVO (Datenschutz/IT-Sicherheit: recurring best-practice, not fixed-interval law), and EU AI Act Art. 4 (KI-Act: one-time-plus-role-triggered, no fixed interval). New additive role-filter row (`#role-filters`, hidden outside compliance modules) narrows the question list by role tag, persisted per-profile; a renewal-due badge now shows in the certificates view when a compliance completion is overdue or due within 30 days. Verified: 40 questions/module unchanged, zero locale gaps, role filter narrows correctly (e.g. Datenschutz 40→18 for "IT"), renewal metadata survives into `core.json`, seeded 13-month-old completion correctly showed "overdue." (4) **Compliance-business scoping**: two new research docs - `docs/business-team-features-scoping.md` (backend architecture options for real employer/team completion-tracking - Netlify Functions + managed Postgres recommended as the pragmatic v1 over a full custom backend or buying an off-the-shelf LMS, with GDPR data-controller obligations explicitly flagged as needing real legal review, same posture as the existing DN-12 item) and `docs/compliance-business-case.md` (market/competitor research - real, current competitors named, ~$3B German compliance e-learning market noted as already well-served by incumbents selling the tracking/reporting layer this app doesn't have yet, NIS2/AI-Act-Art.4 timing creates a real but narrow window, recommends a small legally-reviewed pilot on Arbeitssicherheit+KI-Act rather than a full pivot). Integration: re-ran `build_modules.py` + `assets/build_sign_reference.py` (531/96/90/90/40/40/40/40 across all 8 modules, zero locale gaps), `node --check` clean on `app/app.js` and the new Netlify function, confirmed no private key material anywhere in tracked files (only in a gitignored `.env.local`), confirmed no header-button id collisions between the new Review/role-filter UI and the existing profile/certificates buttons.
- **DN-4 — Multi-select ("multi_choice") question type (2026-08-06)** — Added support for questions with more than one correct answer, matching the real Führerschein exam's own format (a meaningful subset of official Fragenkatalog questions require selecting 2+ of 4 options, scored all-or-nothing - no partial credit for a partial or over-broad selection). Schema already had an array-shaped `correct` field (previously always length 1) so no migration was needed; added `"question_type": "multi_choice"` as a second valid value alongside the existing `"single_choice"`. Added 5 new original questions (`mehrfach-01..05` in `data/pilot_questions.json`, full 12-locale text/explanation) covering required vehicle equipment (§53a/§35h StVZO), pedestrian-crossing rules (§26 StVO), post-accident duties (§34 StVO), the Autobahn sign 330.1's bundled restrictions, and Rettungsgasse (emergency-corridor) formation rules (§11 Abs. 2 StVO) - each fact WebSearch-verified, with 2- and 3-correct examples for variety. `app/app.js`: flashcard/detail view needed no logic change (reveal already iterates `q.correct`, so multiple checkmarks just work); exam view's `pick()`/`applySelection()` now branch on `question_type` - single_choice keeps its original overwrite-one-key behavior unchanged, multi_choice toggles keys into/out of an array (`ex.answers[q.id]` becomes an array only for multi_choice questions); new `isExamAnswerCorrect(q, given)` helper does exact-set comparison (order-independent, no partial credit, rejects both under- and over-selection) and is now the single source of truth used by both `computeExamResults()` and this verification; the wrong-answer review list now joins multiple option texts instead of assuming a single string. `app/styles.css`: new `.badge.multi-select` hint badge (shown in the flashcard list, detail view, and exam meta line so a learner knows to expect more than one right answer before reading the options) reusing the already contrast-verified `--correct-bg`/`--green` pairing, plus a `.option-checkbox` square-indicator style so multi-select options read as genuinely different from the single-select radio-style cards (not just a background-tint difference). Verified via Playwright: detail-view reveal shows all 3 correct marks for a 3-correct question with the new badge text rendering; exam-mode checkbox toggle-on/toggle-off tracked correctly in `ex.answers`; `isExamAnswerCorrect` returns true only for the exact correct set regardless of click order, and false for partial, over-broad, empty, and unanswered cases; zero console errors. Ran `build_modules.py` + `assets/build_sign_reference.py` to rebuild `app/data/**` with the 5 new questions (531 total, zero locale gaps).
- **DN-20b/DN-25 content-quality rewrite, DN-46 closeout (4 remaining sign fixes), local profile switcher, and Open Badges signing scoping (2026-08-06)** — A second file-disjoint parallel-agent batch (4 agents), integrated and independently verified before committing: (1) **Content quality**: rewrote DE/EN `text`/`explanation` for 42 of the 50 original pilot ids (`vorfahrt-01..25`, `zeichen-01..25`) in `data/pilot_questions.json` - more plausible distractors (DN-20b) and simplified DE wording for low-literacy readers per `docs/PERSONAS.md`'s documented patterns (DN-25); 8 ids were already adequate and left untouched. Verified: still exactly 526 questions, all 50 target ids have valid DE/EN content, no duplicate option text. Ids with meaningfully changed DE wording flagged for a future translation-sync pass across the other 10 locales: `vorfahrt-04, -05, -06, -09, -13, -17, -24, zeichen-06`. (2) **DN-46 closeout**: the last 4 lowest-confidence sign findings fixed against the same ADAC reference brochure used for DN-47 - 124/Stau (two car-shaped silhouettes with a rounded roofline, offset for perspective, replacing plain rectangles), 117/Seitenwind (a striped/segmented windsock pennant replacing a solid black triangle), 151/Bahnübergang (a sloped locomotive-nose silhouette with a window cutout replacing a generic box - confirmed via `image_ref` search that sign 151 is NOT shared with 156, so this redraw is isolated), 273/Mindestabstand (boxier truck silhouettes, "70m" label moved above the vehicles instead of below, centered dashed gap). All 4 rendered via cairosvg and visually confirmed legible. (3) **Local profile switcher**: multiple people (e.g. a family) can now share one device without progress/settings colliding. New header "👤 <name> ▾" button opens a dialog (same `.exam-modal`/`role="dialog"` pattern as `#certificates-view`/`#module-picker`) listing profiles, switching, and creating new ones. Per-profile: language, active module+scope, topic filter, module-intro-seen flags, completions/certificates. Shared across profiles: theme. `migrateOrInitProfiles()` runs first in `init()` and moves any existing single-user's flat `localStorage` keys (`dn-lang`, `dn-filter`, `dn-exam-type`, `dn-scope-code`, `dn-completions`, dynamic `dn-intro-seen-*`) into a new "Default" profile's namespace on first load after this update, so existing users don't lose their progress. Verified via Playwright: existing-user migration preserves state under "Default", a genuinely new profile gets fully isolated language/module/intro-seen state (correctly shows its own module-intro dialog, proving isolation rather than a bug), switching back restores the other profile's state correctly, theme stays shared, and dialog close/back-gesture behavior matches the existing modals - zero real console errors. (4) **Open Badges signing scoping**: new `docs/open-badges-signing-scoping.md` documents the current baseline (self-issued, explicitly `unverified: true` OB3/VC-shaped JSON from `credentialJsonDoc()`), four real signing architecture options (serverless-function-signed JWT, manual/batch signing, third-party issuing platform, DID-based `did:web` + JSON-LD Data Integrity), the verification story for each, and content-claim honesty constraints (e.g. Arbeitssicherheit/AI Act credentials can only claim what a self-paced quiz can actually attest to) - recommends starting with a Netlify Function signing plain JWTs against a static JWKS as the smallest viable real step. Integration: re-ran `build_modules.py` + `assets/build_sign_reference.py` (confirming the known rmtree-wipe/regenerate pattern), removed a stray `verify.py` scratch file an agent left in the repo root, confirmed the sign SVG registry has no new orphans beyond the two known pre-existing ones.
- **DN-48 — Full 12-locale coverage, compliance-module scale-up to unlock exam mode, "Zettacard" rebrand, and 7 sign-icon corrections against an official reference (2026-08-05)** — A large parallel-agent push (12 subagents via one orchestrated workflow, plus 1 more for the sign fixes) closing out several outstanding gaps at once: (1) **Locale coverage**: Motorrad, LKW, Angelschein, and all 4 DN-44 compliance modules were DE/EN-only pilots; translated into the same 10 additional languages Fuehrerschein already had (uk/pl/ar/zh/hi/tr/fr/ru/es/it), using DE as the canonical source cross-checked against EN - every module in the app now has full 12-locale parity, independently verified per-module (all counts × 12 locales, zero gaps) before integration. (2) **Compliance-module scale-up**: Datenschutz/Arbeitssicherheit/KI-Verordnung/IT-Sicherheit each grew from 20 to 40 questions (4 new per existing topic, 20 new per module), every new legal_basis citation verified via WebSearch against the real GDPR/BDSG, ArbSchG/DGUV/ASR, EU AI Act, and BSI-Grundschutz/ISO-27001/NIS2 text respectively (not invented from memory) - this clears the 30-question exam-mode threshold, so exam simulation is now enabled for all 8 modules, not just the original 4. (3) **Rebrand**: in-app branding changed from "drivenow" to "Zettacard" (title, header, manifest.json install name, certificate/credential issuer name and download filenames, README/AGENTS.md) while deliberately keeping the live Netlify URL (`drivenow-fahrschule.netlify.app`) and the GitHub repo name unchanged, and leaving genuinely historical references (a service-worker comment documenting a past cache-name value, a BACKLOG.md historical log entry) untouched rather than rewriting history; also broadened manifest.json's name/description and index.html's meta description, which still narrowly said "Klasse-B driving-theory" despite the app covering 8 modules across driving/fishing/compliance. (4) **7 more sign-icon corrections**, this time checked against the official ADAC "Verkehrszeichen in Deutschland" reference brochure (the real StVO Verkehrszeichenkatalog) rather than WebSearch text descriptions alone: 272 (U-turn arrow was mirrored - curve belongs at the top, not the bottom), 268 (replaced an unclear spoke-and-ring pattern with a tire + diamond chain-mesh overlay), 354 (replaced invented wavy water-lines with the actual printed "Wasser-Schutzgebiet" text label the real sign carries), 363 (Polizei text now sits in a white inset panel, not directly on blue), 242.2/244.2 (the "Ende" variant now greys out the whole inset circle/pictogram/text with one clean diagonal line, not a dashed hatch band across the entire plate), 356 (added the "Verkehrshelfer" text label that was missing entirely), 358 (red cross now sits on a white inset panel, matching the real sign, not directly on the blue background). Integration work (build_modules.py's per-module locale lists updated and re-run, `app/data/fuehrerschein/sign_reference.json` regenerated since the build step's directory wipe had cleared it, a stray leftover "drivenow" reference in ki_act's master content fixed) and end-to-end verification (Playwright across 7 module/language combinations confirming real translated text renders with zero console errors and exam mode is enabled everywhere, plus a rendered visual grid check of the 7 corrected sign icons) done directly, not delegated, before committing.
- **Stale service-worker cache fix + Sign Reference view + a second real contrast bug (2026-08-05)** — User reported "reselecting LKW works but I can't get back to the Datenschutz stuff" and, separately, sent a screenshot showing the module picker's Klasse B/BE option buttons rendering with near-unreadable dark text on a dark background, asking whether the earlier contrast fix hadn't actually shipped. Both turned out to be real, root-caused bugs, not user error: (1) `service-worker.js`'s `CACHE_NAME` had never changed since the project's very first commit - since browsers only re-run a service worker's install lifecycle when the SW script's bytes change, any visitor who opened the app before the DN-44 compliance-module rollout stayed permanently stuck on their first-visit cached `data/modules.json`/`app.js`, missing Datenschutz/Arbeitssicherheit/KI-Act/IT-Sicherheit forever with no way to unstick short of manually clearing site data - confirmed via a Playwright "seeded stale cache, reload without clearing storage" simulation. Fixed by bumping `CACHE_NAME` (unblocks everyone stuck right now) and switching the app-shell/manifest fetch handling from unconditional cache-first to network-first-with-cache-fallback (structural fix so a future deploy can't reintroduce this just by forgetting to bump the cache name by hand). (2) The screenshot exposed a genuinely different, more severe contrast bug than the one already fixed this round: `.exam-mode-btn` (used by the module-picker options and the exam-mode training/simulation picker) had NO explicit `color` at all, silently defaulting to the browser's native `<button>` text color instead of the theme's text color - missed by the earlier WCAG audit because that audit only checked declared CSS-custom-property color/background pairings, not the absence of a color declaration entirely. Fixed with an explicit `color: var(--text)` (11.6:1 dark / 12.3:1 light). Also built, in the same round: a new "Signs" header button (Fuehrerschein module only) opening a browsable Sign Reference view - all 121 unique StVO signs actually referenced by this module's questions, each with its real icon plus a name/description DERIVED from this project's own already-verified question content (correct-answer text + explanation, not new claims), grouped under Gefahrzeichen/Verbotszeichen/Gebotszeichen/Richtzeichen/Sonstige (new `app/data/fuehrerschein/sign_reference.json`, generated by `assets/build_sign_reference.py`). Verified via Playwright: full reselect flow (Führerschein → LKW → Datenschutz) loads real content with zero console errors; Sign Reference view renders all 121 icons, correctly hidden outside Fuehrerschein, closes via both the in-app back button and the browser back gesture.
- **UX/accessibility review + proactive sign-catalog audit follow-up (2026-08-05)** — User reported "dark blue on blue, hard to read" on the content-picker area; rather than eyeballing colors, wrote a WCAG luminance/contrast calculator and computed exact ratios for every `styles.css` custom-property pairing. Found and fixed the real bug: the header's Exam button (`.exam-start-btn`) was 1.71:1 (dark theme) / 3.69:1 (light theme) against WCAG AA's 4.5:1 requirement — swapped its color token for the already-verified `--badge-topic-text`. Also found and fixed a borderline light-theme `--amber` contrast issue (4.14:1 → 4.55:1+, darkened to `#8a5b09`, matching the existing `--green` darkening precedent) and a real functional bug: `#module-picker-cancel` had no `textContent` set anywhere in the codebase, rendering as a permanently blank pill (now localized across all 12 languages + conditionally hidden on the mandatory first-ever visit). Extended colorblind accessibility: exam-mode's "your selected answer" state previously relied on color alone — added a `.selected-mark` checkmark, following this codebase's own existing "never signal by color alone" principle. Added flag-emoji prefixes to both language `<select>` pickers (header + detail view) for all 12 languages, as a deliberate *partial* reversal of an earlier flags-only rejection — full endonym text stays the primary label. Then applied the 14 confirmed HIGH/MODERATE findings from the earlier proactive sign-catalog audit: 242.1/242.2/244.1/244.2 (Fussgängerzone/Fahrradstrasse — new white-plate+inset-circle+text design replacing flat blue squares, with a diagonal hatch on the "Ende" variants), 363 (Polizei — removed an invented star, now real "Polizei" text), 354 (Wasserschutzgebiet — tanker-truck-over-water pictogram replacing a generic water drop), 214 (fixed a wrong-meaning bug — was a left/right double arrow, now correctly geradeaus-und-rechts), 222 (distinct bypass-arrow shape, no longer visually identical to 209/211), 268 (Schneekettenpflicht — tyre + visible chain-link net, not two abstract circles), 278/282 (new `circle_end_restriction()` template — proper thin grey ring instead of a recolored thick prohibition ring), 325.2/331.2 (switched from the grey "end" line to the red diagonal line matching this project's own already-correct 330.2 convention), 272 (redrawn as a proper closed U-turn loop, previously read as "turn right"). All 14 verified via cairosvg rasterization grid review plus an HTTP check confirming every `image_ref` path actually used by `fuehrerschein/core.json` serves correctly from the deployed `app/assets/signs/` path.
- **Sign-icon fixes (12 user-reported design flaws)** — Fixed Zeichen 215 (Kreisverkehr - now 3 real counterclockwise crescent arrows, not one unclear arc), 293 vs 350 (these are two DIFFERENT real things - 293 is the painted zebra-stripe road marking, 350 is the blue pedestrian-crossing sign - but were mapped to the identical icon; 293 now shows real zebra stripes, and fixed 350's foot-stripe fill which was the same blue as its own background and invisible), 307 (cancellation bar is now a real black band with white pinstripes, diamond correctly stays yellow), 136 (Kinder - two running children now have a real height/size difference and kicked-leg poses, not near-identical stick figures), 120/121 (bold solid black wedge silhouettes, not thin wireframe lines), 201 (Andreaskreuz - real half-white/half-red arms with no backing plate; caught and fixed a real presentation bug in the first attempt where the white halves were invisible against this app's white page background), 260 (real sign bans motorcycles AND cars - added the missing motorcycle+rider pictogram, the existing question content already correctly described both vehicles), 237 (Radweg bicycle now has a distinct handlebar and saddle, not an ambiguous triangle), 240/241 (these were backwards - 240/shared-path needs a horizontal divider, 241/separated-path needs a vertical divider; both corrected). 102 (Kreuzung) was independently WebSearch-verified as already correct (the real sign has no arrow tips) - no code change, noted so it isn't "fixed" again by a future session. Also verified 220 (Einbahnstraße) needed its missing text label added. All fixes independently WebSearch-verified against the real StVO pictograms before redrawing, per this project's standing sign-fact discipline, and confirmed via cairosvg rasterization plus a live Playwright check (all 12 images resolve correctly in the detail view, zero console errors).

  **Also discovered while regenerating**: `app/assets/signs/` (the directory actually served to users) had drifted out of sync with `assets/signs/` (the generator's own output folder) for many files beyond this round's 12 - including an **earlier session's DN-32 zeichen-68/214 redraw, which was verified at the time but never actually copied to the deployed path**, meaning it was never really live despite being logged as done. Ran a full sync to close the gap, and fixed the root cause: `generate_signs.py` now writes to both directories in the same run, so this can't silently recur.
- **Locale-fallback crash fix (real bug, user-reported as "LKW for Russian" not loading)** — `loadModuleData()` was keying fallback question text under the RESOLVED locale (e.g. `en` when Russian wasn't available) instead of `state.lang` (the UI language actually selected), but every render/exam function reads `q.text[state.lang]` - so any module without full 12-locale coverage (Motorrad, LKW, Angelschein, and all 4 new DN-44 compliance modules) crashed/rendered blank under any of the 10 non-DE/EN UI languages, not just Russian. Fixed by keying under `state.lang`. Verified via Playwright across LKW/ru, Motorrad/fr, Angelschein/ar, Datenschutz/zh (list + detail view, zero errors) and Fuehrerschein/hi (confirmed real full-locale translations still render, unaffected).
- **DN-44 content (first pilot batch)** — Built the first real content for the workplace-compliance product direction: 4 fully separate modules (Datenschutz, Arbeitssicherheit, KI-Verordnung/AI Act, IT-Sicherheit), 20 original DE/EN questions each (80 total), wired through the existing DN-39 module architecture with a placeholder single-option `scopeKind: "class"` (`code: "ALL"`) since there's no class/region dimension yet - needed zero app.js scope-matching changes, only manifest entries, 4 new `build_modules.py` split calls, and `TOPIC_LABELS` entries. Content is grounded in verified public law/standards (GDPR Art. 5/6/9/28/32-34/44-46 + §53 BDSG, ArbSchG §§3-5/9/12/15-16 + DGUV Vorschrift 1/ASR A2.2-A2.3/ArbStättV, EU AI Act 2024/1689 Art. 1/3/4/5/6/14/26/50/113, BSI IT-Grundschutz Bausteine + ISO 27001 Annex A controls + NIS2 Art. 21/23) - every `legal_basis` citation was independently WebSearch-verified before writing, not sourced from any vendor training catalog. Notably, the KI-Verordnung agent caught and corrected a wrong date assumption in its own task brief (Article 4's AI-literacy obligation actually took effect Feb 2025 alongside Article 5's prohibited practices, not Aug 2026 with the rest of the regulation) - a real example of the verify-before-asserting discipline paying off. This is a first pilot batch (20q/module, deliberately below the 30-question exam-mode threshold, same pattern as Angelschein's original DN-39 seed before DN-11 scaled it up) - `legal_review_status: "NOT legally reviewed"` on all 4. Verified end-to-end via Playwright: all 8 modules list correctly in the picker, each new module's single-scope selection auto-collapses (same UX Fuehrerschein had before BE), flashcard view renders/reveals/translates correctly, exam button correctly disabled at 20<30, zero console errors, zero regressions to the 4 existing modules.
- **DN-14 / DN-44 infra prep** — Built completion tracking + a self-issued, Open Badges-shaped certificate/credential system, as general-purpose infrastructure ahead of any DN-44 compliance content, per PO's explicit build-order call ("infrastructure first"). Passing an Exam Simulation (not Training mode) records a completion on-device (`localStorage`, key `dn-completions`) and surfaces a "🎓 My certificates" panel (new header button) offering, per completion: a downloadable printable HTML certificate, and a downloadable JSON credential shaped as an Open Badges 3.0 / W3C Verifiable Credential (`type: ["VerifiableCredential", "OpenBadgeCredential"]`), explicitly marked `unverified: true` with a plain-language reason, since this zero-backend static PWA has no signing authority. **PO has since confirmed Open Badges 3.0 alignment as a real product goal, not just a stopgap shape** — see the new note under DN-44 below on what a future "real" (cryptographically verifiable) implementation would need. Verified end-to-end via Playwright: sim-mode pass records + shows a certificate, training-mode pass does not, both downloads fire correctly, and the certificates panel closes cleanly on phone back-gesture (matching the DN-13/DN-17 discipline). Committed, not yet redeployed to Netlify per PO's standing "don't deploy every small change" instruction.
- **DN-26** — Rewrote zeichen-42/47's weak throwaway distractors (unrelated sign categories like "Ortstafel"/"verkehrsberuhigter Bereich" that were trivially eliminable without knowing the actual fact) into plausible, related-concept distractors: zeichen-42 (Vorwegweiser) now distinguishes it from Wegweiser/Ausfahrttafel/Entfernungstafel; zeichen-47 (Zeichen 333, exit-distance marker) now distinguishes it from the plain exit marker, a rest-stop sign, and an exit-confirmation sign. Verified both render correctly in-app with the new option text.
- **Legal review prep (DN-12 support)** — Compiled and delivered a "Legal Review Preparation Packet" (docx) indexing every distinct legal/factual citation across all four modules by statute, the full list of 121 Fuehrerschein sign catalog numbers for spot-checking, the provisional-placeholder exam-mechanics caveats (Motorrad/LKW reusing Klasse-B thresholds), and a concrete checklist of what a reviewer should verify. This is NOT the legal review itself (DN-12 remains open and urgent) - it's prep work to make an actual review efficient once the PO arranges one.
- **DN-32** — Ran the DN-30 batch's first-ever independent Student Review (87 sign questions, zeichen-51 through zeichen-137 - every other content batch in this project has had one, this was the last gap). Found zero near-duplicates and zero DE/EN mismatches (the batch held up well there), but found and fixed: 5 weak "this sign has no legal meaning" throwaway distractors (zeichen-107/118/123/126), a batch-wide missing-terminal-punctuation bug across all 12 Zusatzzeichen questions (zeichen-126-137, a merge-time formatting-convention mismatch between the 4 parallel generation agents that the original id-collision-only merge never caught), a wrong `legal_basis` citation for the whole Zusatzzeichen block (was citing Anlage 4, which actually covers Verkehrseinrichtungen not Zusatzzeichen - corrected to §39 Abs. 3 StVO / VwV-StVO zu §§39-43), and a high_stakes/points miscalibration (zeichen-66, a rail-crossing warning, was tagged low-severity while every comparable hazard sign in the batch was tagged high_stakes - corrected; zeichen-97/98, a parking-zone administrative sign pair, was tagged high_stakes while every sibling Beginn/Ende sign pair in the batch wasn't - corrected down to match).

  A second, independently WebSearch-verified round on top of the Student Review's raised concerns found a genuinely fictional sign: **zeichen-68** was labeled as real Zeichen 214 but described a "pass this obstacle on either side, driver's choice" concept that doesn't correspond to ANY real German traffic sign (confirmed: Zeichen 214 actually means "vorgeschriebene Fahrtrichtung: geradeaus oder rechts," and Zeichen 222's only real variants are one-sided "links vorbei"/"rechts vorbei," with no bidirectional-choice variant ever having existed) - rewritten to test 214's real meaning, and its SVG redrawn from a fictional double-arrow to the real straight-or-right mandatory-direction pictogram. The same verification pass also found the DN-30 batch had assigned real Zusatzzeichen catalog numbers to the wrong plate text: **1010-51/53/60** are real weight/pedestrian/trailer-truck *qualifier* plates, not "Radfahrer frei"/"Mofa frei"/"Anlieger frei" as the batch had them, and **1020-30** is real "Anlieger frei," not a numbered-parking-permit plate. Fixed by moving each plate's text to its verified-correct number (Radfahrer frei → 1022-10, Anlieger frei → 1020-30, numbered parking permit → 1020-32) and rewriting zeichen-132 (which had used an unconfirmable "Mofa frei" claim - no reliable source ever verified a standalone Mofa-frei Zusatzzeichen number) to test 1010-53's real, confirmed meaning ("Fußgänger" qualifier) instead. All fixes Playwright-verified (correct images load for zeichen-68/130/132/136, new/redrawn SVGs render cleanly within frame), zero schema errors, zero broken image_ref links across all 124 sign files.
- **DN-45** — Added Klasse BE (car + trailer) as a second class option inside the existing Fuehrerschein module, per PO decision that BE is naturally an add-on to B (not an independent test like Motorrad/LKW were). 26 original questions covering the B-vs-BE weight distinction, coupling procedure, Stützlast, reversed-steering-with-trailer, mirror requirements, load securing, Auflaufbremse vs. coupled brakes, speed limits when towing, and trailer-snaking response - translated into all 12 locales (not DE/EN-pilot-only, since BE shares the same 12-locale content files as the existing 500 B questions, and a per-locale content gap would have rendered blank questions for non-DE/EN users). Generalized `questionMatchesScope()` (DN-42's region-stacking logic) with an optional per-class-option `extends` field in modules_manifest.json, so BE additively includes B's 500 questions (526 total when BE selected) while B alone and every other class-scoped module (Motorrad A1/A2/A, LKW C1/C/CE) keep their existing exact-match behavior completely unaffected - verified live (B: 500, BE: 526, Motorrad/LKW unchanged). Verified a non-DE/EN locale (French) correctly renders a BE-only question, and that exam mode remains available (526 ≥ 30).
- **DN-43** — Built the module intro wizard: a skippable 7-step walkthrough (`modules_manifest.json`'s new optional `intro.steps` block) shown once per device the first time a module with one is selected, and reopenable any time via a new header "ⓘ About this module" button. Implemented for Fuehrerschein only so far (DE/EN, same scope decision as sign alt text - falls back to EN for other UI languages). Follows the app's existing modal/dialog conventions exactly: `.exam-modal`/`.exam-modal-card` styling, `inert`-behind-dialog, focus-to-title on open, history.pushState/popstate-driven close (so the phone back-gesture works correctly, not just the in-app back button - the DN-13/DN-17 lesson about back-gesture handling). Info button and intro are both hidden for modules without an `intro` block (Motorrad/LKW/Angelschein currently have none). Verified end-to-end via Playwright: first-visit auto-show, dot progress (7 steps), Next/Back navigation, Skip, Start-on-last-step, doesn't re-show on a second visit to the same module once seen, reopens correctly via the info button, back-gesture (`page.go_back()`) closes it cleanly and un-inerts the header/main, and confirmed zero effect on modules without an intro.
- **DN-42** — PO decided region-scope should stack: made Angelschein's region filter additive (nationwide ALL + the selected region) instead of exact-match-only. Added `questionMatchesScope()` in app.js, scoped to `scopeKind === "region"` only so class-scoped modules (Fuehrerschein/Motorrad/LKW) keep their existing exact-match behavior unchanged (their content already lists every applicable class directly per question, unlike region's separate ALL code). Verified: ALL still shows 91, NRW now shows 96 (91+5), Motorrad A1 unaffected (86).
- **DN-11** — First real Angelschein content batch: 90 original DE/EN questions across 6 topics (fischkunde, gewaesserkunde, geraete, hege, tierschutz, fischereirecht) added to the existing 6-question architecture-proof seed, 96 questions total. Generated via 6 parallel content agents, each under the same AGENTS.md sourcing rule as Fuehrerschein/Motorrad/LKW (no official Fischerpruefung-catalog sourcing or paraphrasing — only independently verifiable fisheries biology/ecology/law facts, general legal-basis phrasing used instead of invented paragraph numbers where a specific citation couldn't be verified). 5 questions are NRW-region-scoped, the rest nationwide (ALL). Zero schema problems, zero id collisions with the existing seed. Verified end-to-end via a real browser click-through against both region options on the live deploy. `legal_review_status` remains "NOT legally reviewed" (DN-12). PO explicitly scoped alt-text translation (DN-28) as out of scope this round — sign images are fixed exam material, not learning-material text.
- **DN-41** — Sign-asset QA pass: audited all 122 StVO sign SVGs for text overflow/clipping, geometry extending past each sign's own border, and factual/design accuracy against the app's own question data (not just visual plausibility). Verified with real Playwright/Chromium screenshots + OCR, not just computed coordinate bounds — this caught a real regression a pure-geometry check missed (108/110 text left at a stale position after an earlier transform). Fixed ~10 text-overflow signs, ~17 geometry-overflow signs, and redrew 268 (tire+chain, was two abstract rings), 282 (five black diagonal stripes, was gray with a border glitch), 328 (Nothalte-/Pannenbucht, was a train-wagon icon), and 330-1/330-2 (removed spurious strut lines that read as a zebra crossing). Committed and deployed live.
- **DN-40** — Motorrad (Klasse A1/A2/A) and LKW (Klasse C1/C/CE) modules: 90 original DE/EN questions each, generated via parallel content agents under the same AGENTS.md sourcing constraints as Fuehrerschein (no amtlicher Fragenkatalog / commercial exam-prep sourcing). Wired through the existing DN-39 module-split build (new `modules_manifest.json` entries + two `split_module()` calls) with zero app.js changes needed, since `EXAM_TOPIC_DRAW` already namespaces per exam_type with a graceful uniform-fallback. **Explicitly independent from every other module and from each other** — separate exam_type, content files, `class_scope`, core/locale JSON, per product requirement that Motorrad and LKW must never share a question pool. `legal_review_status: "NOT legally reviewed"` on both, using Klasse-B exam mechanics (30q/10 error points) as a provisional placeholder pending real Klasse A/C parameter review. Verified end-to-end via a real headless-browser click-through (module picker → class picker → real question render) against both localhost and the live deploy.
- **DN-37/GitHub go-live** — Repo established at `github.com/tronicum/Zettacard`, dual-licensed (AGPLv3 code / CC BY-NC-SA 4.0 content, explicit split documented in LICENSE.md). Netlify site `drivenow-fahrschule` deployed and live at https://drivenow-fahrschule.netlify.app, serving all four modules. Full infra/deploy detail lives in the project's `netlify-deploy-status.md` doc, not duplicated here.
- **DN-39** — Module architecture: multi-exam-type, per-locale content split. Adds the "which exam are you studying for" selection layer requested to future-proof the app before adding more Fuehrerschein classes (LKW/C-CE, motorcycle/A) or other exam types (Angelschein) - folds in DN-36's locale-split recommendation since both restructurings touch the same data-loading code. Runtime data is now `app/data/modules.json` (manifest) + `app/data/<exam_type>/{core.json,locales/<lang>.json}`, generated by `data/build_modules.py` from the same flat editable master files content agents already edit (`pilot_questions.json`, plus a new `angelschein_seed.json`). A visitor now downloads ~250-400KB (their module's core + one locale) instead of the old ~5MB single `data.json` with all 12 locales inlined. Added a small (6-question) original Angelschein seed dataset - explicitly flagged as architecture-proof content, not a real DN-11 batch - including one region-scoped (NRW-only) question to exercise the region filter (Angelschein's scope dimension is state/region, unlike Fuehrerschein's class, since German fisheries law is state law). Exam mode's topic-draw weights are now namespaced per module, and the Exam button disables itself under the 30-question threshold rather than silently running a shorter "exam" against a small content pool. Also fixed the standalone-preview build script's fragile exact-string-replace patch (flagged as a risk in DN-9's own retro) with a generic fetch()-monkeypatch that doesn't need updating every time a data path changes. Playwright-verified: module picker on first load and on manual re-open, class/region filtering, per-locale reload on language switch, exam mode intact, dialog focus-on-open, zero console errors.
- **DN-29/DN-38** — Exam mode (training + simulated real exam) passed independent QA re-verification after the fix pass above: all 4 findings confirmed fixed live (copy no longer names Vorfahrt specifically in any of 12 locales, scoring code correctly left as-is, aria-pressed present, keyboard focus retained across all 30 questions in both modes), zero regressions in the existing flashcard view. **QA verdict: READY FOR NETLIFY DEPLOY (pending PO's own final look)** — see DN-37.
- **DN-34/DN-35** — Scaled from 287 to 500 questions (3 brand-new topics: Ladungssicherung und Mitfahrende, Verhalten bei Unfaellen und Erste Hilfe, Alkohol/Drogen und Fahrtuechtigkeit) and expanded locale coverage from 7 to 12 (added Turkish, French, Russian, Spanish, Italian; backfilled the 5 existing non-DE/EN locales for the 300 questions that were missing them). All 500 questions now ship in all 12 locales (6000 question-translations). Cross-batch consistency + Student Review caught and fixed a Zeichen 208/308 sign-name bug, 3 cross-batch duplicate questions, and several plain-language issues before merge.
- **DN-30** — Answered "do we cover all Verkehrszeichen?" by scaling the sign library from 35 to 122 distinct signs (87 new) across all four StVO Anlagen, with exactly one original question per sign (DE+EN; other 5 locales are a follow-up round — see DN-31). PO-agreed hybrid scope: full SVG coverage of the commonly-relevant catalog, not literal 100% of the 1500+ numbered official variants (impractical/untestable), and not multiple questions per sign. Generated via 4 parallel independent agents, each required to WebSearch-verify any sign it wasn't fully certain about — caught a real error before shipping (Zeichen 410 Europastrasse is green, not blue) and, as a side effect of the research, caught and fixed a pre-existing bug in already-shipped content (Zeichen 133/136 had been confused since DN-2 — 133 is "Fussgaenger," 136 is "Kinder," the two-children pictogram had been mislabeled as 133). Added 5 new shape templates the catalog needed (yellow/green/white rectangle families, yellow vs blue direction-arrow plates). Merge verified by regenerating all 122 SVGs and visually inspecting a full-grid screenshot, not just trusting the code. 287 total questions, schema-revalidated (image_ref → actual file existence check added this round) with zero errors.
- **DN-26b** — Fixed 3 real sign-shape/color bugs found via a user-reported bug (Zeichen 306/307 rendered as an upright square instead of a diamond, with an incorrect black border that the real sign doesn't have) plus a full audit of all 34 signs against the real StVO catalog requested in the same round: Zeichen 267 (no entry) was using the wrong prohibition template (red-ring circle instead of solid red disc with white bar), Zeichen 283/286 (Halt-/Haltverbot) were using the plain mandatory-sign template with no prohibition ring. All fixed in generate_signs.py, regenerated, and visually verified via a full 34-sign screenshot grid (not just code review) — twice, once by the fixing agent and once independently by me before trusting the report.
- **UX** — Redesigned the language picker per an independent UX review of the DN-9 7-language `<select>`: replaced 2-letter codes with native endonyms (the review's finding: codes are only decodable by someone already fluent in Latin-script abbreviations, which defeats the point for AR/ZH/HI readers). PO's flags-only idea was evaluated and specifically rejected by the review — flags represent countries, not languages, so a flags-only picker trades a literacy problem for a representation-accuracy problem (Arabic has no single flag; English/Chinese span many countries). Kept a generic 🌐 icon instead. Also added `navigator.language`-based first-load locale detection (previously always defaulted to German) and a per-locale (not all-languages-concatenated) aria-label.
- **DN-9** — Added 5 locales — Ukrainian, Polish, Arabic, Simplified Chinese, Hindi — on top of DE (canonical) + EN, across all 200 questions (question, all 4 options, explanation). Translated via 10 parallel independent agents (5 languages x 2 chunks), merged and schema-validated: zero gaps across 200 questions x 7 locales. Replaced the DE/EN two-button toggle with a `<select>` (doesn't scale past ~3 languages), added RTL support (`dir="rtl"` for Arabic only, plus a logical-property CSS fix so layout mirrors correctly), and UI_STRINGS/TOPIC_LABELS for all 7 languages. Sign/diagram alt text is still only DE/EN (see DN-28) with a safe fallback chain (requested locale → EN → DE) so nothing renders empty. Caught and fixed a real bug during verification: the standalone-preview build script's asset-inlining string-replace silently stopped matching after an unrelated app.js refactor, so images fell back to broken paths — build script now asserts its patches actually apply instead of failing silently.
- **DN-7/DN-8** — Scaled content from 100 to **200 questions**: 25 Allgemeines Verhalten, 25 more Verkehrszeichen (zeichen-26..50), 25 Autobahn und Ueberholen, 25 Parken und Halten. DE+EN, same schema, CC BY-NC-SA 4.0. Independent Student Review before merge caught and got fixed same-round: a real sign-number bug (Zeichen 277 used for two different real signs across autobahn-20/zeichen-29 — corrected), 5 near-duplicate question pairs (replaced the duplicate side of each with a distinct fact), and a high_stakes calibration gap (school-bus/children scenario bumped to match a comparable severity item). Added TOPIC_LABELS for the 3 new topics and verified via Playwright before commit (caught the missing-filter-label class of bug proactively this time, having been bitten by it in the DN-5/DN-6 round). Note: "Autobahn/Parken" go a bit beyond strict Grundstoff into Zusatzstoff-adjacent territory — folds DN-8's scope in without a separate PO scoping pass; flag if that needs revisiting.
- **DN-5/DN-6** — Scaled content from 50 to 100 questions: 25 Gefahrenlehre + 25 Umwelt und Technik, DE+EN, same fact-layer/text-layer schema, CC BY-NC-SA 4.0. Independently written from StVO/StVZO principles. Applied lessons from prior reviews from the start: plain-language DE phrasing (per the low-literacy Student check dimension) and plausible distractors. Went through an independent Student Review before Done (scored 50/50 cold but flagged systemic implausible-distractor and high_stakes-calibration issues, same class as DN-1's original finding) — fixed in the same round rather than filed for later: see DN-20/DN-22 below.
- **DN-18** — Fixed vorfahrt-08's EN/explanation mismatch ("nose to nose" vs "right side to right side").
- **DN-19** — Fixed DE grammar run-on in vorfahrt-11 option (a) and garbled stem in zeichen-04.
- **DN-20** — Rewrote implausible distractors flagged by Student Review, this round scoped to the new 50-question batch (umwelt-01..25 mostly, plus gefahr-13/16/20). The original pilot 50's distractors (Vorfahrt/Verkehrszeichen) are still open — see DN-20b below, split out so this doesn't silently read as "fully done."
- **DN-22** — Applied high_stakes/points recalibration to gefahr-16 (pedestrian hidden by parked car) and gefahr-24 (cyclist in truck blind spot) — both were under-weighted relative to their real danger, now 5pts/high_stakes:true like comparable items. zeichen-04 (the original DN-22 subject) still needs its own look — folded into DN-20b/DN-21 below since it's Verkehrszeichen-batch scope.
- **DN-24** — Fixed all findings from the first formal accessibility audit (WCAG 2.1 AA / BITV 2.0) and the first low-German-literacy Student check, both run 2026-08-04 as independent agents: dialog semantics + native `inert` focus trap on the detail view, `<html lang>` sync with the language toggle, real localized non-spoiler alt text for all signs/diagrams, 4 color-contrast fixes (dark accent, light correct-mark green, filter-pill text, badge background), and an explicit `:focus-visible` ring. Also fixed a self-caught integrity bug found while doing this work: the 7 Vorfahrt diagrams baked the answer (priority badges) into the pre-reveal image — split into plain/answer SVG variants. Verified via Playwright (focus/inert/lang/diagram-swap/keyboard walkthrough) and a light-theme screenshot pass. Both checks are now standing dimensions, see docs/PERSONAS.md. Supersedes the a11y-relevant half of DN-23 (focus-outline styling); DN-23's remaining items (safe-area-inset padding, option-row tap affordance hint) stay open below.
- **DN-3** — 7 original birds-eye scenario diagrams for the high_stakes Vorfahrt questions with no sign to illustrate, with numbered priority markers (1 = has right of way, 2 = must yield) so the diagram states the rule, not just the scene. Wired into the app. Visually verified via screenshot (not just XML-checked) — caught and fixed 2 real clarity bugs (a priority badge floating at an ambiguous point instead of next to the entity it applied to; two overlapping text labels).
- **DN-2** — 34 original sign SVGs, one per unique `image_ref` in the data (all Verkehrszeichen-topic signs plus every sign a Vorfahrt question cites), shape/color/category matching the StVO's own annexes. Wired into the app via `resolveImage()` in app.js. Visually verified — caught and fixed a real bug (one file had a duplicate SVG attribute, making it invalid XML, so it silently failed to load entirely — would have shipped a single broken image if only spot-checked instead of viewing the full grid).
- **DN-1** — Retroactive Student Review of DN-0, run as an independent agent (not me self-grading). Result: 50/50 answered correctly cold, but the review's real value was diagnostic — found distractors are too easy/implausible relative to the real exam (biggest fidelity gap), a DE/EN meaning mismatch in vorfahrt-08 ("nose to nose" vs. the explanation's "right side to right side" — could teach the wrong mental model), two DE grammar bugs (vorfahrt-11 option a, zeichen-04 stem), a near-duplicate pair (vorfahrt-11/12), and an unresolved ambiguity in whether zeichen-07/08 (high_stakes but not Vorfahrt-topic) count toward the "2+ wrong Vorfahrt" auto-fail rule. Findings split into DN-18 through DN-22 below.
- **DN-17** — Fixed all 3 P0 and 4 P1 findings from the first UX-persona review (see DN-13's UX entry below): back-gesture/history handling, list scroll preserved across navigation, focus+aria-live on answer reveal, non-color correct-answer marker, localStorage persistence for lang/filter, 44px+ tap targets, aria-pressed on toggles. Also shipped light/dark theme (not from the review, PO-requested) — caught and fixed one bug in that work myself before commit (header/badge colors hardcoded to dark theme, invisible in light mode).
- **DN-13** — Sprint 1 MVP: PWA question browser. Click through all 50 questions, filter by topic, switch DE/EN anywhere (list or detail view), reveal correct answer + explanation + legal citation on demand. Installable (manifest + icons), offline-capable (service worker precaches app shell + data.json). **Explicitly out of scope: exam simulation, scoring, pass/fail, timers, error-point tracking.** Supersedes/fulfills DN-10 (PWA scaffold). Files: `app/` (index.html, app.js, styles.css, data.json, manifest.json, service-worker.js, icons/). *(Student Review: passed. UX Review [new persona, see docs/PERSONAS.md]: found 3 P0s in its first run — fixed in DN-17.)*
- **DN-0** — Pilot batch: 25 Vorfahrt + 25 Verkehrszeichen MCQs, DE+EN, fact-layer/text-layer schema, CC BY-NC-SA 4.0 licensed. *(Student Review done retroactively per KANBAN.md retro log.)*

## Student Review

*(empty)*

## In Progress



## Ready

*(empty — pull from Backlog; DN-15 is now largely done as a side effect of DN-2/DN-3, remaining scope there is just future content batches without art yet)*

## Backlog

- **DN-49 follow-up idea** — user asked (2026-08-11) whether `pdf.js` could be used later: it's a viewer/renderer (displays existing PDFs), not a generator, so it doesn't replace `printCertificateAsPdf()`'s print-to-PDF approach (see DN-49 Phase 3 in Done above). Real future use: an in-app "preview the certificate before you print/save" step using `pdf.js` to render the PDF inline instead of going straight to the browser's print dialog blind. Not scoped further - only worth building if a user actually asks for a preview step.
- **DN-52** — Scoped, not yet built: a "kickstart learning journey" bridging a true beginner from zero to confident-enough-to-practice, rather than dropping every new user straight into a flashcard list or a 30-question exam. Full scoping in `docs/kickstart-learning-journey-scoping.md`: a shape/category primer (signs' shape+color already signal Gefahrzeichen/Verbotszeichen/Gebotszeichen/Richtzeichen, reusing the existing sign-category taxonomy), per-topic primers (reusing `TOPIC_LABELS`' existing topic keys), situational/scenario primers for topics that are really decision-trees in disguise (Vorfahrt as the flagship, reusing DN-3's existing birds-eye diagrams), a new lightweight "practice quiz" tier between flashcards and the real Exam Simulation (short, mixed-or-topic-scoped, no certificate stakes, but still feeding the existing Leitner spaced-repetition schedule via the same hook Exam Simulation already uses), and a kickstart path wizard extending the existing DN-43 module-intro mechanism into an explicit three-way choice ("learn the basics" / "flashcards" / "test yourself, diagnostically") rather than a silent default into a question list. Recommended build order: primers first (pure content, reuses existing infrastructure) -> practice-quiz mechanic -> path wizard tying it together -> roll out to every other module once the Fuehrerschein pilot proves the format. **PO decision (2026-08-08)**: an early, voluntary Exam Simulation pass DOES count as the module's real, certificate-worthy completion - no gating on this before Phase 3 anymore. PO's own reasoning: the whole point is not having to redo training already demonstrably mastered; the "someone could game a quick cert without really learning it" risk was explicitly considered and accepted as out of scope, since it's identical to the risk already inherent in any unproctored self-service exam attempt this app already allows today - this feature doesn't introduce a new exposure, just makes an existing one more visible. Remaining open questions for the PO, per the doc's section 9: decision-flow-vs-worked-examples per scenario topic; whether the wizard's return-visit dashboard belongs in the first build; Fuehrerschein-only pilot vs. multi-module (recommended: pilot first).
- **DN-51** — Smallest viable step (standalone JWT download button, 2026-08-07), the `achievement.id`/proof-shape OB3-conformance fixes (2026-08-08), the external-validator re-verification pass (2026-08-11), and the `jku`/`vc.id`/versioned-context conformance fixes (2026-08-11, see Done section above) are all DONE. Remaining, not yet built: a `did:web` issuer identity - the only fix that would additionally satisfy CertLister's and 1EdTech's *specific* signature-resolution implementations, which resolve `kid` only as a DID and don't honor the (correct, spec-standard) `jku` claim this round added - see `docs/open-badges-signing-setup.md`'s "Fixes applied 2026-08-11" section for the full detail; this is Option 4 in `docs/open-badges-signing-scoping.md`'s original architecture menu, the most involved of the four and not recommended as a first step there, so it stays a deliberate, scoped-but-not-built follow-up rather than something attempted piecemeal; the in-browser portion of `scripts/test_full_exam_badge.mjs` still can't run in this sandbox (Playwright/Chromium `net::ERR_CONNECTION_RESET`, confirmed still present 2026-08-11) and needs either a healthy sandbox or an outside runner; real wallet-side integration (an actual "add to Credly"/"add to Learner Credential Wallet" button, VC-API support) - deferred until a real user asks for it, per `docs/badge-wallet-portability-scoping.md`'s recommendation; and the open question of whether an opt-in (not required) email-claim-link flow is ever worth adding later.
- **DN-49 follow-up note** — that same AI Mode conversation independently proposed, as "the next logical step for your B2B model," a paid dashboard showing employers how many of their employees have completed which badge — this is the same idea already logged under DN-49 Phase 2 in `docs/paid-verifiable-certificates-scoping.md`, now with independent validation from a second source rather than only this project's own reasoning. Also flagged from that conversation: a claimed "3-15 € per employee/month" price range for LawPilots/Haufe Akademie/IS-FOX was **not sourced with a link** in that chat and directly conflicts with this project's own earlier WebSearch-backed finding that those same vendors publish no public pricing at all (`compliance-competitor-pricing-and-course-gaps.md`) — treat that specific number as unverified, not as a citable fact, until independently confirmed with a real source before it's used in any pitch deck or pricing decision.
- **DN-50** — Hinweisgeberschutz pilot (2026-08-08) and its scale-up to 40 questions/12 locales (2026-08-08, same day) are both DONE, see Done section above. **Correction (2026-08-13, board audit)**: this card's "LkSG deliberately NOT built" line is stale - LkSG shipped later (30 questions, DE+EN, live in production) and now has its own first-pass citation ledger as of today's DN-12 package consolidation (see Done above and the "DN-12" card below) - the mid-reform status this card originally flagged is still real and still tracked (a government bill to abolish the §10 reporting duty and narrow the fine catalogue was cabinet-approved Sept 2025, still in Bundestag committee as of early 2026; the module's own metadata and one of its questions already teach this pending-reform status explicitly rather than waiting for it to settle), it just no longer means "not built." Remaining scope: legal review (folds into DN-12). Two other topics flagged as *possible* but weaker/unverified candidates in the original research: AGG §12 (harassment-prevention) training (search results were thin, needs a dedicated follow-up before treating as a real gap) and ESG/CSRD, Exportkontrolle - Kartellrecht and Geldwäsche/AML are both now built (`kartellrecht`, `kyc_aml`), so only ESG/CSRD and Exportkontrolle remain unconfirmed white space. Brandschutzhelfer and Erste-Hilfe were also researched and explicitly NOT recommended as new modules despite having hard legal mandates too, since both require in-person practical hours the quiz-app format can't provide - a theory-only companion angle would need separate framing, not a straight new module.
- ~~**DN-46**~~ — DONE (2026-08-06, see Done section above). Polish-tier sign-icon findings from the proactive catalog audit (2026-08-05), lower confidence/severity than the 14 fixed in that round: 124 (Stau — plain rectangles don't read as queued cars), 151 (Bahnübergang — train icon reads as a generic box), 273 (Mindestabstand — vehicles too abstract to read as trucks), 117 (Seitenwind — pennant/flag instead of a proper windsock cone). All 4 fixed against the ADAC reference brochure. (Renumbered from a duplicate "DN-45" - DN-45 is already used above for the Klasse BE rollout.)
- ~~**DN-47**~~ — DONE (2026-08-06, see Done section above). PO supplied the official ADAC "Verkehrszeichen in Deutschland" reference brochure (adac.de, based on the real StVO Verkehrszeichenkatalog) as ground truth for sign-icon accuracy, explicitly to use as a template/reference for future redraws (not to trace/copy its specific artwork - same "verify against the official pictogram, draw an original simplified interpretation" discipline this project has used throughout, see generate_signs.py's own header comment). Cross-checked it against several of this session's own recent redraws and found concrete, confirmed corrections still needed (lower priority than DN-44/coverage work, per PO - logged here for whenever sign polish resumes); all items below were fixed in the DN-47/DN-46 sign round and the follow-up DN-46 closeout:
  - **272 (Verbot des Wendens)**: the official icon's curve is at the TOP (like an upside-down "U"/a "∩"), with both prongs hanging down and small arrow-flare tips at the bottom ends - this session's redraw has the curve at the BOTTOM instead (mirrored orientation). Needs flipping top-to-bottom, not a full redesign.
  - **268 (Schneekettenpflicht)**: official icon is a normal tire silhouette (side profile, visible tread/hub) with a diagonal DIAMOND cross-hatch (chain net) mesh overlaid across it - this session's redraw instead used a tire + radiating spoke-like lines + a plain inner ring, which reads more like a life-preserver than a tire+chain-mesh. Needs the mesh pattern redrawn as an actual diamond lattice over a proper tire shape.
  - **354 (Wasserschutzgebiet)**: official sign is a tanker-truck pictogram PLUS the literal printed words "Wasser-Schutzgebiet" underneath (matching the same icon+text-label convention as 242.x/244.x/356) - this session's redraw added wavy water-lines under the truck instead of text, which isn't what the real sign shows at all. Needs the water-lines replaced with an actual "Wasserschutzgebiet" text label (reuse `symC_text()`/the same rect_white_black_border-with-text approach already used elsewhere).
  - **363 (Polizei)**: official sign has the "Polizei" text sitting inside a distinct WHITE inset panel/plate within the blue square border (not white text directly on the blue background, which is what this session's redraw did) - needs a white background rect behind the text for a faithful match, similar in spirit to 356/358's white-panel-on-blue construction.
  - **242.2/244.2 ("Ende" zone plates)**: official signs greyscale the ENTIRE inset circle AND its pictogram/text (all go grey, not blue+black) and add a single diagonal line through just the circle - not a black/white dashed hatch band across the whole white plate, which is what this session's `sign_zone_plate(ended=True)` currently draws. Needs the inset circle's fill/pictogram/text color switched to grey for the Ende variant, and the hatch simplified to one clean diagonal line crossing just the circle.
  - **356 (Verkehrshelfer) / 358 (Erste Hilfe)**: worth double-checking against the brochure too (356 has a "Verkehrshelfer" text label under its pictogram in the official design; 358's red cross sits on a white inset panel, not directly on blue) - not fully verified this pass, flagged for whoever picks up DN-47/DN-46 next to confirm against the same brochure before redrawing.
  - Reference brochure fetched to `/tmp/adac_signs.pdf` this session (24 pages, covers Gefahrzeichen/Vorschriftzeichen/Richtzeichen/Verkehrseinrichtungen/Sonstige Zeichen/a Zusatzzeichen selection with full pictogram grids) - re-fetch from https://www.adac.de/-/media/pdf/vek/fachinformationen/infrastruktur/verkehrszeichen-adac-bro.pdf if picking this up in a future session, since the local temp copy won't persist.
  - **Closeout note (2026-08-11)**: re-picked up this card specifically to close the one item it had left unverified - 356/358. Re-fetched the same ADAC brochure to `/tmp/adac_signs.pdf` and this time read the actual pictogram-grid pages directly (Vorschriftzeichen p.7-8 for 268/272, Richtzeichen p.12-13 for 354/356/358/363), rather than relying on the earlier text-only page scan. Found that 356/358 had, in fact, already been fixed - by the very next commit after this card was written: the same-day "Fix all 86 flagged sign icons across 5 categories" round (`e042638`, 2026-08-06) closed 356/358 along with 84 other signs, but this BACKLOG card was never updated to reflect it, so it kept showing a stale "not fully verified" flag on already-done work. Re-verified all 6 items in this card end-to-end this session rather than trusting the git history alone: read `assets/generate_signs.py` in full (`sym_uturn_ban`, `sym_snow_chain`, `symC_water_truck`, `symC_police_panel`, `sign_zone_plate(ended=True)`, `symC_guard`, `sym_first_aid_cross`); confirmed `assets/batch_a/b/c/d_signs.py` are dead, unimported standalone files (nothing in the repo imports them - only `generate_signs.py`'s own internal `BATCH_A/B/C/D_SIGNS` dicts feed the real registry, so those stale sibling files can't silently reintroduce old artwork on a future run); re-ran `python3 assets/generate_signs.py` (zero diff against the committed `assets/signs/`+`app/assets/signs/` output, confirming the checked-in SVGs are already current, not stale); and rendered 272/268/354/363/242.2/244.2/356/358 via `cairosvg` into a spot-check grid, checked directly against the brochure's own pictogram pages. All 8 match the brochure: 272's curve sits at the top like an inverted "U" with both prongs hanging down and an arrow-flare tip on one; 268 is a proper tire silhouette (rim/hub/tread) with a diamond cross-hatch chain-mesh overlay, not a life-preserver; 354 shows the tanker-truck pictogram over a two-line "Wasser-/Schutzgebiet" text label (no water-lines); 363 has "Polizei" sitting on a white inset panel within the blue square; 242.2/244.2 are fully grey (circle + pictogram + text) with a single clean diagonal line crossing just the circle, not a hatch band across the whole plate; 356 has the guard-with-paddle pictogram plus a "Verkehrs-/helfer" label; 358 is a red cross on a white inset panel on blue. No code changes were needed this round - closing this card as fully verified against the brochure, not merely fixed.
- ~~**DN-18/DN-19**~~ — Already fixed in an earlier round (board was stale, listed here as if still open - see the Done section entries above, which predate this cleanup). Verified this round: vorfahrt-08's EN now correctly says "right side to right side" matching its explanation, vorfahrt-11's grammar and zeichen-04's stem both read cleanly. Removing the duplicate open-backlog entries.
- ~~**DN-20**~~ — DONE (folded into the 2026-08-06 round, see Done section above). Both the new-batch rewrite and the DN-20b retrofit onto the original pilot 50 happened in that same pass - this card and DN-20b below were left open by mistake, never checked off. Re-verified 2026-08-08 (board audit): spot-checked distractors across both vorfahrt-01..03 and zeichen-01..03 - all read as genuinely plausible wrong answers, not absurd ones.
- ~~**DN-21**~~ — Resolved as part of DN-29 (corrected after an initial wrong assumption, caught by DN-38 QA): the auto-fail rule counts ANY `high_stakes:true` question, not just `topic_code=="vorfahrt"` — this spans 6 topics in the real data (61 questions total), including zeichen-07/08, matching the real exam's broader "besonders gefahrenrelevante Fragen" category rather than a narrower Vorfahrt-only reading.
- ~~**DN-20b**~~ — DONE, see DN-20 above - stale duplicate card, same fix. Re-verified 2026-08-08: the 2026-08-06 Done entry also flagged 8 ids (`vorfahrt-04/05/06/09/13/17/24`, `zeichen-06`) whose DE wording changed enough to need a translation-sync check across the other 10 locales - checked `data/pilot_questions.json` directly, all 8 already have fully-synced, correct translations in every locale (no stale mismatch found), so that flagged follow-up needed no further action either.
- ~~**DN-22b**~~ — Resolved this round: zeichen-04 (children-playing warning) bumped from 4pts/not-high_stakes to 5pts/high_stakes:true, matching zeichen-07/08 (rail crossings) and the rest of the batch's severity calibration for genuinely safety-critical scenarios.
- ~~**DN-23**~~ — DONE (2026-08-08, see Done section above): safe-area-inset padding across all fixed/sticky full-bleed surfaces, plus a tap-affordance hint (badge + `:active` feedback) for the pre-reveal "try it yourself" option rows. (Focus-outline styling done earlier as part of DN-24.)
- ~~**DN-25**~~ — DONE, folded into the same 2026-08-06 round as DN-20/DN-20b (see Done section above - that entry explicitly covers both "more plausible distractors (DN-20b) and simplified DE wording for low-literacy readers... (DN-25)" for the same 42 ids). Only one low-literacy Student check has ever run (2026-08-04, see docs/KANBAN.md) and its findings were the ones addressed here - no newer/different literacy review exists that this card could still be referring to. Stale duplicate card, never checked off.
- ~~**DN-4**~~ — DONE (2026-08-06, see Done section above). Add multi-select (`multi_choice`) question type to the schema and generator, with at least 5 example questions using it.
- ~~**DN-27 (pilot)**~~ — 28 of 40 "gefahr" topic questions done this round (see Done above). Remaining scope: verhalten/umwelt/autobahn/parken topics (~140 more questions) still have no diagram illustrations, plus the other 12 "gefahr" questions could be revisited if a non-spatial illustration approach (e.g. an icon/pictogram rather than a scene) is ever wanted for them.
- ~~**DN-28**~~ — Originally out-of-scope by PO decision (sign/diagram alt text ruled fixed exam material, not learning-material text). **Revisited and reversed 2026-08-08, see Done section above**: PO asked whether all language packs were current, was told this was the one deliberate gap, asked to close it anyway - now done for SIGN_ALT/DIAGRAM_ALT and the Sign Reference catalog across all 12 locales, plus the unrelated MODULE_INTRO_STRINGS gap found in the same pass.
- ~~**DN-31**~~ — Already done, board was stale: verified this round that all 500 questions in `pilot_questions.json` (including all 87 DN-30 sign questions, zeichen-51 through zeichen-137) already carry all 12 locales with zero gaps — this was folded into DN-34/35's locale-expansion round and just never got checked off here.
- ~~**DN-33**~~ — Reviewed this round while fixing the 12 sign-icon reports: the placeholder names already used (Musterstrasse, Musterdorf, Musterstadt, Beispieldorf, Musterhausen) are the standard German idiom for "generic example," directly equivalent to English "Anytown"/"Example St." - a German reader already recognizes "Muster-"/"Beispiel-" as self-evidently fictional, same as no English speaker thinks "Anytown, USA" is a real town. "B 1" (401's route-number example) uses the same low-number convention official German driving materials use for generic examples, not a claim about the real B1. No change needed - closing as already adequately clear, not deferred as unresolved.
- **DN-12** — Professional legal review pass on all published content (StVO/StVZO citations, sign numbers, point weighting) — needed before any public/commercial release.
- ~~**DN-14**~~ — DONE (2026-08-07, see Done above): seen-tracking + manual star/bookmark, independent of a full exam pass, now implemented (localStorage-based, not IndexedDB — simpler and consistent with every other per-question feature in this app, e.g. the Leitner SRS boxes).
- **DN-15** — ~~Wire real sign/diagram images into the app~~ — done as a side effect of DN-2/DN-3 landing directly wired in. Remaining scope: wire images for future content batches (DN-5/6/7/8/11) once they get their own art.
- ~~**DN-16**~~ — DONE (2026-08-06, see Done section above, implemented as a 5-box Leitner system with localStorage rather than IndexedDB). Spaced-repetition learning mode, Leitner-system style: questions move between boxes (e.g. 1-5) based on right/wrong answers, higher boxes reviewed less frequently, box state persisted per-device.
- ~~**DN-36**~~ — Done as part of DN-39 (see Done above) - folded together since both were the same restructuring of the data-loading path.
- **DN-44** — New product direction (not driving/fishing licenses): recurring German workplace-compliance training modules — Datenschutz (GDPR/BDSG), Arbeitssicherheit (workplace safety law), the EU/German AI Act, and IT-Security. PO's stated goal: something a person completes once and can carry with them across employers, instead of redoing the same mandatory compliance training every time they change jobs. PO decisions: **4 fully separate modules** (not one module with topic options), and **infrastructure before content** — the completion/certificate system was built first for exactly this reason, and **the first real 20-question-per-module content pilot has now landed** (see Done above) - so DN-44 is no longer "not started," but the pilot batches are intentionally below exam-mode's 30-question threshold and have zero legal review, same status every other module starts at. PO has also explicitly confirmed **Open Badges 3.0 / W3C Verifiable Credentials alignment as a real product goal**, not just a convenient shape to borrow — meaning a real (cryptographically signed, third-party-verifiable) implementation is a genuine future direction to design toward, not just flavor text on a JSON file. What that would actually require, to scope later: an issuer keypair/signing authority (conflicts with today's zero-backend static-PWA architecture — would need at minimum a lightweight signing service, even a serverless one, since client-side-only signing can't be trusted by a third party), and a public issuer profile/verification method a badge-checking tool could resolve. Also still true: because Arbeitssicherheit legally requires "tätigkeitsbezogen" (activity-specific) training and the AI Act requires role-based tailoring, any portable credential — signed or not — can only certify the *general baseline* was completed; a new employer would still need to layer their own site-specific supplement, the same additive pattern already used for region/class scope (ALL+region, B+BE). Source material itself (GDPR/BDSG text, ArbSchG, the EU AI Act, IT-Grundschutz) is public law/standards, not a restricted proprietary catalog — friendlier sourcing situation than AGENTS.md's constraint 1 deals with for the license/fishing content today. **Next natural steps** — the question-count scale-up flagged here is DONE: verified 2026-08-08 (board audit) that all 4 modules (`data/{datenschutz,arbeitssicherheit,ki_act,it_sicherheit}_pilot.json`) already carry 40 questions each in all 12 locales, per the DN-48 round (2026-08-05) - this card's own wording just never got updated to reflect that. Only remaining item: a real legal review pass, which folds into DN-12's existing (deliberately deferred) urgency. The signing-architecture scoping conversation mentioned above is no longer open — real ES256-signed JWT credentials are live in production as of 2026-08-07 (see Done above), independently verified against the live JWKS and against two external OB3 validators, with the `achievement.id`/proof-shape conformance gaps also since fixed (2026-08-08, see Done above).
- **DN-49** — Phase 1 (0€ MVP) live in production; a paid tier remains deferred indefinitely until real demand shows up post-launch (see Done above for what shipped, and `docs/paid-verifiable-certificates-scoping.md` for the full scoping/decision history). Blobs scale-testing and the allowlist-drift bug it surfaced are DONE (2026-08-08, see Done above). **Correction (2026-08-13, board audit)**: this card's own "remaining, not yet done: a PDF-download variant" line was stale - Phase 3 (client-side PDF certificate download) already shipped 2026-08-11, see the detailed "DN-49 Phase 3" Done entry above (`printCertificateAsPdf()`, wired into both the "My certificates" panel and the exam-results screen, all 12 locales, Playwright-verified). It deliberately does NOT use jsPDF/html2canvas - the Done entry explains why (this is a zero-dependency, offline-first static PWA with no bundler; a CDN-loaded PDF library would either break offline use or require vendoring one for unproven demand) - so don't re-propose vendoring a PDF library here without new information that changes that tradeoff (e.g. a real B2B customer specifically asking for pixel-consistent server-rendered output, which the scoping doc's section 3.3 already names as the actual trigger for revisiting this). Only genuinely remaining item: the actual B2B pitch/outreach using this feature as the hook - **outreach/pitching is not something achievable by an AI agent's tools and should stay flagged as a human task, not attempted**. (Renumbered from a duplicate "DN-48" - that ID was already used above for the 2026-08-05 locale-coverage/rebrand round.)
